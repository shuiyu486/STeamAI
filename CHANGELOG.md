# Changelog

## Unreleased

### Added

- Batch 472 完成 default status product-path smoke closure：case-local nested cwd product-path smoke 现在在 `init -Apply` → `start -Apply` → `continue -Apply` → lane handoff 后额外验证默认 `status` 第一屏，锁定 legacy/default text 在 lane workspace 内同样输出 case metadata、Mission Commander summary、queue、lane executor、ledger counts、sections、handoff command 与 continue boundary，避免新会话必须记住 `-Format text` 才能接手。coverage 复用真实 case-local product path，不新增 runtime 行为；该批不改变 default/table/text/json status 语义，不执行 heavy-tool，不 replay adapter，不写 authority/confirmed，不新增 PowerShell runtime logic，不改变 release blocker 状态。
- Batch 471 完成 case status product-path smoke closure：case-local nested cwd product-path smoke 现在在 `init -Apply` → `start -Apply` → `continue -Apply` → lane handoff 后额外验证 `status -Format text` 第一屏，锁定 queue、lane executor、ledger counts、sections、handoff command 与 continue boundary 同屏可读，避免 status 投影后续回退为只能解析 JSON 或必须重跑 `overview`。coverage 复用真实 case-local product path，不新增 runtime 行为；该批不改变 status/overview/handoff/continue 语义，不执行 heavy-tool，不 replay adapter，不写 authority/confirmed，不新增 PowerShell runtime logic，不改变 release blocker 状态。
- Batch 470 完成 case status lane executor handoff closure：case-mode `status -Format json/text` 与默认 status text 的 `caseMission` 现在直接投影 lane executor snapshots（lane/label/status/workspace/executor/generation、ready/blocked、blocker counts、resume/handoff、commander state/primary、blocker reason 与 boundary lines），让 replacement executor 第一屏 status 不必回退 `overview` 或 `handoff` 就能判断每条 lane 的 ownership、workspace、blocked reasons 和首选 handoff/continue/reconcile 命令。coverage 锁定 ready lane executor handoff、blocked lane executor handoff、existing queue/ledger/blocker/evidence review handoff 与 `.rekit` snapshot no-write invariant；该批不改变 Mission Commander queue builder、overview/handoff/continue 语义，不执行 continue/handoff/heavy-tool，不 replay adapter，不写 case facts/board/authority/confirmed，不新增 PowerShell runtime logic，不改变 release blocker 状态。
- Batch 469 完成 case status queue bucket handoff closure：case-mode `status -Format text` 与默认 status text 的 `caseMission` 现在直接输出 Mission Commander action queue 的 total/unblocked/blocked/requiresReview/followUp/current，以及 current/unblocked/blocked/reviewRequired/followUp bucket action、reason 与 boundary lines，让 replacement executor 第一屏 status 不必解析 JSON 或从 next actions 手工重组 queue bucket，即可判断当前 evidence review、blocked reconcile、review-required 与 follow-up 顺序。coverage 锁定 ready case queue bucket、blocked/evidence review queue bucket、existing ledger/blocker/evidence review handoff 与 `.rekit` snapshot no-write invariant；该批不改变 `caseMission` JSON contract、Mission Commander queue builder、overview/handoff/continue 语义，不执行 continue/handoff/heavy-tool，不 replay adapter，不写 case facts/board/authority/confirmed，不新增 PowerShell runtime logic，不改变 release blocker 状态。
- Batch 468 完成 case status ledger progress handoff closure：case-mode `status -Format json/text` 与默认 status text 的 `caseMission` 现在直接投影 overview ledger/progress handoff（`factCounts`、`sections`、section event summaries 与 batch summaries），让 replacement executor 第一屏 status 即可看到 observations/requests/candidates/publications/pendingDecisions 总量、open candidates/pending gates 等 section 计数和最近事件摘要，而不必先回退 `overview` 才能判断 ledger/progress 状态。coverage 锁定 case status JSON/text/default ledger/progress handoff、existing blocker/evidence review handoff 与 `.rekit` snapshot no-write invariant；该批不改变 overview/handoff/continue 语义，不执行 continue/handoff/heavy-tool，不 replay adapter，不写 case facts/board/authority/confirmed，不新增 PowerShell runtime logic，不改变 release blocker 状态。
- Batch 467 完成 case status blocker brief handoff closure：case-mode `status -Format json/text` 与默认 status text 的 `caseMission` 现在直接投影 Mission brief blocker lists（`readyLanes`、`blockedLanes`、`pendingGates`、`authorizedGates`、`openDecisions`、`interventions`），让 replacement executor 第一屏 status 即可看到被 pending-gate/intervention/open-decision 阻塞的 lane、具体 gate/decision/intervention 行，并和 execution evidence review / action queue / no-continue 边界同屏接手。coverage 锁定 case status JSON/text/default blocker brief handoff、evidence review handoff 与 `.rekit` snapshot no-write invariant；该批不改变 Mission brief builder、overview/handoff/continue 语义，不执行 continue/handoff/heavy-tool，不 replay adapter，不写 case facts/board/authority/confirmed，不新增 PowerShell runtime logic，不改变 release blocker 状态。
- Batch 466 完成 case status execution evidence review handoff closure：case-mode `status -Format json/text` 与默认 status text 的 `caseMission` 现在直接投影 `executionEvidenceReview[]` 明细，包含 gateEventId、action、outputRefs/evidenceRefs、review/handoff command、Mission Commander evidence action、follow-through outcome `when`/`evidence` 与 no-replay/no-authority boundary，让 replacement executor 第一屏 status 即可判断先 review 哪条 authorized observation evidence，而不必再回退 overview/handoff。coverage 锁定 case status JSON/text/default evidence review handoff、ready case empty projection 与 `.rekit` snapshot no-write invariant；该批不改变 evidence review builder、overview/handoff/continue 语义，不执行 continue/handoff/heavy-tool，不 replay adapter，不写 case facts/board/authority/confirmed，不新增 PowerShell runtime logic，不改变 release blocker 状态。
- Batch 465 完成 case status Mission Commander handoff closure：case-mode `status -Format json/text` 与默认 status text 现在直接输出 read-only `caseMission`，复用 overview 的 Mission brief、Mission Commander action queue、next actions、execution evidence review count、handoff preview/apply command 与 continue 必须先 WhatIf 的边界；missing-board 场景只提示运行 `overview` 或 `start -Apply` 初始化 Mission Commander state，不静默创建 `.rekit/board.json`。coverage 锁定 case status JSON/text/default handoff 与 missing-board no-write invariant；该批不改变 overview/handoff/continue 语义、不执行 continue/handoff/heavy-tool、不写 case facts/board/authority/confirmed、不新增 PowerShell runtime logic、不改变 release blocker 状态。
- Batch 464 完成 status project handoff closure：kit-mode `status -Format json/text` 现在直接投影短 `projectHandoff`，包含按需 read-first 路由、latest batch goal/validation、known gaps、release minimum validation commands 与下一步，让新会话只运行 `status` 就能看到当前进度、阻塞类别、验证门禁和接手路线；同时恢复 `docs/go-first-convergence-plan.md` 顶部对 `docs/mission-control-product-direction.md` 的北极星链接，修复本地 manifest 不变量测试。该批只增强 read-only status handoff 与文档链接，不改 case-mode status、release-check contract、sync/promote/workstream/ledger/gate 语义，不执行 heavy-tool、不写 authority/confirmed、不改变 release blocker 状态。
- Batch 463 完成 documentation routing pressure reduction closure：`docs/context-routing.md` 现在明确文档索引、推荐话术和接手清单都不是默认必读清单，新增文档必须声明按需入口和不应默认读取的内容；README、根 `CLAUDE.md`、common context-budget policy、pack reference template、Agent Team usage、reference absorption、vision、Mission Control product direction 与 Go-first convergence 的旧 read-first 话术已收敛到 `CLAUDE.md` + `docs/context-routing.md` + 当前场景顶部区，避免新会话绕过 router 串读长文档。该批只调整文档路由和渐进式披露规则，不改 runtime、不执行 heavy-tool、不写 authority/confirmed、不改变 release blocker 状态。
- Batch 462 完成 continue digest execution evidence follow-through detail closure：`/rekit continue -Apply` 的 run `digest.md` 现在在 `execution evidence review` follow-through outcome 中直接输出 outcome `when` 与逐条 `evidence` refs，让 Mission Commander / replacement executor 只读 continue run digest 时也不必回退 JSON、overview、handoff 或 lane `RESUME.md`，便可判断 recorded-evidence-review 与 boundary-or-escalation-review 的适用条件和证据 refs。coverage 锁定 continue digest normal/escalated evidence follow-through when/evidence、existing continue status/checkpoint JSON contract、Mission Commander next actions、evidence-first ordering、no replay/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 461 完成 lane RESUME execution evidence follow-through detail closure：lane `RESUME.md` 的 `execution evidence review` follow-through outcome 现在直接输出 outcome `when` 与逐条 `evidence` refs，让 replacement executor 只读 lane-local resume 时也不必回退 JSON、overview 或 project/lane handoff，便可判断 recorded-evidence-review 与 boundary-or-escalation-review 的适用条件和证据 refs。coverage 锁定 lane resume normal/escalated evidence follow-through when/evidence、typed checkpoint JSON contract、existing Mission Commander next actions、evidence-first ordering、no replay/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 460 完成 overview execution evidence follow-through detail closure：`overview -Format text` 的 Execution evidence review section 现在在每个 follow-through outcome 后直接输出 outcome `when` 与逐条 `evidence` refs，让 Mission Commander / replacement executor 只看 overview terminal handoff 时也不必回退 JSON 或 project/lane handoff，便可判断 recorded-evidence-review 与 boundary-or-escalation-review 的适用条件和证据 refs。coverage 锁定 overview text normal/escalated evidence follow-through when/evidence、existing overview JSON contract、executionEvidenceReview followThrough/actionQueue、evidence-first ordering、no replay/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 459 完成 execution evidence handoff follow-through detail closure：project/lane handoff Markdown 现在在已记录 authorized execution observation evidence 的 `followThrough` outcome 后直接输出 outcome `when` 与 `evidence` lines；project handoff 使用 owner-qualified `evidence follow-through when/evidence` 行，lane handoff 的 execution evidence review section 使用 `when:` / `evidence:` 行，让 Mission Commander / replacement executor 接手 case-local handoff 时不必回退 JSON 才能判断 recorded-evidence-review 与 boundary-or-escalation-review 的适用条件和证据 refs。coverage 锁定 project/lane handoff Markdown、existing handoff JSON contract、executionEvidenceReview followThrough/actionQueue、evidence-first ordering、no replay/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 458 完成 authorized execution follow-through text evidence closure：`gate -ExecutionReportContract/-ValidateExecutionReport/-Apply -GateEventId ... -Format text` 现在在 authorizedExecutionFollowThrough outcome text 中直接输出 outcome `when` 与 `evidence` lines，覆盖 adapter report contract 的 write/validate、valid record、invalid repair，以及 validation valid/invalid 和 execution evidence record handoff，让 Mission Commander / replacement executor 不必回退 JSON 才能判断每个 authorized execution follow-through outcome 的适用条件与证据要求。coverage 锁定 adapter contract/validation/execution evidence text、existing follow-through action/repair/verification/boundary lines、JSON contract、read-only validation、bounded observation evidence record、no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 457 完成 promote candidates cleanup/reconsume text identity closure：`promote -CreateCandidates -Format text` 在保留既有 cleanup action / reconsume command / reconsume boundary legacy lines 的同时，新增 cleanup action detail（path/candidatePath/indexPath/action）、reconsume command detail（check name + command）与 reconsume boundary detail（check name + boundary），让 Mission Commander / replacement executor 处理 candidate cleanup 与 fresh/attached reconsume 时不再把无 owner 的命令/边界行手工匹配回 cleanupTargets 或 verificationChecklist。coverage 锁定 WhatIf 与 actual create-candidates text detail、legacy text compatibility、existing JSON contract、candidate generation、manual merge/cleanup/reconsume guidance-only、no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 456 完成 promote candidates decision text detail closure：`promote -CreateCandidates -Format text` 现在在 pack-memory decision follow-through / cleanup / reconsume handoff 中直接输出 decision outcome 的 when/evidence、follow-through boundary、top-level reconsume commands/boundaries，以及 reconsume check 的 when/evidence，让 Mission Commander / replacement executor 判断 accept/reject/superseded/blocked/not-needed 条件、证据和 reconsume 验证时不必回退 JSON。coverage 锁定 WhatIf 与 actual create-candidates text detail、existing JSON contract、candidate generation、manual merge/cleanup guidance-only、no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 455 完成 summary artifact reviewer result handoff closure：`plan-subagents` 写出的 `summary.md` shard handoff 区域现在直接输出 reviewer result path、可复制 reviewer result JSON skeleton、routeOutput required field hints，以及 packet/route/shard binding guidance，让 Mission Commander / replacement executor 只读取 review summary artifact 时也不必从长 `dispatchPrompt` 或 `packet.json` 中再手工抽取 strict `ReviewerResult`。coverage 锁定 summary artifact skeleton、routeOutput field hints、packetId/routeId/shardId binding、existing dispatch/intake/writeback/no auto-spawn/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 454 完成 reviewer dispatch prompt skeleton closure：`plan-subagents` 生成的 shard `dispatchPrompt` 与 packet shard prompt 现在直接要求 read-only reviewer 返回单个 `ReviewerResult` JSON object，而不是 routeOutput alone；dispatch prompt 内嵌 reviewer result JSON skeleton、routeOutput required field hints、packet/route/shard/session replacement guidance，以及 top-level decision/confidence/evidenceRefs 与 routeOutput 的绑定说明。coverage 锁定 packet shard prompt 不再出现旧式 `Return the route output contract only`、dispatchPrompt skeleton/hints/no-write/no-heavy/no authority/confirmed/no PowerShell runtime logic 边界。
- Batch 453 完成 reviewer dispatch result skeleton text closure：`plan-subagents -Format text` 现在在每个 shard handoff 的 strict reviewer result contract 后直接输出可复制的 reviewer result JSON skeleton（含 packetId、routeId、shardId、items、reviewerSession、decision/confidence/summary、evidenceRefs/risks/conflicts、recommendedVerdict 与 routeOutput）以及逐 routeOutput required field 的 valueHint，让 Mission Commander / replacement executor 在 dispatch read-only reviewer 或准备 strict intake 时不必打开 packet/summary 或解析 planning JSON 才能构造符合 `ReviewerResult` / route output contract 的单对象结果。coverage 锁定 planning text skeleton、routeOutput field hints、existing reviewer orchestration/shard handoff text、existing reviewer intake text compatibility、no auto-spawn/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 452 完成 reviewer intake nested note text reuse closure：`plan-subagents ... -ReviewerResultPath ... -Format text` 现在在 verification / decision writeback checkpoint 旁直接复用 Batch 451 `note` text handoff，输出 nested `note.AppendResult` 的 mutation/applied/reason/eventId/path、事件字段/evidence refs、Mission brief、当前 executor action / Mission Commander next actions 与 WhatIf would-action delta，让 Mission Commander / replacement executor 在 reviewer intake preview、apply recovery 或 already-complete 路径中不必回退 JSON 才能判断 verification-before-decision 的 note-level 当前/预期/重复写回状态。coverage 锁定 reviewer-intake WhatIf text、already-complete duplicate text、partial recovery text、现有 reviewer intake JSON compatibility、no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 451 完成 note append explicit text handoff closure：`note -Format text` 现在在 append-only fact event 写入或 `-WhatIf` 预览后直接输出 note summary（mutation/applied/reason/eventId/path/kind/lane/subject）、target、事件字段/evidence refs/related refs、Mission brief、当前 executor action、当前 Mission Commander action/next actions，以及 WhatIf would executor action / would Mission Commander next actions，让 Mission Commander / replacement executor 在记录 verification/candidate/decision/intervention 等 ledger 事件后不必解析 JSON 才能判断 post-write 或 would-write handoff。coverage 锁定 actual append text、WhatIf text/no-write、no JSON object、默认 JSON compatibility、`note -List` legacy table/text/tsv compatibility、strict duplicate eventId/lane guard、no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 450 完成 overview explicit text operational handoff closure：`overview -Format text` 现在从 legacy table/tsv render 拆出，直接输出 overview summary、lane summaries、fact counts、Mission brief、逐 lane executor action、Mission Commander action queue/current/buckets、Mission Commander next actions、execution evidence review、section counts/events 与 next steps，让 Mission Commander / replacement executor 在 case-local/nested cwd product path 中不必回退 JSON 或 legacy prose 才能确定当前可执行 action、ready/blocked lanes、evidence review 与 ledger section state。coverage 锁定 case-local nested `overview -Format text`、no JSON object、legacy table compatibility、existing JSON inventory compatibility、read-only/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 449 完成 release-check public surface/deprecation text inventory closure：`release-check -Format text` 现在在 release handoff 之外直接输出 Go-native public surface command groups/profiles/boundaries/policies/facade prerequisites、PowerShell deprecation command/module/freeze/blocked/retired/public façade/reference inventory、public façade removal prerequisites/plan/deletion gates/execution steps/boundary/recovery/docs/impact migration targets、case shim readiness phrases/forbidden/boundaries 与 public default docs readiness documents/phrases/forbidden/boundaries，让 Mission Commander / replacement executor 不必回退 JSON 或 legacy table 才能复核 public path、PowerShell-free default、retained façade blocker 与 default docs readiness。coverage 锁定 explicit text output、不 emit JSON object、legacy table/tsv compatibility、JSON inventory compatibility、read-only/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 448 完成 release-check text release handoff closure：`release-check -Format text` 现在输出 release-check summary（mutation/ready/gateProfile/requiredCommands/documents/packs/boundaries/knownGaps/warnings）、CI release gate inventory 与 `inventory-ready-not-remote-ci-green` boundary、required commands、required docs、heavy action inventory、release handoff readFirst/signals/latest batch/release notes/pack maturity/validation/known gaps/next actions，以及 full known-gap detail，让 Mission Commander / replacement executor 在 release 判断和交接时不必解析 JSON 才能区分 inventory ready、本地验证最小集与远程 CI green。coverage 锁定 explicit text output、不 emit JSON、legacy table/tsv compatibility、JSON inventory compatibility、read-only/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 447 完成 read-only health/inventory text handoff closure：`status -Format text` 现在直接输出 status summary（mutation/mode/targetProvided/pack/target/runtimeRoot/templateRoot）、manifest summary、case metadata 与 case shim readiness counts/warnings；`doctor` / `validate -Format text` 现在输出 command/mode/pack/target/row count/summary 以及逐 row file/bytes/limit；`packs -Format text` 现在输出 pack count、逐 pack manifest/maturity/schema/managed/template/local/promote/tooling/prompts/routes/heavyToolGates/authority/version/description 与 heavy action names。coverage 锁定 explicit text output、不 emit JSON、default/table/tsv compatibility、JSON inventory compatibility、read-only/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 446 完成 case lifecycle text handoff closure：`attach` / `repair` / `init` / `bootstrap` 的 `-WhatIf/-Apply -Format text` 现在直接输出 lifecycle summary（mutation/reviewRequired/requiresConfirmation/applied/writes/blocked/moved/metadataSource/backupRoot/caseRoot/repoRoot/pack/projectName）、逐 write 的 path/kind/action/source/target/backup、blocked actions 与 nextSteps，让 Mission Commander / replacement executor 在新 case 初始化、旧 case 接入、移动修复和兼容 bootstrap 后不再必须解析 JSON 才能复核 metadata/shim/state、managed docs、blocked scope 与后续 doctor/sync handoff。coverage 锁定 preview/apply text、bootstrap command identity、默认 JSON compatibility、case write semantics 不变、no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 445 完成 promote apply text handoff closure：`promote -Apply/-WhatIf -Format text` 现在在 case -> kit pack write preview 或实际 bounded promote 后直接输出 apply summary（mutation/applied/changed/blocked/skipped/writes/requiresReview/cleanup/backupRoot）、逐 write 的 path/kind/action/source/target/backup/reason、pack validation rows、denied actions 与 nextSteps，让 Mission Commander / replacement executor 不再必须解析 JSON 才能复核 would-promote/promote、blocked deny、backupRoot、doctor validation 与 cleanup handoff。coverage 锁定 WhatIf text、actual apply text、默认 JSON compatibility、candidate workflow 不变、no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 444 完成 sync/promote review plan text handoff closure：`sync -Format text` 与 `promote -Format text` 现在在 non-apply review-first path 直接输出 review plan summary（direction/mutation/changed/blocked/reviewRequired/items/toolingItems/manifest）与逐项 item/tooling item 的 path/kind/action/risk/direction/recommendation、paths、hashes、deny violations 与 replacement counts，让 Mission Commander / replacement executor 不再必须解析 JSON 才能先复核 kit->case / case->kit review scope。coverage 锁定 sync/promote review plan text item detail、默认 JSON compatibility、review artifact behavior 不变、no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 443 完成 update apply command identity parity：`update -Apply/-WhatIf` 现在在 sync/update 共享 Go-native apply path 中保留 `update` command identity，JSON/text apply summary、write detail prefix 与 WhatIf nextStep 均提示 re-run `update -Apply`，让 Mission Commander / replacement executor 不再把 update handoff 误读成 sync handoff。coverage 锁定 update WhatIf text、update WhatIf JSON、sync default compatibility、no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 442 完成 sync apply text handoff closure：`sync -Apply/-WhatIf -Format text` 现在在 Go-native CLI terminal path 直接打印 apply summary（mutation/applied/writes/backupRoot）、每个 write 的 path/kind/action/source/target/backup 与 nextSteps，让 Mission Commander / replacement executor 在 kit->case review-first 预览或执行写入后不再必须解析 JSON 才能复核 managed-file、template-file、managed-block、metadata/shim/state 写入范围与 backup handoff。coverage 锁定 sync WhatIf text preview、actual sync apply text、default JSON compatibility、no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 441 完成 reviewer intake note event text detail closure：`plan-subagents -ReviewerResultPath ... -WhatIf/-Apply -Format text` 现在在 verification/decision writeback lines 后直接打印 nested `note.AppendResult` event 的 kind、eventId、applied、subject、target、lane、confidence、evidenceRefs、summary，以及 verdict/decision/reason、packetId/routeId/shardId、reviewerSession 与 owner binding fields，让 Mission Commander / replacement executor 不再必须解析 JSON 才能复核 verification-before-decision note payload 与 reviewer provenance。coverage 锁定 reviewer intake WhatIf note event text detail、existing reviewer result/checkpoint/post-validation/action queue/next-action text、no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 440 完成 reviewer intake partial writeback checkpoint text closure：`plan-subagents -ReviewerResultPath ... -Apply -Format text` 在 reviewer intake text 中现在直接打印 verification/decision writeback checkpoint（status、verificationApplied/eventId、decisionApplied/eventId），让 Mission Commander / replacement executor 在 verification-recorded 等 partial recovery 状态下不解析 JSON 也能确认已落账 verification、未落账 decision 与可安全重试的同一 apply command。coverage 锁定 partial recovery text checkpoint、existing commander retry/action queue/next-action text、verification-before-decision/no hand-written decision/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 439 完成 reviewer intake result text detail closure：`plan-subagents -ReviewerResultPath ... -WhatIf/-Apply -Format text` 现在在 intake status 后直接打印 reviewer result 的 packetId、routeId、shard、decision、confidence、reviewerSession、recommendedVerdict、summary、items、evidenceRefs、risks、conflicts 与 sorted routeOutput key/value，让 Mission Commander / replacement executor 不再必须解析 reviewer JSON 才能复核 reviewer output provenance、read-only tool_scope、next_action 与 verification-before-decision writeback 输入。coverage 锁定 reviewer intake WhatIf/already-complete text result detail、existing post-validation/action queue/next-action text、no reviewer file/ledger write、no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 438 完成 execution evidence record text detail closure：`gate -Apply -GateEventId ... -ExecutionStatus ... -Format text` 在 execution evidence record result 中现在直接打印 recorded observation 的 subject、summary、target、recordRequired、executionReportPath、actualBudget、outputRefs/evidenceRefs、boundaryHits 与 escalation，让 Mission Commander / replacement executor 在刚记录 bounded observation evidence 后不再必须解析 JSON 或重跑 overview 才能 review evidence payload 与 no-replay boundary。coverage 锁定 execution evidence detail/budget/output refs text、existing evidence commander action/action queue/next-action text、no replay/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 437 完成 adapter report validation valid sidecar text closure：`gate -ValidateExecutionReport -Format text` 在 valid sidecar path 现在直接打印 normalized adapter report sidecar identity（kind、adapterId、action、status、gateEventId）、actualBudget、outputRefs/evidenceRefs、boundaryHits、escalation 与 summary，让 Mission Commander / replacement executor 不再必须解析 JSON 才能 review 已验证 adapter report 并准备 bounded observation record。coverage 锁定 valid sidecar text handoff、existing validation follow-through/action queue/next-action text、read-only/no observation/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 436 完成 adapter report validation repair hint text detail closure：`gate -ValidateExecutionReport -Format text` 的 invalid sidecar repair hint 现在直接打印 stable code/stage、fields、allowedValues、allowedOutputPaths、allowedStopConditions、maxBytes、escalateToMain 与 detail，让 Mission Commander / replacement executor 不再必须解析 JSON 才能按失败类型修 sidecar、确认 recordBlocked/rerunValidation 与避免 premature record。coverage 锁定 boundary-marker-missing repair hint text detail、existing validation follow-through/action queue/next-action text、read-only/no observation/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 435 完成 plan-subagents reviewer orchestration lifecycle text closure：`plan-subagents -Format text` 现在在 top-level reviewer orchestration summary 后直接打印 orchestration scope、packet/result root identity、owner binding、lifecycle steps（inputs、mustPass、nextOnSuccess/Failure、action）、runtime boundary 与 completion criteria，让 terminal Mission Commander / replacement executor 不再必须解析 JSON 才能确认 reviewer dispatch→collect→preview→apply→post-validation 的完整 orchestration lifecycle。coverage 锁定 orchestration scope/owner/lifecycle/boundary/completion text、existing shard handoff/action queue/next-action text、no auto-spawn/no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 434 完成 plan-subagents owner/writeback text operational closure：`plan-subagents -Format text` 的 shard handoff 现在继续打印 owner binding（target lane、binding mode、current executor、executor generation、requiredForIntake、spawn owner、runtime session boundary、可选 takeover provenance）、reviewer writeback handoff、main-agent next action、writeback step `mustPass[]` 与 command binding `source`，让 terminal Mission Commander / replacement executor 在 reviewer dispatch/intake 前不再必须解析 JSON 才能确认 lane ownership、session boundary、preview/apply blocking conditions 与 command provenance。coverage 锁定 owner binding/writeback/mustPass/source text、existing shard handoff/action queue/next-action text、no auto-spawn/no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 433 完成 plan-subagents shard handoff text consumption closure：`plan-subagents -Format text` 现在在 reviewer orchestration/per-shard dispatch、Mission Commander action queue 与 next-action lines 之外，直接打印 `shardHandoffs[]` 的 reviewerResultPath/items/expectedOutput、read-only boundary、dispatch prompt、strict `reviewerResultContract`、`reviewerIntakeCommands` preview/apply handoff、preview checks/blocked outputs、intake checklist、reviewer decision mappings、conflict handling、writeback sequence、post-review merge、completion/failure criteria，让 terminal Mission Commander / replacement executor 不再必须解析 JSON packet 或打开 `summary.md` 才能安全 dispatch read-only reviewers 和准备 strict intake。coverage 锁定 planning text shard handoff/contract/intake/writeback/post-review lines、existing action queue/next-action text、no auto-spawn/no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 432 完成 adapter report contract live-validation text closure：`gate -ExecutionReportContract -Format text` 现在在 validate/record commands、authorizedExecutionFollowThrough、Mission Commander action queue 与 next-action lines 之外，直接打印 `liveValidation` handoff：invocation cwd/authorized workspaces/report file/case-relative path/replay behavior、sidecar template refs/boundary/summary fields、pack tooling adapter candidates 的 id/status/entry/purpose/sideEffects/report/evidence guidance/stopCondition hints/recordOnlyAfterGate，以及 read-only/strict-record notes，让 replacement executor 不再必须解析 JSON 才能写入 sidecar、选择 adapter、运行 read-only validation 并避免 premature record。coverage 锁定 default contract text、nested output workspace text、generic-binary-re adapter candidate product-path text、existing validation/record next-action text、read-only/no observation/no authority/confirmed/no-heavy/no PowerShell runtime logic 边界。
- Batch 431 完成 promote candidates review checklist text closure：`promote -CreateCandidates -Format text`（含 WhatIf preview 与 actual candidate generation）现在在 mainAgentExecutionPlan、decision follow-through、cleanup/reconsume、Mission Commander action queue 与 next-action lines 之外，直接打印 `reviewPlan.reviewItems[]` 与 `reviewPlan.decisionChecklist[]` 的 per-candidate decision/action/hint/checklist/verification/boundary lines，让 terminal Mission Commander / replacement executor 不再必须解析 JSON 才能执行逐项 review、accept/reject/superseded、blocked/non-promotable 与 tooling reconsume checklist。coverage 锁定 WhatIf/actual text review item/checklist 输出、existing execution plan/follow-through/action queue/next-action text、no merge/no cleanup/no init/no doctor/no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 430 完成 promote candidates WhatIf text preview parity：`promote -CreateCandidates -WhatIf -Format text` 现在使用同一 terminal handoff 输出，直接打印 preview candidate identity、`reviewPlan.mainAgentExecutionPlan[]` 的 materialize/review/cleanup/reconsume steps、decision follow-through、cleanup/reconsume、Mission Commander action queue 与 next-action lines，让 Mission Commander / replacement executor 在确认 candidate generation scope 前不再必须解析 JSON。coverage 锁定 WhatIf text preview、materialize-candidates bounded command、WhatIf no-write boundary、existing actual create-candidates text execution plan、unsupported format guard、no merge/no cleanup/no init/no doctor/no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 429 完成 promote candidates main-agent execution plan text consumption closure：`promote -CreateCandidates -Format text` 现在在 decision follow-through、cleanup/reconsume、Mission Commander action queue 与 next-action lines 之外，直接打印 `reviewPlan.mainAgentExecutionPlan[]` 的 step/when/expected/appliesTo/actions/commands/evidence/boundary，让 terminal Mission Commander / replacement executor 不再必须解析 JSON 才能按 materialize、review decisions、cleanup、pack doctor、fresh-case reconsume 与 attached-case reconsume 的 bounded plan 接续。coverage 锁定 create-candidates text execution plan、existing decision follow-through/action queue/next-action text、no merge/no cleanup/no init/no doctor/no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 428 完成 reviewer-intake post-validation text consumption closure：reviewer-intake `-Format text` 的 post-validation section 现在直接打印 overview verification/decision totals、doctor row count、handoff lane/project/executor snapshot、handoff action queue summary/current 以及 post-validation next-action lines，让 terminal Mission Commander / replacement executor 不再必须解析 JSON 的 nested `postValidation.overview` / `postValidation.handoff` 或额外运行 overview/handoff/doctor 才能确认 writeback 后的可接续 action。coverage 锁定 preview postValidation text、already-complete postValidation handoff queue/current/next actions、existing reviewer-intake commander queue、no auto-spawn/no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 427 完成 plan-subagents / reviewer-intake CLI text action-queue parity：`plan-subagents -Format text` 现在直接输出 review artifact identity、reviewer orchestration、per-shard dispatch、Mission Commander action、`missionCommanderActionQueue` summary/current 与完整 next-action lines；reviewer-intake `-WhatIf/-Apply -Format text` 也输出 intake status、orchestration snapshot、verification/decision/post-validation、Mission Commander action、action queue 与 next actions，让 Mission Commander / replacement executor 在 terminal text path 中不再必须解析 JSON 或打开 `summary.md` 才能消费 reviewer dispatch/intake current/unblocked/blocked/reviewRequired/followUp buckets。coverage 锁定 planning text 与 reviewer-intake text parity、existing JSON contract、no auto-spawn/no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 426 完成 reviewer orchestration dispatch/intake action consumption closure：`plan-subagents` planning result、packet `reviewerOrchestration` 与 `summary.md` 现在在既有 `missionCommanderAction` / `missionCommanderNextActions[]` 旁同步输出 `missionCommanderActionQueue`，直接给出 reviewer dispatch / intake preview / intake apply 的 current/unblocked/blocked/reviewRequired/followUp buckets；reviewer-intake result 也输出同一 queue，覆盖 preview apply guidance、blocked fail-closed、partial retry、complete/already-complete postValidation handoff，让 Mission Commander / replacement executor 不再从 reviewer next-action list 手工计算当前可消费 action。coverage 锁定 top-level/nested packet queue、summary text、CLI reviewer orchestration E2E、reviewer intake preview/apply/duplicate/blocked/partial recovery、no auto-spawn/no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 425 完成 execution evidence review downstream artifact follow-through：`mission.ExecutionEvidenceReviewItem` 现在直接携带 `followThrough`，把已记录 authorized execution observation evidence 的 normal review、boundary/main escalation 与 duplicate replay review 显式结构化为 outcome-specific actions/verificationCommands/expected/evidence/boundary，并嵌入 evidence-review scoped `missionCommanderActionQueue`；overview、project/lane handoff JSON/Markdown、`/rekit continue` status/digest、lane `RESUME.md` 与 typed `checkpoints/latest.json` 同步投影 follow-through，CLI/package coverage 锁定 overview/project handoff JSON、Markdown text、continue artifacts、lane resume/checkpoint、normal/escalated/duplicate states、review-only queue、no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 424 完成 authorized execution adapter/report consumption follow-through closure：`gate -ExecutionReportContract`、`gate -ValidateExecutionReport` 与 execution evidence `gate -Apply -GateEventId ...` JSON 现在新增 `authorizedExecutionFollowThrough`，把 write-and-validate、valid record、invalid repair、recorded evidence review、boundary/main escalation review 与 duplicate replay review 显式结构化为 outcome-specific actions/repairActions/verificationCommands/expected/evidence/boundary，并嵌入同一 `missionCommanderActionQueue`；CLI text 同步输出 adapter report contract/validation/execution evidence follow-through outcomes，让 Mission Commander / replacement executor 不再从 contract、validation、record result、duplicate replay 与 evidence review queue 手工拼接 lifecycle。coverage 锁定 contract 三 outcome、valid validation record handoff、missing/invalid sidecar repair hints、normal/boundary/duplicate execution evidence follow-through、text/JSON parity、read-only validation、bounded observation evidence record、duplicate no-append、no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 423 完成 pack-memory accepted/rejected decision follow-through closure：`promote -CreateCandidates` 的 `reviewPlan` 现在新增 `decisionFollowThrough[]`，按每个 candidate / blocked / no-op item 将 `accept`、`reject`、`superseded`、`blocked` 与 `not-needed` outcome 映射到具体 actions、cleanupActions、verificationCommands、expected/evidence/boundary；同时新增 `missionCommanderActionQueue`，让 Mission Commander / replacement executor 不再从 promote candidate `missionCommanderNextActions[]` 手工计算 current action 与 review/cleanup/reconsume 分桶。CLI text 同步输出 per-candidate decision follow-through outcomes、cleanup/verification/boundary 与 action queue summary/current，再列完整 next-action lines；coverage 锁定 WhatIf/actual candidate JSON、managed-doc accept/reject/superseded、tooling accept fresh/attached reconsume、blocked non-promotable、text output、runtime guidance-only/no merge/no cleanup/no init/no doctor/no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 422 完成 workstream / replacement executor action queue closure：`start`、`continue`、`handoff` 与 `reconcile` JSON envelope 现在在既有 `missionCommanderNextActions[]` 旁新增 `missionCommanderActionQueue`，直接输出 summary/counts/current/unblocked/blocked/reviewRequired/followUp buckets；CLI text 同步打印 queue summary/counts/current，再列完整 next-action lines。lane `RESUME.md`、typed `checkpoints/latest.json`、continue `status.json` / `digest.md`、project/lane handoff Markdown 也投影同一 queue，让 Mission Commander / replacement executor 不再从 `missionCommanderNextActions[]` 手工计算 start/takeover、blocked continue、handoff 与 reconcile 的 current action。coverage 锁定 start preview/apply、blocked existing lane、project/lane handoff preview/apply、blocked continue/reconcile、lane checkpoint contract、no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 421 完成 authorized execution adapter/report action queue closure：`mission` package 新增共享 `MissionCommanderActionQueue` builder，`overview` 改为复用该实现；`gate -ExecutionReportContract`、`gate -ValidateExecutionReport` 与 execution evidence `gate -Apply -GateEventId ...` JSON 现在在既有 `missionCommanderNextActions[]` 旁新增 `missionCommanderActionQueue`，直接输出 summary/counts/current/unblocked/blocked/reviewRequired/followUp buckets。gate adapter/report/execution evidence text 同步打印 queue summary/counts/current，再列完整 next-action lines；coverage 锁定 contract blocked record handoff、valid report record handoff、missing/invalid sidecar repair guidance、normal evidence review、duplicate `evidence-already-recorded` replay 与 no-heavy/no-authority/confirmed/no PowerShell runtime logic 边界。
- Batch 420 完成 Mission Commander overview action queue closure：`overview -Format json` 现在在既有 `missionCommanderNextActions[]` 旁新增 `missionCommanderActionQueue`，把 next-action list 汇总为 `summary`、`counts`、`currentAction`、`unblockedActions[]`、`blockedActions[]`、`reviewRequiredActions[]` 与 `followUpActions[]`，让主 Agent / replacement executor 不再从完整 next-action 列表手工计算当前可执行 action、阻塞 action、review-required action 和 follow-up 数量。overview text 同步新增 `Mission Commander action queue` section，直接打印 counts/current action；coverage 锁定 execution evidence review 优先于 blocked lane reconcile、current action 选择、blocked/review/follow-up counts、JSON/text queue 输出与 overview read-only/no authority/confirmed/no-heavy-tool/no PowerShell runtime logic 边界。
- Batch 419 完成 gate request Mission Commander top-level next-action projection closure：normal `gate -WhatIf/-Apply -Format json` request path 现在输出 top-level `missionCommanderAction` 与 `missionCommanderNextActions[]`，让 pending/authorized gate request 不再要求主 Agent 从 nested `executorAction.missionCommanderAction`、`wouldExecutorAction`、`nextSteps[]`、request eventId 和 adapter report commands 手工拼接。`gate -WhatIf` 会把 pending-gate preview 投影为 `needs-gate-apply` bounded request-ledger write，把 preauthorized/authorized preview 投影为 `needs-authorized-gate-apply` durable authorization decision write；preview primary apply action 保持 review-owned，follow-up handoff/continue/report-contract 在 apply 成功并刷新 request/executor state 前保持 blocked/requiresReview。pending-gate apply 后直接投影 refreshed `needs-gate-decision` handoff/continue-WhatIf；authorized-gate apply 后投影 `ready-for-execution-report-contract`，primary command 使用实际 `GateEventId` 读取 `-ExecutionReportContract -Format json`，再由 lane executor/tool adapter 在授权边界外部执行并记录 bounded observation evidence。CLI text 同步打印 normal gate request `mission commander next action` lines；coverage 锁定 pending/authorized preview、pending apply、authorized apply report-contract handoff、request-ledger-only/no authority/confirmed/no-heavy-tool/no PowerShell runtime logic 边界。
- Batch 418 完成 reconcile Mission Commander top-level next-action projection closure：`reconcile -WhatIf/-Apply -Format json` 现在输出 top-level `missionCommanderAction` 与 `missionCommanderNextActions[]`，让 Human-in-the-Lane intervention resolution 不再要求主 Agent 从 nested `executorAction.missionCommanderAction` 与 `nextSteps[]` 手工拼接。preview 会把 selected open intervention 投影为 concrete reconcile apply command（包含实际 `-InterventionId`，以及适用的 `-Executor` / `-Actor` / `-Reason`），primary action 保持 review-owned 且可作为 bounded apply action 消费；continue/handoff follow-up 在 apply 成功并刷新 executor action 前保持 blocked/requiresReview。apply 后直接投影 refreshed ready lane 的 continue/handoff，或继续投影 blocked lane 的 reconcile/gate/decision next actions。CLI text 同步打印 `mission commander next action` lines；coverage 锁定 JSON/text preview、apply 后 ready projection、case-local ledger/lane/resume/checkpoint/board write boundary、no authority/confirmed/no-heavy-tool/no PowerShell runtime logic 边界。
- Batch 417 完成 start / replaceable executor takeover Mission Commander next-action projection closure：`start -WhatIf/-Apply -Format json` 现在输出 top-level `missionCommanderAction` 与 `missionCommanderNextActions[]`，复用 lane Mission Commander builder，把创建 lane、进入 existing lane、executor claim/takeover preview、apply 后 ready/blocked lane handoff 收口为主 Agent 可直接消费的 source/blocked/requiresReview/reasons/boundary/command 列表。preview 的 start apply primary action 保持 review-owned，continue/handoff follow-up 在 apply 成功并刷新 executor action 前保持 blocked/requiresReview；apply 后直接投影 ready continue/handoff 或 blocked reconcile/gate/decision next actions。CLI text 同步打印 `mission commander next action` lines，替换 executor 不再需要从 nested `executorAction.missionCommanderAction` 与 `nextSteps[]` 手工拼接 start/takeover 顺序；package/CLI coverage 锁定 new lane preview、existing main takeover preview、apply continue projection、blocked lane text projection、no authority/confirmed/no-heavy-tool/no PowerShell runtime logic 边界。
- Batch 416 完成 plan-subagents planning Mission Commander dispatch guidance closure：`plan-subagents` planning result 现在新增 top-level `missionCommanderAction` 与 `missionCommanderNextActions[]`，并在 `packet.json` / `reviewerOrchestration` 与 `summary.md` 同步投影 reviewer dispatch、result collection、attached-case intake preview/apply、dispatch-only unattached target 与 empty-plan replan guidance。attached-case next actions 把 reviewer dispatch 标记为主 Agent review-owned，把 `reviewerOrchestration.intake.preview` / `reviewerOrchestration.intake.apply` 标记为 blocked/requiresReview，要求 reviewer JSON 已放入 result path、preview valid 且 evidenceRefs 经主 Agent复核后才能 apply；out-of-case review artifacts 只输出 dispatch-only 与 init WhatIf handoff，不把 reviewer intake/writeback 当作可立即执行。package/CLI coverage 锁定 top-level 与 nested packet projection、summary text、empty plan、out-of-case、attached-case product path、no auto-spawn/no-heavy-tool/no authority/confirmed 边界；该批不新增 PowerShell runtime logic、不执行 reviewer、heavy-tool、init/apply 或 reviewer-intake writeback，不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 415 完成 reviewer-intake Mission Commander writeback guidance closure：`plan-subagents -ReviewerResultPath ... -WhatIf/-Apply -Format json` 的 reviewer-intake envelope 现在新增 top-level `missionCommanderAction` 与 `missionCommanderNextActions[]`，把 previewed、blocked、verification-recorded partial recovery、complete、already-complete 与 post-validation-failed 写回状态收口为主 Agent 可直接消费的 apply/retry/handoff/overview/postValidation next-action guidance。complete/already-complete 会提升 returned lane handoff `postValidation.missionCommanderNextActions[]` 为 `reviewerIntake.postValidation.*` source，partial recovery 明确重跑相同 apply command，不要求主 Agent 从 nested verification/decision `note.AppendResult`、postValidation handoff 与 nextSteps 手工拼接；package/CLI coverage 锁定 preview apply guidance、post-validation next actions、duplicate already-complete、blocked fail-closed 与 partial recovery JSON。该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 reviewer result strict contract、verification-before-decision append 顺序、sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 414 完成 note append Mission Commander action-delta closure：`note` append/what-if/duplicate JSON 现在除 `executorAction` / `wouldExecutorAction` 外同步输出当前或 post `missionCommanderAction`、`missionCommanderNextActions[]`，并在 `-WhatIf` 场景输出 would `missionCommanderAction` / `wouldMissionCommanderNextActions[]`，让主 Agent 可直接看到 note 写入前后对 lane commander state、primary/follow-up handoff 与 blocked/requiresReview next-action 的影响。blocker-kind what-if 会同时保留 current ready commander projection 与 would blocked projection；actual blocker append 返回 post blocked commander projection；duplicate eventId 仍是 no-op，只保留 unchanged current projection，不生成 misleading would guidance。package/CLI coverage 锁定 verification no-op readiness、candidate/decision/intervention/request blocker delta、applied blocker post action、duplicate no-op、ready/blocked next-action source 与 no authority/confirmed/no-heavy-tool/no-write-when-what-if 边界；该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 note 既有 append 模型、sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 413 完成 Mission Commander lane follow-up next-action closure：共享 `mission.MissionCommanderNextActions(...)` 现在把 lane-level `missionCommanderAction.followUpCommands` 同步投影为 `missionCommanderActions.followUp` next-action items，让 overview、project/lane handoff、continue artifacts、lane resume/checkpoint 与 gate evidence record 的 ordered handoff 不再只暴露 lane primary command。ready lane follow-up 保留 `/rekit handoff <lane>` 作为 primary action 后的可消费 handoff；blocked lane follow-up 保持 `blocked=true`/`requiresReview=true`，携带 `follow-up is available only after resolving current lane blockers`、`run as -WhatIf first` reason 与 `do not run continue for blocked lanes` boundary。project/lane handoff Markdown 的 `Mission Commander next actions` 同步打印 reason/boundary lines，避免替换 executor 回查 JSON 才能理解 follow-up 条件；CLI/package coverage 锁定 overview text/JSON、project/lane handoff preview/apply、execution evidence record、shared mission builder 与 text handoff no-continue boundary；该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 412 完成 adapter report contract/validation text next-action handoff closure：`gate -ExecutionReportContract` 与 `gate -ValidateExecutionReport` 现在除 JSON 外也支持 text/table/tsv 兼容文本输出，直接打印 adapter report contract/validation identity、case-relative validate/record command、Mission Commander action 与 `mission commander next action` lines。contract 文本把 record handoff 标记为 blocked，并输出 `do not record evidence until validation returns valid=true` 与 `replace <executor-id>` boundary；valid=true validation 文本只给 record + handoff；invalid sidecar validation 文本只给 repair hint + rerun read-only validation，不推荐 `-Apply` record。CLI coverage 锁定 text output、nested authorized output workspace handoff、invalid boundary repair、read-only no observations/no authority/confirmed、no-heavy-tool 与 no premature record；该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 411 完成 adapter report contract/validation Mission Commander next-action closure：`gate -ExecutionReportContract` 与 `gate -ValidateExecutionReport` JSON 现在输出 `missionCommanderNextActions[]`，把 read-only validation、valid=true 后 record evidence、handoff、invalid/missing sidecar repair hints 与 rerun validation 收口为 source/reasons/boundary/command 的可消费 next-action list。contract 阶段的 record command 会标记 blocked，明确必须等 validation returns valid=true 且替换 `<executor-id>` 后才能运行；validation 阶段 valid=true 只给 record + handoff，invalid/missing path 只给 repair action + rerun read-only validation，不给 `-Apply` record。package 与 CLI coverage 锁定 contract/validation JSON projection、nested workspace case-relative product path、no observations on validation, no authority/confirmed、no heavy-tool 与 no premature record boundaries；该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 410 完成 authorized execution duplicate/idempotent review projection closure：`gate -Apply -GateEventId ...` 重复记录同一 execution evidence eventId 时，JSON 现在把 duplicate-specific `evidence-already-recorded` commander action 同步投影到即时 `executionEvidenceReview[]` 与 `missionCommanderNextActions[]`，并只保留 review handoff 与 overview follow-up，不再混入 normal evidence review 的 `/rekit continue ... -WhatIf` 或 lane-level `missionCommanderActions` continue 建议。CLI text 同步输出 `applied=false`、`evidence-already-recorded` 与 `duplicate record did not append observation evidence` boundary。package 与 CLI coverage 锁定 duplicate no-append/no-replay、review-only next actions、text/JSON projection、no authority/confirmed、no heavy-tool；该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 409 完成 pack-memory cleanup / reconsume Mission Commander next-action command UX：`promote -CreateCandidates` 的 `reviewPlan` 现在新增 `missionCommanderNextActions[]`，把 decision checklist review、per-candidate cleanup、pack doctor、fresh-case init/doctor 与 attached-case doctor 收口为主 Agent 可按序消费的 next-action list（source/reasons/boundary/command），并保持 cleanup/reconsume 只作为 guidance。Go CLI `-Format text` 现在输出 promote candidates summary、cleanup targets/actions、reconsume checks/commands/boundaries、pack-memory commander action 与 `mission commander next action` lines，让主 Agent 不必解析完整 JSON 即可执行 bounded checklist。package 与 CLI coverage 锁定 WhatIf/actual JSON projection、text output、indexPath cleanup、fresh/attached reconsume commands、no authority/confirmed、no heavy-tool 与 runtime-does-not-execute boundaries；该批不新增 PowerShell runtime logic、不执行 candidate merge/cleanup/init/doctor、不执行 `promote -Apply`、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 408 完成 gate execution evidence Mission Commander next-action projection closure：`mission` package 现在拥有共享 `ExecutionEvidenceReviewItems(...)` / `ExecutionEvidenceReviewItemFromObservation(...)` builder，workstream 复用同一 parser；`gate -Apply -GateEventId ...` 的 execution evidence record JSON 现在直接输出 `executionEvidenceReview[]` 与 `missionCommanderNextActions[]`，把刚记录的 bounded observation evidence 立即投影为 evidence-first handoff/overview/continue ordering，boundary-hit/escalated evidence 继续抑制 autonomous continue；CLI text 也输出 `mission commander next action` lines。CLI coverage 锁定 normal execution evidence record、adapter escalation suppression 与 text output；该批不 replay heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 407 完成 continue Mission Commander next-action projection follow-through：`/rekit continue` JSON envelope 现在新增 `executionEvidenceReview[]` 与 `missionCommanderNextActions[]`，run `status.json` 同步写出两者，run `digest.md` 新增 `Mission Commander next actions` section，并复用 `mission.MissionCommanderNextActions(...)` 的 evidence-first ordering、blocked reason、continue suppression 与 no-replay boundary 语义，让主 Agent 在执行或预览 continue 后仍可直接消费 evidence review/handoff/overview 顺序，不必回查 overview/handoff/resume/checkpoint 或手工拼接 `executionEvidenceReview[]` 与 `executorAction`。CLI coverage 锁定 continue JSON、run status、run digest、boundary-hit/escalated evidence 优先级与 autonomous `continue` suppression；该批只增强 continue result/run artifact 投影，除既有 continue case-local fact/run/resume/checkpoint writes 外不新增写入路径，不 replay heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 406 完成 lane-local Mission Commander next-action projection follow-through：lane `RESUME.md` 现在新增 `Mission Commander next actions` section，typed `checkpoints/latest.json` 新增 `missionCommanderNextActions[]`，并复用 `mission.MissionCommanderNextActions(...)` 的 evidence-first ordering、blocked reason、continue suppression 与 no-replay boundary 语义，让 replacement executor 从 lane-local resume/checkpoint 即可直接读取 evidence review/handoff/overview 顺序，不必回查 project/lane handoff 或手工拼接 `executionEvidenceReview[]` 与 `executorAction`。CLI/workstream coverage 锁定 resume Markdown、checkpoint JSON contract、boundary-hit/escalated evidence 优先级与 autonomous `continue` suppression；该批不 replay heavy-tool、不执行 continue、不写 observation/request/authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 405 完成 handoff Mission Commander next-action projection follow-through：`mission` package 现在提供共享 `MissionCommanderNextActionItem`、typed `ExecutionEvidenceReviewItem` 与 `MissionCommanderNextActions(...)` builder，`overview` 与 `project/lane handoff` 复用同一 evidence-first ordering、blocked reason、continue suppression 与 no-replay boundary 语义；project/lane handoff JSON 新增 `missionCommanderNextActions[]`，project handoff Markdown 逐 lane 输出 `commander next action`，lane handoff Markdown 新增 `Mission Commander next actions` section，让主 Agent 或 replacement executor 不必从 `executionEvidenceReview[]`、`laneExecutorActions[]`、`executorAction` 与 `nextSteps[]` 手工拼接执行顺序。CLI coverage 锁定 preview/apply handoff JSON/Markdown、blocked reconcile action、evidence review priority 与 autonomous `continue` suppression；该批不 replay heavy-tool、不执行 continue、不写 observation/request/authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 404 完成 Mission Commander overview next-action consumption closure：`overview` JSON 现在新增顶层 `missionCommanderNextActions[]`，把 `executionEvidenceReview[]` 的 commander primary/follow-up 与 lane-level `missionCommanderActions[]` primary commands 汇总为主 Agent 可直接按序消费的 next-action list（lane/label/state/source/blocked/requiresReview/reasons/boundary/command）；text output 新增 `Mission Commander next actions` section，并在 mission blocked 或 evidence escalation 场景抑制 autonomous `continue` follow-up，让已记录 observation evidence 的 review/handoff 先于 lane continue。CLI coverage 锁定 overview read-only text/JSON 投影、evidence review priority、blocked reconcile action 与 no-replay boundary；该批不 replay heavy-tool、不执行 continue、不写 observation/request/authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 403 完成 pack-memory main Agent execution plan closure：`promote -CreateCandidates` 的 `reviewPlan` 现在新增 `mainAgentExecutionPlan[]`，把 WhatIf materialize、review decisions、candidate cleanup、pack doctor、fresh-case reconsume 与 attached-case reconsume 收口为主 Agent 可直接执行的有序 handoff steps（commands/expected/evidence/boundary），并明确 runtime 只输出 guidance、不执行 merge/cleanup/init/doctor。package 与 CLI coverage 锁定 WhatIf/actual candidate JSON 的 execution plan、indexPath cleanup、fresh/attached reconsume command handoff、no authority/confirmed、no heavy-tool 与 no case artifact promotion boundaries；该批不新增 PowerShell runtime logic、不执行 candidate merge、不执行 `promote -Apply`、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 402 完成 execution evidence review handoff text suppression closure：project handoff Markdown 的逐 lane 行现在会在 execution evidence review queue 存在 boundary-hit/escalated/escalation evidence 时先显示 `evidence next action`，并把 ready lane 的 `continue command` 降级为“evidence review 后继续候选”，避免文本层仍把 autonomous continue 当作当前动作；lane handoff 的新会话开场同样改为先 review execution evidence、通知 main Agent，并明确当前不要执行 `/rekit continue <lane>`。该批只增强已记录 evidence 的只读 handoff 文本消费顺序；不 replay heavy-tool、不运行 adapter、不执行 continue、不新增 observation/request、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 401 完成 execution evidence review next steps consumption follow-through：overview 与 project/lane handoff 的 top-level `nextSteps[]` 现在会直接提升已记录 authorized execution observation evidence 的 review guidance，包含 `review execution evidence for gateEventId ...`、evidence commander primary `/rekit handoff <lane>`、`/rekit overview` follow-up 与 boundary/escalation main-review stop guidance；当 evidence review queue 中存在 boundary-hit/escalated/escalation 时，top-level next steps 不再推荐 autonomous `continue`，即使 lane executor action 仍因 authorized-gate 非阻塞而 ready。该批只增强已记录 evidence 的只读消费顺序；不 replay heavy-tool、不运行 adapter、不执行 continue、不新增 observation/request、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 400 完成 execution evidence review Mission Commander action closure：`executionEvidenceReview[]` items 现在直接携带 `missionCommanderAction`，把已记录 authorized execution observation evidence 的 review queue 从“refs + handoff command”升级为可直接消费的 commander action（`ready-for-evidence-review` 或 `needs-main-escalation`，primary `/rekit handoff <lane>`，follow-up `/rekit overview` 与仅非 escalation 的 `continue <lane> -WhatIf`，边界复用 no-replay / review refs / no authority-confirmed）。overview text、project/lane handoff text、lane `RESUME.md` 与 typed checkpoint 也展示 commander state/primary/follow-up；CLI/workstream coverage 锁定 normal 与 escalated evidence 的 JSON/Markdown/RESUME/checkpoint projection，并继续保持只投影既有 observation evidence、不 replay heavy-tool、不写 observation/request/authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 399 完成 Mission Commander overview execution evidence review consumption closure：`overview` JSON 现在新增顶层 `executionEvidenceReview[]`，text output 新增 `Execution evidence review` section，复用 workstream 的 authorized execution observation evidence review queue 投影，把已记录的 preauthorized `authorized-gate` observation evidence 直接展示为 gate eventId、status、action、outputRefs/evidenceRefs、review command、handoff command 与 no-replay/no-authority boundary；主 Agent 或替换 executor 不必先运行 project/lane handoff 或回扫 observations ledger 才能发现待审 evidence。CLI coverage 锁定 overview text/JSON 的 evidence review queue，并继续保持 overview read-only、不 replay heavy-tool、不写 observation/request/authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 398 完成 pack-memory cleanup / reconsume command execution UX follow-through：`promote -CreateCandidates` 的 `reviewPlan` 现在新增 `decisionChecklist[]`，把每个 candidate / blocked / no-op item 的 review action、accept/reject、cleanup、verification commands 与 boundary 投影为主 Agent 可直接执行的 bounded checklist；`cleanupTargets[]` 现在携带 `indexPath` 与 `cleanupActions[]`，`reconsume` 新增 `verificationChecklist[]`，分别列出 pack doctor、fresh case reconsume 与 attached case reconsume 的 commands/expected/evidence/boundary。package 与 CLI coverage 锁定 WhatIf/actual candidate review checklist、tooling candidate fresh-case reconsume、candidate cleanup action 与 no authority/confirmed / no heavy-tool / no case artifact promotion boundaries；该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 397 完成 Mission Commander overview action consumption closure：`overview` JSON 现在新增 `missionCommanderActions[]` action index，把每条 lane 的 commander state、primary command、follow-up、boundary、blocked/ready 与 blocker reasons 从 nested `laneExecutorActions[].executorAction.missionCommanderAction` 提升为主 Agent 可直接枚举的顶层索引；text output 新增 `Mission Commander action index` section，直接展示每条 lane 的 primary/follow-up/boundary。CLI coverage 锁定 overview text/JSON 的 action index，并继续保持 overview read-only、不写 case、不执行 continue/heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 396 完成 authorized execution evidence handoff consumption follow-through：project/lane handoff JSON 现在暴露 `executionEvidenceReview[]`，project handoff 文本、lane handoff 文本、lane `RESUME.md` 与 typed `checkpoints/latest.json` 也会投影已记录的 authorized execution observation evidence review queue，包含 gate eventId、status、action、outputRefs/evidenceRefs、review command、handoff command、no-replay/no-authority boundary，并在 boundary-hit/escalated/escalation evidence 上标记 main review boundary。CLI 与 workstream coverage 锁定普通 succeeded evidence 与 adapter escalated evidence 的 JSON/Markdown/RESUME/checkpoint consumption；该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 observation/authority/confirmed、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 395 完成 Mission Commander lane handoff consumption closure：project handoff 的逐 lane 行现在不只展示 commander state/prompt，还直接输出 `commander primary`、`commander follow-up` 与 `commander boundary`；lane handoff 与 lane `RESUME.md` 的 executor action snapshot 现在也包含 `commander primary command`，让替换 executor 或新会话不必回到 JSON 解析 `missionCommanderAction.primaryCommand` 才能知道首选命令。CLI coverage 锁定 project handoff ready/blocked lane 的 primary/follow-up/boundary、lane handoff JSON/text 与 RESUME 中 blocked lane reconcile guidance，并继续保持 blocked lane 不推荐 continue、no heavy-tool、no authority/confirmed、no board/facts/lane creation boundary；该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 394 完成 pack-memory reconsume Mission Commander follow-through：`promote -CreateCandidates` 的 `reviewPlan` 现在暴露 `missionCommanderAction`，把 WhatIf preview、实际 candidate review、blocked/no-op candidate plan 分别收口为主 Agent 可直接消费的 state/prompt/primary/follow-up/boundary；候选生成后直接提示逐项 review `reviewPlan.reviewItems`、手动 merge/reject、清理 rejected/superseded candidate 与 `indexPath`，并在 tooling candidate 被接受合入 `tooling/catalog.yml` / `tooling/recipes/*` 后运行 doctor 与 fresh/attached case reconsume 验证。package 与 CLI coverage 锁定 WhatIf no-write preview handoff、actual candidate review handoff、tooling reconsume follow-through、manual tooling merge、candidate cleanup、no authority/confirmed 与 no heavy-tool boundaries；该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 393 完成 authorized execution evidence Mission Commander closure：`gate -Apply -GateEventId ...` 记录 bounded observation evidence 后，JSON 与 text output 现在直接暴露 evidence-specific `missionCommanderAction`，把普通成功记录收口为 `ready-for-evidence-review`、duplicate eventId 收口为 `evidence-already-recorded`、boundary/escalation 结果收口为 `needs-main-escalation`；主 Agent 可直接执行 `/rekit handoff <lane>`、review output/evidence refs，并只在非 escalation 新记录后看到 `continue -WhatIf` follow-up。package 与 CLI coverage 锁定 normal/duplicate/adapter escalation evidence record、handoff/next steps、no-heavy-tool、bounded observation evidence、no authority/confirmed 与 idempotent no-append invariants；该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 392 完成 lane/tool-adapter live validation UX closure：`gate -ExecutionReportContract` 与 `gate -ValidateExecutionReport` 的 JSON envelope 现在输出 `missionCommanderAction`，把 authorized-gate sidecar handoff 收口为主 Agent 可直接消费的 state/prompt/primary/follow-up/boundary；contract 阶段给出 `needs-adapter-report-validation` 与 concrete read-only validate / valid=true 后 record observation evidence command，validation 阶段按结果给出 `ready-to-record-evidence`、`needs-execution-report-path`、`repair-adapter-report` 或 `needs-main-escalation`，并明确 validation read-only no-write、record 只写 bounded observation evidence、`/rekit` never executes heavy tool、no authority/confirmed。package 与 CLI coverage 锁定 contract、valid preflight、missing path、repair/escalation states、nested workspace product path 与 no-observations/no-authority/no-confirmed invariants；该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 391 完成 Mission Commander Windows product UX closure：`mission.ExecutorAction` 现在投影结构化 `missionCommanderAction`，用 `state`、自然语言 `prompt`、`primaryCommand`、`followUpCommands[]` 与 `boundary[]` 把 overview/start/continue/handoff/gate/reconcile 的下一步收口为主 Agent 可直接消费的接手 guidance；ready lane 才给 `ready-to-continue`，open intervention / pending gate / open decision 分别给 `needs-reconcile`、`needs-gate-decision`、`needs-open-decision-review`，paused/closed/archived lane 保持 `lane-not-open`，start preview 新建 lane 或 executor claim/takeover 场景给 `needs-start-apply` 并保留完整 apply command，feature lane pending-gate guidance 的 `gate -Lane` 使用 canonical lane ID。JSON/text、project/lane handoff、continue digest/status、lane `RESUME.md` 与 typed checkpoint 均复用同一 mission snapshot，避免替换 executor 或新会话从 blocker counts 手工反推命令。该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 390 强化 pack-memory promote candidate product UX：`promote -CreateCandidates` JSON 现在返回 `reviewPlan` envelope，按 candidate/blocked/skip item 投影 `reviewItems[]`、per-candidate merge/reject/cleanup guidance、`cleanupTargets[]`、runtime boundary、completion criteria 与 `reconsume` 指南；Mission Commander 可区分 managed-doc candidate、tooling candidate、blocked deny item 和 WhatIf preview，按人工 accept/reject/superseded 路径合并或删除候选，并在 tooling 合入 `tooling/catalog.yml` / `tooling/recipes/*` 后用 fresh/attached case 验证 pack-memory reconsume。package 与 CLI coverage 锁定 WhatIf no-write、candidate cleanup hints、managed-doc/tooling merge hints、blocked item handling 与 fresh-case reconsume guidance。该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 389 完成 reviewer orchestration E2E：`plan-subagents` planning result、`packet.json` 与 `summary.md` 现在输出 `reviewerOrchestration`，将 Mission Commander 侧多 reviewer dispatch lifecycle、result root、per-shard `reviewerResultPath`、preview/apply intake command、owner binding 与 runtime boundary 汇总成可连续消费的 handoff；reviewer-intake JSON 增加 `orchestrationSnapshot`，返回 dispatch index/total、before/after shard status 与 remaining dispatches；packet identity 继续覆盖新增 orchestration 字段，同时兼容 Batch 389 前缺少该字段的 legacy packet intake 且不允许非空 orchestration tamper 借 legacy hash 绕过；verification/decision ledger 和 lane handoff 现在投影 `reviewerSession` provenance，让替换 executor 或新会话能看到 reviewer verdict 与 main merge decision 来源。CLI E2E 覆盖 attached case 中两 shard / 两 reviewer、一 accept 一 reject、WhatIf→Apply 顺序写回、post-validation 与 handoff reviewer session projection。该批同时补充 durable 文档维护规则：后续修改/创建文档也保持按需路由与渐进披露，避免 active docs 重新臃肿。本批不自动 spawn reviewer、不写 authority/confirmed、不执行 heavy-tool、不新增 PowerShell runtime logic、不改变 sync/promote review-first、case durable schema、公共 façade 删除门禁或远程 CI blocker 状态。
- Batch 388 完成 context routing / progressive disclosure 文档上下文压力优化：新增 `docs/context-routing.md` 作为新会话、上下文压缩后接手和每轮自主推进的第一路由入口；`docs/batch-plan.md` 只保留 current milestone/current batch/next candidates/validation/risk 短状态，完整旧批次归档到 `docs/batch-history.md` 按需搜索；根 `CLAUDE.md` 压缩为边界 + 路由 + 维护入口 + 验证命令。`release-check -Format json` 的 `releaseHandoff.readFirst[]` 从多个长文档收缩为 router/current/release/changelog 四项，public default docs 与 release-check required docs inventory 同步纳入 `docs/context-routing.md` / `docs/batch-plan.md` 并由测试锁定。该批只改维护文档与 inventory/tests，不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态；远程 Linux/macOS/Windows CI 仍按 GitHub runner/billing blocker 记录，不阻塞当前 Windows 本机 product iteration。
- Batch 387 完成 replaceable session executor takeover E2E：`start main -Apply -Executor <replacement> -Actor <actor> -Reason <reason>` 现在可把已有 durable authority lane 当作 lane selector 接手，而不是误建 `feature-main`；`-Name main` preview 同样解析到 existing lane，replacement session 可从 project handoff → executor takeover → lane `RESUME.md` / typed checkpoint / events → lane handoff → `continue main -WhatIf` 的产品路径恢复 ready executor action、handoff/continue command 与 takeover provenance。CLI product-path coverage 锁定 no-heavy-tool、no-authority/no-confirmed invariant；该批不新增 PowerShell runtime logic、不修改 sync/promote review-first、不执行 heavy-tool、不改变 case durable schema 或远程 CI blocker 状态。
- Batch 386 完成 adapter-specific live validation operational slice：`gate -ExecutionReportContract -GateEventId ... -Format json` 现在从 pack `tooling/catalog.yml` 与本次 `heavyToolGates` action 投影 `liveValidation.adapterCandidates[]`、默认 `selectedAdapter` 与 concrete sidecar `adapterId` guidance；`gate -ValidateExecutionReport` 的 read-only envelope 与 `gate -Apply -GateEventId ... -ExecutionReportPath ...` 写入的 observation evidence 现在保留 `adapterContext`，让 Mission Commander / replacement lane executor 能从 pack tooling catalog → authorized-gate contract → sidecar template → validate preflight → evidence record 追踪具体 adapter provenance。package 与 CLI product-path coverage 使用真实 `generic-binary-re` candidate 跑通 authorized-gate contract、case-local `CaseRelativeValidateArgs`、`CaseRelativeRecordArgs`、bounded sidecar intake、observation evidence 记录与 no-authority/no-confirmed invariant。该批不新增 PowerShell runtime logic、不新增 public 参数面、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 385 固化 Mission Control operational-slice cadence：`docs/batch-plan.md`、`docs/autonomous-goal.md`、`docs/mission-control-product-direction.md`、`docs/release-readiness.md` 与根 `CLAUDE.md` 现在明确后续不要连续推进单字段 contract / inventory / metadata 微批次；新增 contract 字段只能作为 Mission Commander orchestration、replaceable executor、reviewer writeback、authorized execution evidence、adapter-specific live validation、pack-memory UX 或 cross-platform product path 的必要支撑，并必须由 package / CLI / 临时 case / product-path 验证证明其解决真实产品断点。该批只更新 durable guidance，不新增 PowerShell runtime logic、不改 runtime schema、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、公共入口删除门禁或远程 CI blocker 状态。
- Batch 384 将 adapter validation repair guidance 前移到只读 execution report contract：`gate -ExecutionReportContract -GateEventId ... -Format json` 现在除 validation failure stages/codes 外，也输出 `validationRepairHints[]`，让 replacement lane executor / tool adapter 在执行前即可消费缺 report path、path/read/decode/schema/identity/refs/budget/boundary/summary 等失败对应的 repair action、相关字段、允许 output paths / stop conditions、size limit、record blocked 与 rerun validation 标记；package 与 CLI coverage 锁定 contract JSON projection，并保持 no-observations、no-heavy-tool、no-authority/no-confirmed 语义。该批不新增 PowerShell runtime logic、不修改 public 参数面、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 383 为 adapter execution report invalid validation envelope 增加 `repairHints[]`：`gate -ValidateExecutionReport -GateEventId ... -ExecutionReportPath ... -Format json` 在 `valid=false` 时除 `error` / `errors[]` / `failureCode` / `failureStage` 外，还输出机器可读 repair action、相关字段、允许的 output paths / stop conditions、size limit、record blocked 与 rerun validation 标记；package 与 CLI coverage 覆盖 path、refs、boundary、summary 等修复提示，并继续锁定 read-only no-observations、no-heavy-tool、no-authority/no-confirmed 语义。该批不新增 PowerShell runtime logic、不修改 public 参数面、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 382 强化 adapter `liveValidation` replay / actor handoff guidance：`gate -ExecutionReportContract -GateEventId ... -Format json` 的 `liveValidation.replayBehavior` 现在明确重复运行 `RecordArgs` 或 `CaseRelativeRecordArgs` 都会返回 `applied=false` / `duplicate eventId` 且不追加 observations，notes 同时明确运行 record handoff 前必须替换 `<executor-id>`，而 read-only validation guidance 同步覆盖 `ValidateArgs` 与 `CaseRelativeValidateArgs`；package 与 CLI focused coverage 锁定 replay guidance、case-relative record command handoff 和 no-heavy-tool/no-authority/no-confirmed 语义。该批不新增 PowerShell runtime logic、不修改 public 参数面、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 381 锁定 adapter `liveValidation` case-relative record handoff 的 actual product path：CLI nested workspace E2E 现在从 case-local cwd 直接消费 contract 输出的 `caseRelativeRecordArgs`，替换 `<executor-id>` 后写入 bounded observation evidence，验证 `executionReportPath` 规范化为 case-relative path、adapter provenance / actual budget / output refs 保留、duplicate replay 返回 `duplicate eventId` 且不重复 append，并继续断言不写 authority/confirmed；这补齐 Batch 380 已投影 case-relative record handoff 但只实际执行 read-only validation handoff 的覆盖缺口。该批不新增 PowerShell runtime logic、不修改 public 参数面、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 380 补齐 adapter `liveValidation` 的 case-relative handoff：`gate -ExecutionReportContract -GateEventId ... -Format json` 现在除 workspace-relative `reportFileName` / `validateArgs` / `recordArgs` 外，还输出 `caseRelativeReportPath`、`caseRelativeValidateCommand` / `caseRelativeRecordCommand` 与 `caseRelativeValidateArgs` / `caseRelativeRecordArgs`，让 replacement lane executor / tool adapter 可从任意 case-local cwd 直接使用默认 case-relative sidecar path 运行 read-only validation 或 record handoff，不必切换到 authorized output workspace 或手工拼接命令；package 与 CLI product-path coverage 锁定 command/args 同源、nested workspace contract projection、case-relative validation no-write invariant 与 reportPath 规范化。该批不新增 PowerShell runtime logic、不修改 public 参数面、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 379 补齐 Mission Commander authorized-gate report contract handoff：Mission brief 的 project/lane gate line 现在在 `status=authorized-gate` 且存在 gate event id 时直接展示 `eventId=<id>` 与 `reportContract=/rekit gate -ExecutionReportContract -GateEventId <id> -Format json`，让 replacement lane executor / tool adapter 在 overview、project/lane handoff、continue digest/status、lane `RESUME.md` 与 `checkpoints/latest.json` 中看到 budget/output/stop boundaries 后，不必重扫 request ledger 即可读取 adapter execution report contract 并进入 validate→record 闭环；package 与 CLI product-path coverage 锁定 Mission brief、overview JSON/text、project/lane handoff、continue artifacts 与 checkpoint 投影。该批不新增 PowerShell runtime logic、不修改 public 参数面、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 378 补齐 adapter `liveValidation` workspace handoff 的机器可读字段：`gate -ExecutionReportContract -GateEventId ... -Format json` 现在直接输出 `defaultReportPath`、`liveValidation.authorizedWorkspaces[]` 与 `liveValidation.reportFileName`，让可替换 lane executor / tool adapter 不必解析 `invocationCwd` 文本或自行拼接 `allowedOutputPaths`，即可选择授权输出 workspace、写入默认 `adapter-report.json` sidecar，并使用既有 workspace-relative validate/record commands + args 完成 preflight→record 闭环；package 与 CLI nested workspace coverage 锁定 JSON projection、command/args 同源和 no-`Target` product path。该批不新增 PowerShell runtime logic、不修改 public 参数面、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 377 强化 Mission Commander authorized-gate handoff boundary visibility：Mission brief 的 project/lane gate line 现在直接展示 `requestedBudget`、`outputPaths` 与 `stopConditions`，让 overview、project/lane handoff、continue digest/status、lane `RESUME.md` 与 `checkpoints/latest.json` 中的非阻塞 `authorized-gate` 不只显示授权 profile/decision，也显示可替换 lane executor 接手 actual heavy action 前必须遵守的预算、输出路径和停止条件边界；package 与 CLI product-path coverage 覆盖 Mission brief、lane-local gate line、overview JSON/text、project/lane handoff、continue artifacts 与 checkpoint 投影。该批不新增 PowerShell runtime logic、不修改 public 参数面、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 376 强化 adapter execution report evidence ref boundary：`gate -ValidateExecutionReport` 与 `gate -Apply -GateEventId ... -ExecutionReportPath ...` 现在拒绝越出本次 `authorized-gate` `outputPaths` 的 sidecar `evidenceRefs`，explicit `-ExecutionEvidenceRefs` 也必须保持 case-relative 且落在 authorized output paths 内；contract 同步输出 `refPathRequires`，failure taxonomy 新增 `evidence-refs-out-of-scope`，`liveValidation.sidecarTemplate` 与 notes 明确 evidence refs 必须位于授权输出边界内。package/CLI/façade coverage 覆盖 contract projection、invalid envelope、nested workspace no-write validation 与 evidence apply fail-closed。该批不新增 PowerShell runtime logic、不执行 heavy-tool、不修改 public 参数面、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 375 强化 adapter execution report live-validation enforcement：`gate -ValidateExecutionReport` 与 `gate -Apply -GateEventId ... -ExecutionReportPath ...` 现在拒绝未被本次 `authorized-gate` `stopConditions` 覆盖的 `boundaryHits`，并要求 `failed` / `boundary-hit` / `escalated` / `aborted` sidecar 带 bounded `summary`；contract 同步输出 `statusSummaryRequires`、authorized stopCondition boundary rule、failure taxonomy 新码 `boundary-hits-not-authorized` / `status-summary-missing`，`liveValidation.sidecarTemplate` 与 notes 也提示这些规则。package/CLI/façade coverage 覆盖 contract projection、invalid envelope、evidence apply fail-closed 与 no-write invariants。该批不新增 PowerShell runtime logic、不执行 heavy-tool、不修改 public 参数面、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 374 补强 adapter `liveValidation` handoff 的可复制命令字段：`gate -ExecutionReportContract -GateEventId ... -Format json` 的 `liveValidation` 现在同时输出 `validateCommand` / `recordCommand` 与结构化 `validateArgs` / `recordArgs`，让 lane executor / tool adapter 或主 Agent可直接展示或执行 workspace-relative preflight→record 命令，同时保持 args 数组适合自动化消费；package/CLI/façade smoke 覆盖 command string 与 args 一致性。该批不新增 PowerShell runtime logic、不执行 heavy-tool、不修改 public 参数面、不写 observations/authority/confirmed（contract read-only）、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 373 将 adapter contract 从边界说明推进为可直接交接的 `liveValidation` handoff：`gate -ExecutionReportContract -GateEventId ... -Format json` 现在输出 bounded sidecar template、workspace-relative `validateArgs` / `recordArgs`、duplicate replay behavior 与 no-write/no-heavy-tool notes，供 lane executor / tool adapter 在 authorized output workspace cwd 中不重扫 ledger、不拼接命令即可执行 preflight→record 闭环；package/CLI/façade smoke 覆盖 contract handoff projection。该批不新增 PowerShell runtime logic、不执行 heavy-tool、不修改 public 参数面、不写 observations/authority/confirmed（contract read-only）、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 372 强化 nested adapter report evidence replay guard：cwd-relative `gate -Apply -GateEventId ... -ExecutionReportPath adapter-report.json` 重复提交同一 validated sidecar 时返回 `duplicate eventId`、不重复 append observations，并继续保持 authority/confirmed 不写；package/CLI/façade smoke 覆盖 nested authorized output workspace cwd / `REKIT_CALLER_CWD` bridge / public façade product path。该批不新增 PowerShell runtime logic、不执行 heavy-tool、不修改 public 参数面、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 371 补齐 nested authorized output workspace evidence product path：在 Batch 370 的 contract/validation 基础上，lane executor / tool adapter 或主 Agent 现在可从 authorized output workspace cwd 省略 `-Target`，用 workspace-relative `adapter-report.json` 调用 `gate -Apply -GateEventId ... -ExecutionReportPath adapter-report.json -Actor ... -Format json` 记录 post-action observation evidence；CLI 与 façade product-path tests 断言 `executionReportPath` 规范化为 case-relative path、adapter provenance/summary/budget/output refs 保留、observations ledger 写入，且 authority/confirmed 不写。retained PowerShell façade 只扩展 safe-delegation coverage / fake backend capture，不新增业务 runtime 或参数面；该批不执行 heavy-tool、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 370 强化 nested authorized output workspace product path：lane executor / tool adapter 在 authorized output workspace cwd 中可省略 `-Target` 调用 `gate -ExecutionReportContract -GateEventId ... -Format json` 与 `gate -ValidateExecutionReport -GateEventId ... -ExecutionReportPath ... -Format json`；CLI 会把当前 cwd 传入 adapter report validation/intake，`ExecutionReportPath` 保持兼容 case-relative 与 case-contained absolute，并新增当前 workspace 相对 sidecar path fallback，最终仍规范化为 case-relative `reportPath` 且必须落在 authorized output paths 下。retained PowerShell façade 只校准 gate safe-delegation / internal caller-cwd bridge，不新增业务 runtime 或参数面，并由 façade smoke 覆盖 nested workspace 无 `-Target` contract/validation 透传。新增 package tests 覆盖 cwd-relative read-only validation 与 evidence recording，CLI E2E 覆盖 nested output workspace 无 `-Target` contract/validation 与 no-observation-write invariant；该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 observations/authority/confirmed（只读 preflight 场景）、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 369 将 adapter validation failure taxonomy 投影到 read-only execution report contract：`gate -ExecutionReportContract -GateEventId ... -Format json` 现在输出 `validationFailureStages[]` 与 `validationFailureCodes[]`，列出 path/read/decode/schema/identity/refs/budget/boundary/summary 阶段及 stable failure code/stage/description，供 lane executor / tool adapter 执行前消费可预期的 preflight failure contract，而不必从源码或失败样例反推 taxonomy。package/CLI tests 覆盖 contract taxonomy、code/stage 映射与 no-observation-write invariant；该批不新增 PowerShell runtime logic、不修改 façade 参数面、不执行 heavy-tool、不写 observations/authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 368 强化 adapter report validation invalid envelope failure taxonomy：`gate -ValidateExecutionReport` 的 invalid sidecar JSON 现在除 `error` / `errors[]` 外还输出 stable `failureCode` 与 `failureStage`，覆盖 report path out-of-scope、malformed JSON、trailing data、gateEventId/action/status/schema mismatch、out-of-scope refs、budget marker missing、boundary marker missing、summary/escalation/boundary hits 等 strict intake 失败类别，让 lane executor / tool adapter 可以用机器可读 code/stage 决定修 sidecar、重跑或升级，而不解析 error 文本。package/CLI tests 覆盖多类 invalid envelope、partial report 保留与 no-observation-write invariant；该批不新增 PowerShell runtime logic、不修改 façade 参数面、不执行 heavy-tool、不写 observations/authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 367 强化 `gate -ValidateExecutionReport` invalid sidecar 的机器可读只读 envelope：authorized gate 已找到且 report path/sidecar strict intake 失败时，CLI/package API 不再只通过非零错误暴露 invalid report，而是输出 `adapter-execution-report-validation` JSON（`isMutation=false`、`applied=false`、`valid=false`、`error`、`errors[]`、`reportPath`、可用时的 partial normalized `report`、contract boundaries 和 next steps），让 lane executor / tool adapter 能在不写 observations ledger 的前提下消费失败原因并修正 sidecar；缺 `-ExecutionReportPath`、无法定位 authorized gate、flag 组合错误或非 JSON format 仍保持 CLI error。package/CLI tests 覆盖 invalid boundary-hit report 返回 `valid=false` envelope、partial report/path/error/contract 保留与 no-observation-write invariant。该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 observations/authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 366 新增 adapter execution report read-only validation preflight：`gate -ValidateExecutionReport -GateEventId ... -ExecutionReportPath ... -Format json` 会复用正式 `gate -Apply -GateEventId ... -ExecutionReportPath ...` 的 authorized-gate、case-contained path、authorized outputPaths、schema/kind/action/status/gateEventId、budget、refs、boundary/escalation strict intake 校验，输出 `adapter-execution-report-validation` JSON envelope（`isMutation=false`、`applied=false`、`valid=true`、normalized report、contract boundaries 和 next steps），让 lane executor / tool adapter 在写 observations ledger 前校验 sidecar。CLI/façade 增加 `-ValidateExecutionReport` flag 与默认 Go 委托，package/CLI/façade tests 覆盖只读 validation happy path、invalid report fail-closed 与 no-observation-write invariant；README、canonical skill、tool adapter policy、tests guide 与 durable batch docs 同步说明。该批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case durable schema、公共入口删除门禁或远程 CI blocker 状态。
- Batch 365 强化 bounded adapter execution report escalation / boundary marker strict intake：`AdapterReport` 新增 optional bounded `escalation` provenance，`AdapterExecutionReportContract` 输出 `escalationMaxBytes=4096` 并继续暴露 boundary/escalation requirements；`gate -Apply -GateEventId ... -ExecutionReportPath ...` 现在会 fail-closed 拒绝缺少 `boundaryHits` / `escalation` marker 的 `boundary-hit` / `escalated` sidecar，以及 actual budget 超出 authorized budget 却无 marker 的 report，同时会把 validated sidecar escalation 嵌入 observation evidence。package/CLI E2E 覆盖 escalation happy path、marker 缺失拒绝、预算越界拒绝、explicit escalation mismatch、contract 字段与 observations ledger provenance；README、canonical skill、tool adapter policy、release readiness、Go-first convergence、agent-team rollout、tests guide 与 batch plan 同步说明。该批不新增 PowerShell runtime logic、不删除 retained façade、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case shim template、case durable schema migration、公共入口删除门禁或远程 CI blocker 状态。
- Batch 364 强化 installed case shim product-path E2E：新增 CLI 临时 case测试覆盖 `attach -Apply` 写入 installed thin shim 后，在 nested case workspace cwd 中无 `-Target` 运行 `status -Format json` 消费 `caseShim` readiness；随后制造 shim drift，验证 `status.caseShim` 只读暴露 drift / `shimMatchesTemplate=false`，再用 `init -Apply` 刷新 shim 并从同一 nested cwd 验证 `status` readiness 恢复和 `doctor -Format json` valid。该批只加强本地 E2E 与 durable docs，不新增 PowerShell runtime logic、不删除 retained façade、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case shim template 内容、runtime schema migration、公共入口删除门禁或远程 CI blocker 状态；完整 Claude Code slash skill installed user entrypoint E2E 与三平台 runner matrix 仍是后续 gap。
- Batch 363 新增 `status` case shim readiness projection：`status -Format json` 现在输出 `caseShim` envelope，投影 canonical thin shim template / canonical skill readiness counts、warnings，以及 case mode 下 installed shim path 与 `installedShimMatchesTemplate`；case envelope 同步暴露 `shimPath` 与 `shimMatchesTemplate`，文本 status 也显示 case shim readiness，让主 Agent / automation 不必等到 fail-fast `doctor` 才发现 case-local `/rekit` thin shim 缺失或 drift。新增 `caseshim.InspectInstalled` helper 与 package/CLI tests 覆盖 installed shim match / drift；该批只读检测、不静默修复，不新增 PowerShell runtime logic、不删除 retained façade、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、case shim template 内容、runtime schema migration、公共入口删除门禁或远程 CI blocker 状态。
- Batch 362 强化 product-path target discovery：Go runtime 在未显式传 `-Target` 时会从当前工作目录向上定位最近 attached case root，使 lane/workspace 子目录中的 `/rekit` 不再把子目录误当 case root；本地 CLI/case E2E 覆盖 nested lane workspace cwd 中无 `-Target` 执行 `status`、`doctor`、`overview`、`gate -WhatIf` 与 `plan-subagents`，并保持 repo 外 case metadata `templateRoot` runtime discovery。该批不新增 PowerShell runtime logic、不删除 retained façade、不改变 installed `/rekit` / case shim readiness 或远程 CI blocker 状态。
- Batch 361 新增 adapter execution report contract projection：`gate -ExecutionReportContract -GateEventId ... -Format json` 只读投影已授权 gate 的 adapter report contract，输出 `adapter-execution-report` schema version、allowed statuses、required fields、authorized output paths、authorized budget、stop conditions、record/notify、boundary/escalation requirements、denied actions 与 next steps，供 lane executor / tool adapter 在执行前消费 deterministic live-validation contract；CLI 与 retained façade 仅新增 flag 解析/透传，Go runtime 仍不执行 heavy-tool、不写 request/observation/authority/confirmed，`-ExecutionReportContract` 拒绝与 `-Apply`、`-WhatIf` 或 execution evidence detail fields 混用。package/CLI tests 覆盖 contract projection 与 E2E JSON 输出，façade smoke 捕获新 flag；README、skill、tool adapter policy、release readiness、rollout、Go runtime migration 与 tests guide 同步说明。
- Batch 360 校准 Batch 359 后 durable docs / release notes：`CLAUDE.md`、Mission Control product direction、autonomous goal、Go-first convergence、release readiness、PowerShell deprecation 与 reference absorption 现在统一说明 authorized execution observation evidence + bounded adapter execution report strict intake 已落地，Batch 358 pack-memory promote/reconsume package E2E 已落地；真实 session/reviewer orchestration、lane executor / tool-adapter live validation、pack-memory product UX、远程 release-gate green 与跨平台 product-path E2E 仍是后续 gap。本批只做文档事实校准与验证，不改 runtime schema、不执行 heavy-tool、不新增 PowerShell runtime logic、不写 authority/confirmed。
- Batch 359 完成 lane executor / tool-adapter execution report integration slice：`gate -Apply -GateEventId ...` 的 execution evidence mode 新增 `-ExecutionReportPath`，可读取 case 内且位于 authorized output paths 下的 bounded `adapter-execution-report` JSON sidecar，strict 校验 `schemaVersion`、`kind`、adapter/action/status/gateEventId、actual budget、case-relative output/evidence refs、boundary hit tokens 与 summary size 后，把 adapter provenance、report path、默认 status/budget/refs/summary 嵌入 observation evidence；CLI/retained façade 只新增参数解析与 Go 透传，不执行 heavy-tool、不新增 PowerShell runtime logic、不写 authority/confirmed，仍要求已有 strict durable autonomy profile + `authorized-gate` request decision。package tests 覆盖 report happy path、gateEventId mismatch、out-of-scope refs，CLI parse 与 façade capture 覆盖新 flag；tool adapter policy、README、release readiness、rollout 与 tests guide 同步说明 adapter report contract。
- Batch 358 完成 pack-memory reconsume E2E 的 package slice：新增 `promote` package test，将 source case 的 `toolingCandidateSources` observation 走 `Plan` / `CreateCandidates` 脱敏候选，确认 candidate header、case root/absolute path/artifact/capture/address/ctx/task redaction 与 deny-pattern 清理后，模拟人工 review 合入 `tooling/recipes/*`，再用 fresh case `init` + attached metadata + `doctor` 验证后续 case 可通过 pack tooling 资产重新消费该经验，同时断言 `sync/init` 不把 tooling recipe 静默复制进 case managed router、不泄漏 source/fresh case 私有路径或样本化输出。README、promote/sync/design、release readiness、rollout 与 tests guide 同步说明：tooling candidate 仍需人工 review 后合入 catalog/recipes；fresh case 通过 `templateRoot` + `templatePack` 消费 pack tooling；`sync` 不复制 tooling files。本批不新增 PowerShell runtime logic、不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first 或公共 façade 删除边界。
- Batch 357 完成 autonomy execution evidence closure 的 Go-native slice：`gate -Apply` 在原有 pending-gate / authorized-gate request decision 写入之外，新增 `-GateEventId <authorized-gate-event-id>` + `-ExecutionStatus` 的 execution evidence mode，只接受 strict request ledger 中已存在的 `authorized-gate` / `preauthorized` decision，并把 post-action observation evidence 写入 `.rekit/facts/observations.jsonl`；记录 authorized gate provenance、actual budget、output refs、evidence refs、boundary hits、escalation、recordRequired 与 notifyMainOn，output refs 必须保持在 authorized output paths 内，预算超限或 boundary/escalation status 必须显式记录 boundary marker。CLI JSON/text、package tests 与 CLI E2E 覆盖成功写入、幂等 duplicate、pending-gate 拒绝、out-of-scope output refs 拒绝、预算超限缺少 boundary marker 拒绝，以及不写 authority/confirmed。retained `rekit.ps1` 仅转发新的 evidence flags，并用 façade smoke 覆盖 default delegation / no-fallback 参数透传；该批不执行 heavy-tool、不新增 PowerShell runtime logic、不自动 spawn 或管理 lane executor/session，不改变 sync/promote review-first、authority/confirmed 或 public façade deletion 边界；真实 full-trace/debug/inject/patch/dump/network/symex 仍由 lane executor / tool adapter 在授权 profile 内执行。
- Batch 356 完成本地 cross-platform product-path E2E slice：Go runtime 在 cwd/executable 不能发现 repo root 时可通过 attached case metadata 的 `templateRoot` 定位 canonical repo root；日常 case commands 在 case-local 工作目录中可省略 `-Target`，当前覆盖 `overview`、`note`、`start`、`handoff`、`continue`、`reconcile`、`gate`、`sync`、`promote` 与 `plan-subagents` 的 implicit attached-case target resolution，而 `attach`、`repair`、`init`、`bootstrap` 仍要求显式 `-Target` 以避免意外创建/修复写入。新增 runtime discovery unit test 与 CLI 临时 case product-path E2E，覆盖 case cwd 中无 `-Target` 执行 `status`、`doctor`、`start -Apply`、`continue -Apply` 与 `handoff -Apply`；release readiness 文档同步区分本地 CLI/case E2E slice、三平台 runner matrix 与 installed `/rekit`/case shim readiness。该批不新增 PowerShell runtime logic、不删除 retained façade、不自动安装或管理 Claude Code session、不改变 heavy-tool/authority/confirmed 与 sync/promote review-first 边界；远程 release-gate green 仍受 GitHub Actions billing/spending limit blocker 约束。
- Batch 355 完成 reviewer orchestration owner binding：`plan-subagents` planning result、`packet.json` 与 `shardHandoffs[]` 现在绑定 target lane 的 `currentExecutor`、`executorGeneration` 与 last takeover owner snapshot，支持 `-Lane` 指定 intake 目标；strict reviewer result contract 新增必填 `reviewerSession`，reviewer intake 会在写任何 facts 前校验 packet/case/pack/route/shard/items/evidence、route output 与 lane owner binding，executor generation 被 takeover 后 fail-closed；verification-before-decision facts 会记录 `reviewerSession`、`ownerExecutor`、`ownerGeneration`、`ownerBindingMode` 与 `ownerBindingTarget`。主 Agent仍负责 spawn/管理 read-only reviewer，runtime 只生成 artifacts、校验 owner/provenance 与顺序写回，不自动 spawn/注册/管理 reviewer/member session，不写 authority/confirmed、不执行 heavy-tool，不新增 PowerShell runtime logic，sync/promote 仍 review-first。
- Batch 354 完成 replaceable lane executor 最小闭环：`start -Apply` 可通过显式 `-Executor` / `-Actor` / `-Reason` 注册或接管当前 lane executor，首次注册写入 `currentExecutor`、`executorGeneration=1` 与 `executor-registered` lane event，后续不同 executor takeover 递增 generation 并写 `executor-takeover` event；同 executor 重试保持幂等。lane `RESUME.md`、`checkpoints/latest.json`、board、overview JSON/text、`laneExecutorActions[]`、project/lane handoff Markdown 现在投影 current executor、generation 与 last takeover metadata；`reconcile` 同步记录 `lastTakeoverBy`。PowerShell façade 只透传 executor/actor/reason 到 Go backend，不新增 PowerShell runtime logic；该路径不自动 spawn、停止或管理 Claude Code session，不写 authority/confirmed、不执行 heavy-tool，sync/promote 仍 review-first。
- Batch 353 完成 Mission Commander bounded reviewer intake/writeback 本机闭环：Go-native `plan-subagents` planning packet 新增完整 `packetId` identity，reviewer result strict contract 绑定 packet/route/shard/items并验证 packet/case-local evidence、route output schema及顶层 decision/confidence/item/evidence binding、read-only/heavy-action 边界；主 Agent实际 spawn read-only reviewer、接收单个 JSON object后，可用 reviewer-intake `-WhatIf/-Apply` 按 verification-before-decision 顺序写 facts，获得 deterministic/idempotent retry、partial-write recovery JSON 与 overview/handoff/doctor post-validation。public inventory 新增 `case-local-review-writeback` boundary；retained `rekit.ps1` 仅增加 centralized flag guard、caller-CWD relative path normalization 与 capturing façade smoke 覆盖，不承载业务 runtime。out-of-case planning 只生成 dispatch artifacts，不暴露可立即运行的 intake commands；该路径不自动 spawn reviewer、不写 authority/confirmed、不执行 heavy-tool，sync/promote 仍 review-first。
- Batch 352 校准 Batch 351 后 Mission Control durable docs/current state：明确 20 个 public commands 已 Go-owned/no-fallback、`rekit.ps1` 仅 retained compatibility façade，区分 `release-check` inventory ready、本地 gate executed 与远程 CI green；记录 2026-07-18 GitHub Actions 因 billing/spending limit 未启动的外部 blocker，将 case migration/orchestration/current policies 中残留的 PowerShell fallback/owner 同步到 Go owner，并将 common policies、pack templates/snippets/recipes 与 smoke 指南中的 heavy-action 语义统一为 lane docs/packet 只表达意图、显式用户确认或 strict durable `autonomy.json` + `authorized-gate` 才构成 grant，且 `gate -Apply` 只写 request decision、不执行动作；active milestone 从 schema-field increments 转向 Mission Commander reviewer/session orchestration、cross-platform product-path、authorized execution evidence 和 pack-memory reconsume 的 coherent vertical slices。
- Batch 351 / Stage 6 强化 Go-native `plan-subagents` reviewer writeback command bindings：`writebackSequence[]` 现在为每个 main-agent owned step 输出 `commandBindings[]`，把 reviewer result contract、decision mapping、conflict handling、verification/decision `previewCommand` / `applyCommand` 与 post-review validation 直接绑定到 sequence step，并在 `summary.md` 同步列出 `command-binding:`；主 Agent 可按 step 读取 exact note command、required fields 与 expected output 后再执行 WhatIf / manual apply，而 runtime 仍只写 review artifacts、不自动 spawn reviewer、不替 reviewer 写 ledger、不写 board/facts/lanes/handoff/authority/confirmed、不执行 heavy-tool、不新增 PowerShell runtime logic、不改变 public command surface、sync/promote review-first、policy schema 或公共 façade deletion 边界。
- Batch 350 / Stage 6 强化 Go-native `plan-subagents` reviewer writeback sequence：`shardHandoffs[]` 现在输出 `writebackSequence[]`，把 validate reviewer result、map reviewer decision、verification note preview/apply、main decision note preview/apply 与 post-review validation 固化为 main-agent owned step contract，并为每步声明 inputs、must-pass checks、blocked-by、success/failure transitions；`summary.md` 同步列出 writeback-step 与 writeback-blocker，让主 Agent 在调度 read-only reviewer 后可按确定顺序执行 intake、note WhatIf、manual apply 与 validation，而 runtime 仍只写 review artifacts、不自动 spawn reviewer、不替 reviewer 写 ledger、不写 board/facts/lanes/handoff/authority/confirmed、不执行 heavy-tool、不新增 PowerShell runtime logic、不改变 public command surface、sync/promote review-first、policy schema 或公共 façade deletion 边界。
- Batch 349 / Stage 6 强化 Go-native `plan-subagents` reviewer intake decision mapping：`shardHandoffs[]` 现在输出 `reviewerDecisionMappings[]` 与 `conflictHandling[]`，把 reviewer `accept` / `reject` / `defer` / `abandon` / `needs-more-evidence` 显式映射到 verification verdict 与 main merge decision，并列出证据缺失、contract validation 失败、recommendedVerdict 冲突、越权写入或 heavy-tool 请求时的 fail-closed/default handling；`summary.md` 同步列出 decision-map 与 conflict-handling，让主 Agent 在 note preview/apply 前有确定的 intake 映射，而 runtime 仍只写 review artifacts、不自动 spawn reviewer、不替 reviewer 写 ledger、不写 board/facts/lanes/handoff/authority/confirmed、不执行 heavy-tool、不新增 PowerShell runtime logic、不改变 public command surface、sync/promote review-first、policy schema 或公共 façade deletion 边界。
- Batch 348 / Stage 6 强化 Go-native `plan-subagents` reviewer result contract：`shardHandoffs[]` 现在输出 `reviewerResultContract` 与 `intakeChecklist[]`，明确 read-only reviewer 必须返回单个 JSON object、required fields、allowed decisions、evidence rules 与 conflict signals；dispatch prompt 和 `summary.md` 同步展示 result contract / evidence-rule / conflict-signal / intake-check，让主 Agent 在运行 note `previewCommand` / `applyCommand` 前先审查 reviewer 输出、证据引用、冲突和 blocked outputs，而 runtime 仍只写 review artifacts、不自动 spawn reviewer、不替 reviewer 写 ledger、不写 board/facts/lanes/handoff/authority/confirmed、不执行 heavy-tool、不新增 PowerShell runtime logic、不改变 public command surface、sync/promote review-first、policy schema 或公共 façade deletion 边界。
- Batch 347 / Stage 6 强化 Go-native `plan-subagents` ledger preview contract：`ledgerWritebackTemplates[]` 现在为 verification / decision 模板同时输出 note `previewCommand` 与 `applyCommand`、preview checks 和 blocked outputs；`summary.md` 同步列出 writeback preview、apply、preview-check 与 blocked-output，让主 Agent 可先运行 `note -WhatIf -Format json` 复核 isMutation/applied、event kind、lane/target/evidenceRefs 与 wouldExecutorAction，再决定是否手动 apply，而 runtime 仍只写 review artifacts、不自动 spawn reviewer、不替 reviewer 写 ledger、不写 authority/confirmed、不执行 heavy-tool、不新增 PowerShell runtime logic、不改变 public command surface、sync/promote review-first、policy schema 或公共 façade deletion 边界。
- Batch 346 / Stage 6 强化 Go-native `plan-subagents` reviewer verdict writeback templates：`shardHandoffs[]` 现在为每个 shard 输出 `ledgerWritebackTemplates[]`，分别给出 reviewer verification 与 main decision 的 `/rekit note` 命令模板、required fields、allowed values 和 post-review merge guidance；`summary.md` 同步列出 verification / decision writeback 与 review 后合并步骤，让主 Agent 在审查 reviewer 输出后手动落账和决策，而 runtime 仍只写 review artifacts、不自动 spawn agent、不替 reviewer 写 ledger、不写 board/facts/lanes/handoff/authority/confirmed、不执行 heavy-tool、不新增 PowerShell runtime logic、不改变 public command surface、sync/promote review-first、policy schema 或公共 façade deletion 边界。
- Batch 345 / Stage 6 强化 Go-native `plan-subagents` bounded dispatch handoff：`plan-subagents` 的 JSON result 与 `packet.json` 现在输出 `shardHandoffs[]`，每个 shard 都包含 read-only reviewer dispatch prompt、items、read-only boundary、expected output、reviewer writeback、main-agent next action、completion criteria 与 failure handling；`summary.md` 同步列出 shard handoff prompts，让主 Agent 可直接调度短命 reviewer 并记录 verdict，而 runtime 仍只写 review artifacts、不自动 spawn agent、不写 board/facts/lanes/handoff/authority/confirmed、不执行 heavy-tool、不新增 PowerShell runtime logic、不改变 public command surface、sync/promote review-first、policy schema 或公共 façade deletion 边界。
- Batch 344 / Stage 5 收敛 Go-native workstream blocker-aware lane-local next steps：`mission.ExecutorAction` 的 `nextAgentActions` / `escalations` 现在只基于当前 lane typed blockers 生成，ready lane 才建议自己的 `/rekit continue <lane>`，blocked lane 只建议 reconcile / pending-gate / open-decision resolution，paused/closed/unready lane 回到 handoff/read-only；`start`、`handoff`、`continue` 与 `reconcile` 的 apply JSON/text/Markdown next steps 复用该 lane-local action，避免 blocked lane 继续建议 continue 或泄漏其它 ready lane 的 continue。该批同时把 CodeGraph MCP 使用边界写入项目 `CLAUDE.md`，限定为维护本仓库源码/模板/文档时的结构、调用链和影响面分析，不用于外部黑盒程序、样本、trace、dump、capture 或 case artifacts 分析；不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、durable case schema、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 343 / Stage 5 新增 Go-native note executor action delta：`note -WhatIf` JSON envelope 现在返回当前 `executorAction` 与内存模拟 append 后的 `wouldExecutorAction`，实际 append 返回 strict ledger 写入后的 `executorAction`，duplicate eventId 保持 no-op 并只返回未改变的当前 action；candidate、defer/open decision、open intervention 与 pending-gate request 分别投影 shared typed open-decision、intervention、pending-gate blocker，verification 等非 blocker kind 保持 ready，malformed facts JSONL 在 projection/write 前 fail-closed，超过 60 KiB 的 encoded event 在任何写入前拒绝，避免 post-action strict reread 产生部分成功。该批不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、durable case schema、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 342 / Stage 5 新增 Go-native overview Mission Commander action snapshot：`overview -Format json` 现在输出逐 lane `laneExecutorActions[]`，文本 overview 写出同一 executor action index，包含 blocked/ready、typed blocker counts、requirements、blocker reasons 与 resume/handoff command；overview next steps 改为复用 Mission brief typed actions，先处理 reconcile / pending gate / open decision，且只为 ready lane 建议 continue。project handoff 同时改用 shared `mission.LaneExecutorActionSnapshot` row，避免 overview 与 handoff 维护并行 action contract。该批不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、durable case schema、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 341 / Stage 5 新增 Go-native gate executor action projection：`gate -WhatIf` JSON envelope 现在同时暴露当前 `executorAction` 与写入该 request 后的 `wouldExecutorAction`，`gate -Apply` JSON/text 输出 apply 后 `executorAction`；pending-gate request 可直接显示会如何阻塞 lane，authorized-gate request 继续保持非阻塞，主 Agent / lane executor 无需另跑 overview/continue/handoff 或读取 checkpoint 即可判断 gate decision 对下一步接手的影响。该批不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 340 / Stage 5 新增 Go-native reconcile executor action projection：`reconcile` preview/apply JSON envelope 现在暴露同一 `executorAction` snapshot，text output 也显示 executor blocker counts、requirements 与 resume/handoff command；Human-in-the-Lane resolution 前后，主 Agent / lane executor 无需再跑 continue/handoff 或读取 checkpoint 就能判断 intervention 是否清除、还有哪些 pending gate / open decision blocker 与下一步接手命令。该批不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 339 / Stage 5 新增 Go-native start executor action projection：`start` preview/apply JSON envelope 现在暴露同一 `executorAction` snapshot，text output 也显示 executor blocker counts、requirements 与 resume/handoff command；新建、进入或 force refresh lane 后，自动化无需再跑 continue/handoff 或读取 checkpoint 就能看到 blocked/ready、pending gate / open intervention / open decision counts 与下一步接手命令。该批不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 338 / Stage 5 新增 Go-native handoff executor action projection：`handoff` preview/apply JSON envelope 现在对指定 lane 暴露 `executorAction`、对项目级 handoff 暴露 `laneExecutorActions[]`；项目 handoff lane index 与 lane handoff Markdown 也写出同一 typed blocker/action snapshot，包含 blocked/ready、pending gate / open intervention / open decision counts、requirements、resume/handoff command、next actions 与 escalations，使新会话只读 handoff 也能判断 lane executor 下一步。该批不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 337 / Stage 5 新增 Go-native continue executor action envelope/status/digest projection：`continue` preview/apply JSON envelope、run `status.json` 与 `digest.md` 现在暴露同一 `executorAction` snapshot，包含 blocked/ready、typed blocker counts、requirements、resume/handoff command、next actions 与 escalations，使 lane executor / 自动化消费 continue 结果时无需再读取 lane checkpoint 才能判断下一步。该批不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 336 / Stage 5 新增 Go-native typed lane executor blocker projection：lane-local `executorAction` 现在直接基于 typed `mission.Facts` 投影 pending gate、effective open intervention 与 open candidate/decision blocker，并在 `RESUME.md` / `checkpoints/latest.json` 暴露 `pendingGates`、`openInterventions`、`openDecisions` counts，避免从 formatted `blockedLanes` 字符串反解析 blocker reason 导致格式漂移时 executor handoff 判断错误。该批不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 335 / Stage 5 新增 Go-native typed lane checkpoint contract：`writeLaneResume` 的 `checkpoints/latest.json` 现在由 typed `laneCheckpoint` schema 写入，并用 package test 锁定 `missionBrief`、`executorAction`、pending/authorized gate shortcut、open interventions、counts、resume path 与 executor metadata 字段，降低后续 durable executor handoff 字段因 inline map 漂移或遗漏的风险。该批不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 334 / Stage 5 新增 Go-native lane-local executor action snapshot：lane `prompts/RESUME.md` 与 `checkpoints/latest.json` 现在在 lane-local Mission Control brief 之外提供结构化 `executorAction`，直接暴露 blocked/ready、blocker reasons、reconcile/pending-gate/open-decision requirements、resume/handoff command、next agent actions 与 escalations，减少替换 executor 只读 checkpoint 时解析 summary 字符串或重扫 ledger 的需求。该批不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 333 / Stage 5 新增 Go-native lane-local Mission Control brief snapshot：lane `prompts/RESUME.md` 与 `checkpoints/latest.json` 现在持久化 lane-local `missionBrief`，复用 handoff/overview 的 blocker 语义展示 ready/blocked、open decisions、next agent actions 与 escalations；替换 executor 只读本 lane durable state 也能看到当前是否被 pending gate、effective open intervention 或 open candidate/decision 阻塞，同时继续保留 pending/authorized gate snapshot。该批不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 332 / Stage 5 新增 Go-native lane resume/checkpoint authorized-gate handoff snapshot：lane `prompts/RESUME.md` 与 `checkpoints/latest.json` 现在记录 lane-local pending gates 与非阻塞 authorized gates，使替换 executor 只读 lane-local durable prompt/checkpoint 也能看到 gate decision、authorization profile 与 heavy-action 边界；`pending-gate` 仍是唯一 gate blocker，`authorized-gate` 仍只表示 durable autonomy request ledger decision。该批不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 331 / Stage 5 新增 Go-native authorized-gate decision visibility and executor handoff：`mission.Brief` 新增 `authorizedGates`，overview 文本/JSON、project/lane handoff、continue digest/status 与 CLI E2E 均展示 `authorized-gate` 的 authorization decision/profile details；`pending-gate` 仍是唯一 gate blocker，`authorized-gate` 作为 durable autonomy 已授权 request ledger decision 非阻塞可见，便于 lane executor / 新会话无需重扫 ledger 即可看到预授权边界。该批不执行 heavy-tool、不写 authority/confirmed、不新增 PowerShell runtime logic、不改变 sync/promote review-first、public command surface、policy schema 或公共 façade deletion 边界。
- Batch 330 / Stage 5 新增 Go-native durable lane autonomy profile 与 fail-closed heavy-action authorization preflight：新增 `internal/rekit/autonomy` typed profile schema、strict decode/validation、manual-gate default 与 preauthorized/autonomous evaluation；`start -Apply` 为 lane 初始化 `.rekit/lanes/<lane>/autonomy.json`，`start`/`continue`/`handoff`/`overview` JSON/Markdown 暴露 autonomy summary，`doctor` 验证现有 profile；`gate -WhatIf/-Apply` 解析 typed budget/output paths/stop conditions 并基于 durable profile 记录 pending-gate 或 authorized-gate request ledger decision，profile 缺失、过期、越界、budget/output/stop mismatch 或 invalid schema 均 fail-closed；Mission brief 只把 pending-gate 视为 blocker，authorized-gate 不阻塞 ready lane。该批不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first、public command surface 或公共 façade deletion 边界。
- Batch 329 / Stage 5 新增 Go-native durable lane reconcile 与 executor takeover：新增 public command `reconcile`，用 append-only `resolvesEventId` resolution event 关闭 lane-local effective open intervention，并刷新 lane event、lane.json executor generation、RESUME/checkpoint 与 board；`overview`、handoff、Mission Control brief 与 `continue` blocker 改用 effective open intervention projection，`continue -WhatIf/-Apply` 在目标 lane 仍有 open intervention 时 fail-closed 且不创建 run、不写 facts/board/resume；public command surface 扩展为 20 个 command，PowerShell façade 只新增 Go delegation/no-fallback 透传，不新增 PowerShell runtime logic；该批不执行 heavy-tool、不写 authority/confirmed、不改变 sync/promote review-first 或公共 façade deletion 边界。
- Batch 328 / Stage 8 重构 Go-native public surface boundary inventory count plumbing：扩展 `GoNativePublicSurfaceBoundaryCounts` 与 `GoNativePublicSurfaceBoundaryCountsFor`，将 boundary row unknown、duplicate、count mismatch、unsorted、summary mismatch、group mismatch、missing 与 coverage mismatch counts 纳入共享 summary，让 boundary row guards、`mutation-boundary-inventory` façade removal prerequisite readiness、releasecheck tests 与 CLI JSON assertions 复用同一 boundary inventory count summary，减少 raw boundary row scan plumbing；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 327 / Stage 8 重构 Go-native public surface symbol catalog count plumbing：新增 `GoNativePublicSurfaceSymbolCatalogCounts` 与 `GoNativePublicSurfaceSymbolCatalogCountsFor`，并嵌入 `GoNativePublicSurfaceCounts`，让 public command symbol catalog rows、empty symbol 与 empty command guards、façade removal prerequisite readiness、releasecheck tests 与 CLI JSON assertions 复用同一 symbol catalog count summary，减少 raw symbol map scan plumbing；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 326 / Stage 8 重构 Go-native public surface profile catalog count plumbing：新增 `GoNativePublicSurfaceProfileCatalogCounts` 与 `GoNativePublicSurfaceProfileCatalogCountsFor`，并嵌入 `GoNativePublicSurfaceCounts`，让 public command profile catalog rows、empty/duplicate command、unknown boundary、heavy-tool、authority/confirmed、kit-write-without-review 与 review-first-without-apply guards、façade removal prerequisite readiness、releasecheck tests 与 CLI JSON assertions 复用同一 profile catalog count summary，减少 raw profile scan plumbing；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 325 / Stage 8 重构 Go-native public surface catalog 与 mutation boundary count plumbing：新增 `GoNativePublicSurfaceCatalogCounts`、`GoNativePublicSurfaceCatalogCountsFor`、`GoNativePublicSurfaceMutationBoundaryCounts` 与 `GoNativePublicSurfaceMutationBoundaryCountsFor`，并嵌入 `GoNativePublicSurfaceCounts`，让 public command catalog empty/duplicate guards、mutation boundary inventory guards、façade removal prerequisite readiness、releasecheck tests 与 CLI JSON assertions 复用同一 catalog / mutation-boundary count summary，减少 raw catalog length、duplicate scan 与 mutation-boundary length/unknown plumbing；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 324 / Stage 8 重构 Go-native public surface coverage count plumbing：新增 `GoNativePublicSurfaceCoverageCounts`、`GoNativePublicSurfaceCoverageCountsFor`、`GoNativePublicSurfaceCoverageDrift` 与 `GoNativePublicSurfaceCoverageDriftFor`，并嵌入 `GoNativePublicSurfaceCounts`，让 handler/symbol/profile public command coverage guards、façade removal prerequisite readiness、releasecheck tests 与 CLI JSON assertions 复用同一 coverage count/drift summary，减少 raw handler/symbol/profile missing/unknown plumbing；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 323 / Stage 8 重构 Go-native public surface profile summary count plumbing：新增 `GoNativePublicSurfaceProfileSummaryCounts` 与 `GoNativePublicSurfaceProfileSummaryCountsFor`，并嵌入 `GoNativePublicSurfaceCounts`，让 profile summary readiness guards、group/summary consistency guards、boundary summary checks、releasecheck tests 与 CLI JSON assertions 复用同一 profile summary count summary，减少 raw `CommandProfileSummary` field / boundary map plumbing；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 322 / Stage 8 重构 Go-native public surface boundary count plumbing：新增 `GoNativePublicSurfaceBoundaryCounts`、`GoNativePublicSurfaceBoundaryCountsFor`、`GoNativePublicSurfaceBoundaryRowCounts` 与 `GoNativePublicSurfaceBoundaryRowCountsFor`，并嵌入 `GoNativePublicSurfaceCounts`，让 mutation-boundary readiness、boundary row validation、releasecheck tests 与 CLI JSON assertions 复用同一 boundary row / command count summary，减少 raw boundary row length/count plumbing；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 321 / Stage 8 重构 Go-native façade removal prerequisite count plumbing：新增 `GoNativePublicSurfaceFacadeRemovalPrerequisiteCounts` 与 `GoNativePublicSurfaceFacadeRemovalPrerequisiteCountsFor`，并嵌入 `GoNativePublicSurfaceCounts`，让 Go-native public surface façade removal readiness helper、public façade removal prerequisite summary、releasecheck tests 与 CLI JSON assertions 复用同一 prerequisite row / not-ready count summary，减少 raw prerequisite length/readiness plumbing；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 320 / Stage 8 重构 public façade removal aggregate validation count plumbing：让 `publicFacadeRemovalPlan` 与 `publicFacadeRemovalImpact` 的 aggregate validation guards 复用已有 plan/deletion/execution/impact count summaries，减少 replacement/deletion/execution/boundary/impact aggregate raw `len(...)` plumbing；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 319 / Stage 8 重构 public façade removal warning count plumbing：扩展 `PublicFacadeRemovalPlanCounts` 与 `PublicFacadeRemovalImpactCounts` 的 warnings coverage，让 removal plan/impact readiness guards、releasecheck tests 与 CLI JSON assertions 复用同一 warning count summary，减少 plan/impact warning length plumbing；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 318 / Stage 8 重构 public façade removal validation command test plumbing：新增 releasecheck package 与 CLI JSON assertion 本地 validation smoke helpers，让 public façade removal helper predicates 复用同一 validation command count + release-check JSON command smoke guard，减少 replacement/deletion/execution/boundary/recovery/documentation/impact 测试中的重复 `counts.ValidationCommands == 8` 与 required command contains plumbing；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 317 / Stage 8 重构 public façade removal validation command coverage plumbing：新增 `publicFacadeRemovalMissingValidationCommands` helper，让 removal plan 与 removal impact 的 replacement entrypoint、deletion gate、execution step、boundary check、recovery step、documentation target、impact work item、migration target 与 smoke migration target validation command coverage guards 复用同一 required-command diff helper，收敛重复的 required validation command contains plumbing；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 316 / Stage 8 重构 public façade removal validation count plumbing：让 `publicFacadeRemovalPlan` 与 `publicFacadeRemovalImpact` 的 row validation guards 复用 Batch 315 的 per-row count helpers，收敛 replacement entrypoint、deletion gate、execution step、boundary check、recovery step、documentation target、impact work item、migration target 与 smoke migration target validation path 中的 raw `len(...)` guard plumbing；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 315 / Stage 8 重构 public façade removal row count plumbing：新增 public façade removal replacement entrypoint、deletion gate、execution step、boundary check、recovery step、documentation target、impact work item、migration target 与 smoke migration target row count helpers，让 public façade removal aggregate counts、releasecheck tests 与 CLI JSON assertions 复用同一 per-row count summary，收敛 validation/input/output/escalation/boundary/audit/path counts；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 314 / Stage 8 重构 PowerShell deprecation nested count plumbing：新增 `PowerShellFallbackRetirementCounts`、`PowerShellFacadeRuntimeCounts`、`PowerShellPublicFacadeCounts`、`PowerShellModuleRemovalCounts`、`PowerShellModuleReferencesCounts` 与对应 `*CountsFor` helper，并将其嵌入 `PowerShellDeprecationCounts`，让 PowerShell deprecation 子 inventory readiness、public façade removal prerequisites、releasecheck tests 与 CLI JSON assertions 复用同一 nested count summary，收敛 fallback/façade/module-removal/module-reference warnings 与 coverage counts；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 313 / Stage 8 重构 Go-native public surface coverage count plumbing：扩展 `GoNativePublicSurfaceCounts` 的 warnings coverage，让 Go-native public surface readiness、CLI `release-check` warning guard、releasecheck tests 与 CLI JSON assertions 复用同一 coverage/warning count summary，收敛 commands/handlers/symbols/profiles/mutation-boundaries/warnings counts；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 312 / Stage 8 重构 public façade removal top-level count plumbing：新增 `PublicFacadeRemovalCounts` 与 `PublicFacadeRemovalCountsFor`，让 public façade removal readiness、release handoff details、CLI `release-check` text output、releasecheck tests 与 CLI JSON assertions 复用同一 top-level removal summary，收敛 prerequisites/warnings 以及 removal plan、deletion gate、execution step、impact nested counts；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 311 / Stage 8 重构 Go-native public surface nested count plumbing：新增 `GoNativePublicSurfaceGroupCounts`、`GoNativePublicSurfacePolicyCounts`、`GoNativePublicSurfacePolicyRowCounts` 与对应 `*CountsFor` helper，让 Go-native public surface readiness、releasecheck tests 与 CLI JSON assertions 复用 group/policy nested counts，收敛 command profile group、boundary group 与 policy violation command counts；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 310 / Stage 8 重构 release handoff count plumbing：新增 `ReleaseHandoffCounts`、`ReleaseHandoffCountsFor`、`ReleaseHandoffPackMaturityCounts` 与 `ReleaseHandoffPackMaturityCountsFor`，让 release handoff readiness、CLI `release-check` text output、releasecheck tests 与 CLI JSON assertions 复用同一 handoff summary，收敛 readFirst/signals/knownGaps/validation/nextActions/warnings/pack-maturity counts；release-check JSON schema、text key、signal count、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 309 / Stage 8 重构 PowerShell deprecation inventory count plumbing：新增 `PowerShellDeprecationCounts` 与 `PowerShellDeprecationCountsFor`，让 PowerShell deprecation readiness、release handoff、CLI `release-check` text output 与 releasecheck/CLI tests 复用同一 deprecation count summary，收敛 command/module/freeze/fallback/façade/module-removal/module-reference/warning counts；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 308 / Stage 8 重构 release-check 顶层 inventory count plumbing：新增 `ReleaseCheckResultCounts` 与 `ReleaseCheckResultCountsFor`，让 release-check readiness、release handoff、CLI `release-check` text output 与 releasecheck/CLI tests 复用同一顶层 inventory count summary，收敛 recommended/required/document/pack/gate-action/boundary/known-gap/warning counts；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 307 / Stage 8 重构 CI release gate count plumbing：新增 `CIReleaseGateCounts` 与 `CIReleaseGateCountsFor`，让 CI release gate readiness、release handoff、CLI `release-check` text output 与 releasecheck/CLI tests 复用同一 CI gate count summary，收敛 workflow/job/required-command/forbidden-string/warning counts；release-check JSON schema、text key、CI release gate 语义、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 306 / Stage 8 重构 readiness inventory count plumbing：新增 `caseshim.ReadinessCounts` / `caseshim.ReadinessCountsFor` 与 `defaultdocs.ReadinessCounts` / `defaultdocs.ReadinessCountsFor`，让 case shim readiness、public default docs readiness、release handoff、CLI `release-check` text output 与 package/CLI tests 复用同一 readiness count summary，收敛 required/canonical/forbidden/boundary/document/warning counts；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 305 / Stage 8 重构 Go-native public surface count plumbing：新增 `GoNativePublicSurfaceCounts` 与 `GoNativePublicSurfaceCountsFor`，让 release handoff、CLI `release-check` text output、facade removal prerequisite summaries 与 CLI/releasecheck tests 复用同一 public surface count summary，收敛 commands/handlers/symbols/profiles/boundaries/policies/prerequisites 与 profile boundary counts；release-check JSON schema、text key、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 304 / Stage 8 重构 public façade removal ancillary count plumbing：新增 `PublicFacadeRemovalPlanCounts` 与 `PublicFacadeRemovalImpactCounts` 及对应 `*CountsFor` helper，让 replacement entrypoint、boundary/recovery/documentation target、impact work item、migration target 与 smoke migration target counts 复用共享 summary，移除 releasecheck/CLI/test 中剩余的 ancillary `publicFacadeRemoval*Count` helper plumbing；release-check JSON schema、text key、`releaseHandoff.signals[]` count、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 303 / Stage 8 重构 public façade removal deletion gate count plumbing：新增 `PublicFacadeRemovalDeletionGateCounts` 与 `PublicFacadeRemovalDeletionGateCountsFor`，让 releasecheck handoff、inventory summary、CLI text output 与 CLI/releasecheck tests 复用同一 deletion gate count summary，移除 releasecheck/CLI/test 中重复的 `publicFacadeRemovalDeletionGate*Count` helper plumbing；release-check JSON schema、text key、`releaseHandoff.signals[]` count、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 302 / Stage 8 重构 public façade removal execution step count plumbing：新增 `PublicFacadeRemovalExecutionStepCounts` 与 `PublicFacadeRemovalExecutionStepCountsFor`，让 releasecheck handoff、inventory summary、CLI text output 与 CLI/releasecheck tests 复用同一 execution step count summary，移除 releasecheck/CLI/test 中重复的 `publicFacadeRemovalExecution*Count` helper plumbing；release-check JSON schema、text key、`releaseHandoff.signals[]` count、public command 集合与既有边界语义保持不变。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 301 / Stage 8 新增 public façade removal execution step escalation boundary guards 与 escalation audit checks：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `escalationBoundaryGuards[]` 与 `escalationAuditChecks[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤升级过程必须遵守的边界保护和必须通过的审计检查固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionEscalationBoundaryGuards=15` 与 `executionEscalationAuditChecks=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 300 / Stage 8 新增 public façade removal execution step escalation state transitions：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `escalationStateTransitions[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤升级过程允许的状态转换固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionEscalationStateTransitions=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 299 / Stage 8 新增 public façade removal execution step escalation ledger events：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `escalationLedgerEvents[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤升级过程必须记录的账本事件固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionEscalationLedgerEvents=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 298 / Stage 8 新增 public façade removal execution step escalation reopen conditions：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `escalationReopenConditions[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤升级关闭后必须重新打开的条件固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionEscalationReopenConditions=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 297 / Stage 8 新增 public façade removal execution step escalation closure checks：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `escalationClosureChecks[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤升级关闭前必须执行的检查固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionEscalationClosureChecks=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 296 / Stage 8 新增 public façade removal execution step escalation resolution artifacts：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `escalationResolutionArtifacts[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤升级收束后必须记录的决策/证据产物固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionEscalationResolutionArtifacts=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 295 / Stage 8 新增 public façade removal execution step escalation stop conditions：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `escalationStopConditions[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤升级后必须停止且不能重试的条件固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionEscalationStopConditions=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 294 / Stage 8 新增 public façade removal execution step escalation retry conditions：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `escalationRetryConditions[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤升级后允许重新尝试的条件固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionEscalationRetryConditions=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 293 / Stage 8 新增 public façade removal execution step escalation decision options：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `escalationDecisionOptions[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤升级 owner 或产品决策时允许选择的决策选项固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionEscalationDecisionOptions=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 292 / Stage 8 新增 public façade removal execution step escalation handoff steps：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `escalationHandoffSteps[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤升级 owner 或产品决策时必须完成的交接动作固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionEscalationHandoffSteps=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 291 / Stage 8 新增 public façade removal execution step escalation recipients：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `escalationRecipients[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤升级 owner 或产品决策时必须路由的接收方固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionEscalationRecipients=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 290 / Stage 8 新增 public façade removal execution step escalation evidence：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `escalationEvidence[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤升级 owner 或产品决策时必须附带的证据要求固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionEscalationEvidence=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 289 / Stage 8 新增 public façade removal execution step escalation triggers：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `escalationTriggers[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤需要升级 owner 或产品决策的触发条件固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionEscalationTriggers=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 288 / Stage 8 新增 public façade removal execution step state transitions：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `stateTransitions[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤 pending/running/blocked/completed 状态间的迁移规则固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionStateTransitions=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 287 / Stage 8 新增 public façade removal execution step ledger events：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `ledgerEvents[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤 start/failed/completed 状态必须记录的账本事件固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionLedgerEvents=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 286 / Stage 8 新增 public façade removal execution step verification artifacts：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `verificationArtifacts[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤完成后必须留存的验证证据固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionVerificationArtifacts=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 285 / Stage 8 新增 public façade removal execution step remediation actions：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `remediationActions[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤失败后的修复动作固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionRemediationActions=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 284 / Stage 8 新增 public façade removal execution step failure signals：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `failureSignals[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤失败时可观测的诊断信号固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionFailureSignals=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 283 / Stage 8 新增 public façade removal execution step audit checks：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `auditChecks[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤必须通过的审计检查固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionAuditChecks=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 282 / Stage 8 新增 public façade removal execution step boundary guards：`publicFacadeRemoval.removalPlan.executionSteps[]` 现在为每个 required step 输出 `boundaryGuards[]`，把未来独立公共 façade removal batch 中 verify alternatives、migrate references、retire smoke、delete façade 与 rerun gate 五个步骤必须遵守的 PowerShell-free、revertable、no external effects 与 no hidden regression 边界固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionBoundaryGuards=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 281 / Stage 8 新增 public façade removal deletion gate escalation audit checks：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationAuditChecks[]`，把未来独立公共 façade removal batch 中升级收束后必须审计的替代入口、公开引用、façade smoke、恢复路径与 release gate 结果固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationAuditChecks=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 280 / Stage 8 新增 public façade removal deletion gate escalation boundary guards：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationBoundaryGuards[]`，把未来独立公共 façade removal batch 中升级流程必须遵守的 no PowerShell runtime、no hidden public reference loss、no default façade smoke regression、revertability 与 no heavy-tool/authority/confirmed 边界固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationBoundaryGuards=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 279 / Stage 8 新增 public façade removal deletion gate escalation state transitions：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationStateTransitions[]`，把未来独立公共 façade removal batch 中升级流程允许的 opened / decision-pending / retry-ready / stopped 状态转换固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationStateTransitions=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 278 / Stage 8 新增 public façade removal deletion gate escalation ledger events：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationLedgerEvents[]`，把未来独立公共 façade removal batch 中升级过程必须记录的账本事件固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationLedgerEvents=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 277 / Stage 8 新增 public façade removal deletion gate escalation reopen conditions：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationReopenConditions[]`，把未来独立公共 façade removal batch 中升级关闭后必须重新打开的条件固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationReopenConditions=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 276 / Stage 8 新增 public façade removal deletion gate escalation closure checks：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationClosureChecks[]`，把未来独立公共 façade removal batch 中升级关闭前必须执行的检查固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationClosureChecks=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 275 / Stage 8 新增 public façade removal deletion gate escalation resolution artifacts：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationResolutionArtifacts[]`，把未来独立公共 façade removal batch 中升级收束后必须记录的决策/证据产物固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationResolutionArtifacts=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 274 / Stage 8 新增 public façade removal deletion gate escalation stop conditions：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationStopConditions[]`，把未来独立公共 façade removal batch 中升级后必须停止而不能重试的条件固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationStopConditions=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 273 / Stage 8 新增 public façade removal deletion gate escalation retry conditions：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationRetryConditions[]`，把未来独立公共 façade removal batch 中升级后允许重试的条件固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationRetryConditions=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 272 / Stage 8 新增 public façade removal deletion gate escalation decision options：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationDecisionOptions[]`，把未来独立公共 façade removal batch 中触发升级后的可选决策固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationDecisionOptions=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 271 / Stage 8 新增 public façade removal deletion gate escalation handoff steps：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationHandoffSteps[]`，把未来独立公共 façade removal batch 中触发升级时必须执行的交接步骤固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationHandoffSteps=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 270 / Stage 8 新增 public façade removal deletion gate escalation recipients：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationRecipients[]`，把未来独立公共 façade removal batch 中触发升级时应该路由到的责任接收者固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationRecipients=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 269 / Stage 8 新增 public façade removal deletion gate escalation evidence：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationEvidence[]`，把未来独立公共 façade removal batch 中触发升级时必须附带的证据要求固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationEvidence=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 268 / Stage 8 新增 public façade removal deletion gate escalation triggers：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `escalationTriggers[]`，把未来独立公共 façade removal batch 中 gate 未满足且不可本地修复时必须提升到主 Agent / 用户决策的触发条件固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateEscalationTriggers=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 267 / Stage 8 新增 public façade removal deletion gate failure signals：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `failureSignals[]`，把未来独立公共 façade removal batch 中 gate 未满足时可观测的失败信号固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateFailureSignals=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 266 / Stage 8 新增 public façade removal deletion gate remediation actions：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `remediationActions[]`，把未来独立公共 façade removal batch 中 gate 未满足时必须先执行的修复动作固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateRemediationActions=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 265 / Stage 8 新增 public façade removal deletion gate blocked execution steps：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `blockedExecutionSteps[]`，把未来独立公共 façade removal batch 中被门禁阻断的 delete-public-facade / rerun-release-gate 执行步骤固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateBlockedExecutionSteps=10`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 264 / Stage 8 新增 public façade removal deletion gate verification artifacts：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `verificationArtifacts[]`，把未来独立公共 façade removal batch 删除前必须产出或检查的证据项固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateVerificationArtifacts=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 263 / Stage 8 新增 public façade removal deletion gate exit criteria：`publicFacadeRemoval.removalPlan.deletionGates[]` 现在为每个 blocking gate 输出 `exitCriteria[]`，把未来独立公共 façade removal batch 删除前必须满足的可判定退出条件固化为机器可读清单；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGateExitCriteria=15`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 262 / Stage 8 新增 public façade removal deletion gates：`publicFacadeRemoval.removalPlan` 现在输出 `deletionGates[]`，把未来独立公共 façade removal batch 必须先通过的 go-native-alternatives-ready、public-references-migrated、facade-smoke-retired、recovery-path-ready 与 release-gate-green 五个 blocking gates 固化为机器可读删除门禁，并要求每项带 input inventory 与 validation commands；release-check text 与 `releaseHandoff.signals[]` 同步展示 `deletionGates=5 deletionGateValidationCommands=40`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 261 / Stage 8 新增 public façade removal replacement entrypoints：`publicFacadeRemoval.removalPlan` 现在输出 `replacementEntrypoints[]`，把未来独立公共 façade removal batch 的 canonical `/rekit` skill、case-local thin shim、direct Go CLI 与 cross-platform release gate 四个替代入口固化为机器可读清单，并要求每项带 audience、purpose、Go-native backed / user-facing 标记与 validation commands；release-check text 与 `releaseHandoff.signals[]` 同步展示 `replacementEntrypoints=4 replacementValidationCommands=32`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 260 / Stage 8 新增 public façade removal boundary checks：`publicFacadeRemoval.removalPlan` 现在输出 `boundaryChecks[]`，把未来独立公共 façade removal batch 必须保留的 no PowerShell runtime logic、no actual heavy-tool、no authority/confirmed write、sync/promote review-first、case-local write semantics 与 no external effects 六个边界固化为机器可读检查，并要求每项带 evidence 与 validation commands；release-check text 与 `releaseHandoff.signals[]` 同步展示 `boundaryChecks=6 boundaryValidationCommands=48`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 259 / Stage 8 新增 public façade removal execution steps：`publicFacadeRemoval.removalPlan` 现在输出 `executionSteps[]`，把未来独立公共 façade removal batch 的 verify Go-native alternative、migrate public references、retire façade smoke、delete public façade 与 rerun release gate 五个 required steps 固化为机器可读执行顺序，并要求每步带 dependencies、input inventory、output artifacts、validation commands、no PowerShell runtime logic 与 no external effects 边界；release-check text 与 `releaseHandoff.signals[]` 同步展示 `executionSteps=5 executionValidationCommands=40`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 258 / Stage 8 新增 public façade removal migration targets：`publicFacadeRemoval.removalImpact` 现在输出 `migrationTargets[]`，把全部 public façade references 转为 required / Go-native preferred / validationCommands 绑定的机器可读迁移目标，并对 roadmap/history docs 保留 historical context 边界；release-check text 与 `releaseHandoff.signals[]` 同步展示 `migrationTargets=74 migrationValidationCommands=592`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 257 / Stage 8 新增 public façade removal smoke migration targets：`publicFacadeRemoval.removalImpact` 现在输出 `smokeMigrationTargets[]`，把未来公共 façade removal batch 中必须迁移或退休的 façade compatibility smoke 与 façade-dependent smoke 明确为 Go-native preferred、no facade compatibility、retire façade assertions 的机器可读目标，并要求每个 target 带 action 与 `validationCommands[]`；release-check text 与 `releaseHandoff.signals[]` 同步展示 `smokeMigrationTargets=29 smokeMigrationValidationCommands=232`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 256 / Stage 8 新增 public façade removal documentation targets：`publicFacadeRemoval.removalPlan` 现在输出 `documentationTargets[]`，锁定未来公共 façade removal batch 必须同步的 README、CLAUDE、canonical `/rekit` skill、case shim、release readiness、PowerShell deprecation、Go-first plan、batch-plan 与 CHANGELOG，并要求 targets 带 purpose/action/validation commands；release-check text 与 `releaseHandoff.signals[]` 同步展示 `documentationTargets=9 documentationValidationCommands=72`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 255 / Stage 8 新增 public façade removal recovery steps：`publicFacadeRemoval.removalPlan` 现在输出 `recoverySteps[]`，把未来公共 façade 删除失败时必须恢复的 separate revertable commit、`rekit/rekit.ps1`、同步文档/release notes 与 Go-native release gate 重新验证纳入机器可读恢复计划，并要求 recovery steps 带 action、paths 与 `validationCommands[]`；release-check text 与 `releaseHandoff.signals[]` 同步展示 `recoverySteps=4 recoveryValidationCommands=32`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 254 / Stage 8 新增 public façade removal impact validation commands：`publicFacadeRemoval.removalImpact.workItems[]` 现在为每个 façade removal impact category 输出 `validationCommands[]`，把后续公共 façade 删除批次的逐类处理动作与推荐 Go-native release gate 验证命令绑定，并要求 validation commands 非空且 release-check text / `releaseHandoff.signals[]` 同步展示 `validationCommands=64`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 253 / Stage 8 新增 public façade removal impact work items：`publicFacadeRemoval.removalImpact` 现在输出 `workItems[]`，把每个 façade removal impact category 映射到 required action、count 与 paths，并要求 work items 覆盖全部 reference categories、action 非空；release-check text 与 `releaseHandoff.signals[]` 同步展示 `workItems=8`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 252 / Stage 8 新增 public façade removal impact inventory：`publicFacadeRemoval` 现在输出 `removalImpact`，并把 `removal-impact-inventoried` 纳入 prerequisites，扫描仓库内 `rekit/rekit.ps1` / `rekit.ps1` / `facade-smoke.ps1` 引用并分类为 public façade entrypoint、facade compatibility smoke、facade-dependent smoke、pack wrapper compatibility、public default docs、roadmap/history docs 与 release inventory/test 影响面；release-check text 与 `releaseHandoff.signals[]` 同步展示 `removalImpact=true`、impact references/categories 和 unclassified count。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 251 / Stage 8 新增 public façade removal plan readiness：`publicFacadeRemoval` 现在输出 `removalPlan`，并把 `removal-plan-documented` 纳入 prerequisites，要求 `docs/powershell-deprecation.md` 持续记录未来公共 façade 删除独立 batch 的替代入口、恢复计划、验证命令、文档同步、CHANGELOG、no PowerShell runtime logic 与 no actual heavy-tool/authority 边界；release-check text 与 `releaseHandoff.signals[]` 同步展示 `removalPlan=true planChecks=9`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 250 / Stage 8 新增 top-level public façade removal prerequisite inventory：`release-check -Format json` 现在输出 `publicFacadeRemoval`，汇总公共 façade retained/documented boundary、19 个 façade command 的 Go-default/no-fallback 覆盖、Go-native public surface readiness、legacy runtime detached、legacy module removal settled 与 module reference blockers clear 六条 prerequisites；release-check text 与 `releaseHandoff.signals[]` 同步展示 `public facade removal ... prerequisites=6`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 249 / Stage 8 新增 Go-native public surface façade removal prerequisites：`goNativePublicSurface` 现在输出并校验 `facadeRemovalReady` 与 `facadeRemovalPrerequisites[]`，把未来删除公共 `rekit/rekit.ps1` façade 前必须满足的 entrypoint、catalog/handler/symbol/profile coverage、mutation boundary inventory、profile policy guards 与 unsupported-command diagnostic 固化为机器可读 prerequisites；release-check text 与 `releaseHandoff.signals[]` 同步展示 `facadeRemovalReady=true facadePrerequisites=5`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 248 / Stage 8 新增 Go-native public command profile policy rows：`goNativePublicSurface` 现在输出并校验 `commandProfilePolicies[]`，把 public profile guardrails（no heavy-tool、no authority/confirmed、kit write review-first、review-first apply-required、known mutation boundary）固化为机器可读 policy rows，并要求 violation count 为 0；release-check text 与 `releaseHandoff.signals[]` 同步展示 `policyRows=5 policyViolations=0`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 247 / Stage 8 新增 Go-native public command per-boundary rows：`goNativePublicSurface` 现在输出并校验 `commandProfileBoundaries[]`，把 7 个 public mutation/read-only boundary 升级为一等 JSON rows（`boundary`、`count`、`commands[]`），并要求 rows 与 `mutationBoundaries[]`、`commandProfileSummary.boundaries` 和 `commandProfileGroups.byBoundary` 一致；release-check text 与 `releaseHandoff.signals[]` 同步展示 boundary rows、case-local apply、kit review-first 与 read-only command sets。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 246 / Stage 8 新增 Go-native public command profile groups inventory：`goNativePublicSurface` 现在输出并校验 `commandProfileGroups`，把 read-only、mutating、case write、kit write、review-first、apply-required、heavy-tool 与 authority/confirmed 计数反向展开为具体 command sets，并按 mutation boundary 暴露 `byBoundary` 分组；release-check text 与 `releaseHandoff.signals[]` 同步展示 read-only、review-first 与 kit-write command groups。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 245 / Stage 8 新增 Go-native public command profile summary counts：`goNativePublicSurface` 现在输出并校验 `commandProfileSummary`，把 19 个 public command profiles 汇总为 `total=19`、`readOnly=5`、`mutating=14`、`writesCase=13`、`writesKit=1`、`reviewFirst=3`、`applyRequired=11`、`heavyTool=0` 与 `authorityConfirmed=0`，并校验 per-boundary counts 与 profile catalog 一致；release-check text 与 `releaseHandoff.signals[]` 同步展示这些 summary counts。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 244 / Stage 8 新增 Go-native public command mutation boundary catalog：`goNativePublicSurface` 现在输出并校验 `commandProfiles[]` 与 `mutationBoundaries[]`，为 19 个 Go-native public commands 固化 read-only、case-local append/apply/bootstrap、review artifact、review-first 与 kit review-first 等机器可读边界，并继续要求 public command profile 不标记 actual heavy-tool 或 authority/confirmed 写入；release-check text 与 `releaseHandoff.signals[]` 同步展示 `profiles=19 boundaries=7`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 243 / Stage 8 加固 Go-native public command surface handler coverage：`goNativePublicSurface` 现在输出并校验 `handlerCommands[]` 与 `symbolCommands{}`，要求 Go CLI dispatcher handlers、public command symbols 与 Go-owned command catalog 同步覆盖 19 个 public commands；release-check text 与 `releaseHandoff.signals[]` 同步展示 `handlers=19 symbols=19`，并新增 dispatcher drift 测试。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command 输出既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 242 / Stage 8 新增 Go-native public command surface inventory：新增 `internal/rekit/commands` 作为 Go-owned public command catalog，并在 `release-check -Format json` 顶层输出 `goNativePublicSurface`，机器可读地锁定 `cmd/rekit` entrypoint、19 个 Go-native public commands、`go run ./cmd/rekit -- -Command <command>` 替代路径、unsupported command diagnostic，以及 Go-native catalog 与 PowerShell public façade command surface 的双向一致；release-check text 与 `releaseHandoff.signals[]` 同步展示 `Go-native public surface ... commands=19 ... unsupportedDiagnostic=true`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 public command 集合、Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 241 / Stage 8 新增 Go-owned public façade retention inventory：`release-check -Format json` 的 `powerShellDeprecation` 现在输出 `publicFacade` 子库存，机器可读地锁定 `rekit/rekit.ps1` 仍作为迁移期公共 façade 保留、19 个 façade command 均为 Go-default/no-fallback、底层 Go-native 替代路径存在，以及未来删除公共 façade 必须作为独立 removal batch 处理；release-check text 与 `releaseHandoff.signals[]` 同步展示 `publicFacade=true retained=true facadeCommands=19 noFallback=19`。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 `rekit.ps1` delegation/no-fallback semantics、Go command output 既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed、policy schema migration 或外部副作用边界。
- Batch 240 / Stage 8 物理删除 legacy PowerShell runtime modules：删除 `rekit/lib/*.ps1`，保留 `rekit/rekit.ps1` 作为迁移期 façade；`powerShellDeprecation.moduleRemoval` 与 `fallbackRetirement` 新增/同步 retired module baseline，release-check text 与 `releaseHandoff.signals[]` 锁定 `removalModules=0 retiredModules=13`、`removalCandidates=0 retired=13 facadeDeps=0 undocumented=0`，并让 manifest release invariant 防止 legacy lib modules 回归。该批不删除公共 façade，不新增 PowerShell runtime logic，不改变 `rekit.ps1` delegation semantics、Go output schema、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 239 / Stage 8 移除 façade smoke 的 legacy runtime sentinel compatibility fixture：`facade-smoke.ps1` 不再复制 `rekit/` 并改写隔离 `lib/Manifest.ps1`，而是直接检查公共 façade source 不包含 legacy module imports / retired dispatcher patterns，并继续验证默认 Go delegation 与 disabled no-fallback 行为；`powerShellDeprecation.moduleReferences` 基线同步变为 `moduleReferences=true activeTests=0 fixtures=0 blockers=0 unclassified=0`。该批不删除 PowerShell 文件，不新增 PowerShell runtime logic，不改变 `rekit.ps1` delegation semantics、Go output schema、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 238 / Stage 8 将 continue preflight 从 legacy PowerShell module dependency 收敛为 Go-owned package coverage：`rekit/tests/continue-preflight-smoke.ps1` 现在只作为 Go test wrapper，覆盖 `internal/rekit/workstream` 的 authority append policy matrix 与 `internal/rekit/cli` 的 continue WhatIf / Apply guard，不再 dot-source `rekit/lib/*.ps1`；`powerShellDeprecation.moduleReferences` 基线同步变为 `moduleReferences=true activeTests=0 fixtures=1 blockers=0 unclassified=0`，release-check text 与 `releaseHandoff.signals[]` 同步更新。该批不删除 PowerShell 文件，不新增 PowerShell runtime logic，不改变 `rekit.ps1` delegation semantics、Go output schema、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed 或外部副作用边界；remaining compatibility fixture 继续由 `facade-smoke.ps1` 隔离验证。
- Batch 237 / Stage 8 新增 Go-owned PowerShell module reference surface inventory：`release-check -Format json` 的 `powerShellDeprecation` 现在输出 `moduleReferences` 子库存，机器可读地扫描并分类 `rekit/lib/*.ps1` 引用面，区分 active test dependencies、compatibility fixtures、inventory guards、documentation references、historical references、removal blockers 与 unclassified references；text output 与 `releaseHandoff.signals[]` 同步展示 `moduleReferences=true activeTests=8 fixtures=1 blockers=0 unclassified=0`，为后续独立删除或归档 PowerShell 文件前处理主动测试依赖和 fixture 提供确定性清单。该批只做 Go-owned release inventory、测试和文档，不删除 PowerShell 文件，不新增 PowerShell runtime logic，不改变命令集合、Go output schema 中既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 236 / Stage 8 新增 Go-owned PowerShell module removal readiness inventory：`release-check -Format json` 的 `powerShellDeprecation` 现在输出 `moduleRemoval` 子库存，机器可读地列出 deprecation matrix 中的 removal-candidate PowerShell modules、是否仍存在、是否被公共 façade 引用、是否存在未登记的 `rekit/` / `rekit/lib/*.ps1` 文件；text output 与 `releaseHandoff.signals[]` 同步展示 `moduleRemoval=true removalCandidates=14 facadeDeps=0 undocumented=0`，为后续独立删除或归档 PowerShell 文件批次提供确定性候选清单。该批只做 Go-owned release inventory、测试和文档，不删除 PowerShell 文件，不新增 PowerShell runtime logic，不改变命令集合、Go output schema 中既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 235 / Stage 8 新增 Go-owned façade runtime dependency release inventory：`release-check -Format json` 的 `powerShellDeprecation` 现在输出 `facadeRuntime` 子库存，机器可读地扫描 `rekit/rekit.ps1` 是否仍包含 legacy runtime module dot-source、legacy command switch fallback dispatcher 代表性调用，以及是否保留 Go delegation、no-fallback guard 与 retired dispatcher error；text output 与 `releaseHandoff.signals[]` 同步展示 `facadeRuntime=true legacyImports=false dispatcher=false`，作为 Batch 234 删除公共 façade legacy dispatcher 后的确定性回归门禁。该批只做 Go-owned release inventory、测试和文档，不删除 `rekit/lib/*.ps1` 文件，不新增 PowerShell runtime logic，不改变命令集合、Go output schema 中既有字段语义、case-local write semantics、sync/promote review-first、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 234 / Stage 8 删除不可达的 PowerShell façade legacy dispatcher：在 Batch 232 已清空 ordinary fallback candidates、Batch 233 已将 legacy import 后移之后，`rekit.ps1` 删除 no-fallback guard 后方已不可达的 `rekit/lib/*.ps1` dot-source block 与 command switch fallback dispatcher；公共 façade 现在只负责参数兼容、Go delegation、no-fallback error 和 retired dispatcher error。release invariant 改为锁定 façade 不再包含 legacy module import 或 legacy command dispatcher，`facade-smoke.ps1` 继续用隔离 fixture 验证 default delegation 与 `REKIT_GO_DISABLE=1` no-fallback 不加载 legacy runtime。本批不删除 `rekit/lib/*.ps1` 文件，不改变命令集合、Go output schema、fallback retirement inventory counts、sync/promote review-first、case-local write semantics、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 233 / Stage 8 收缩 PowerShell façade 默认路径的 legacy module 依赖：`rekit.ps1` 不再在参数解析后立即 dot-source `rekit/lib/*.ps1`，而是在 Go delegation 与 no-fallback guard 之后才加载 legacy runtime modules；默认 Go 委托、fake Go delegation 和 `REKIT_GO_DISABLE=1` no-fallback 路径因此不预加载 retired PowerShell modules。`facade-smoke.ps1` 增加隔离 façade fixture，将 `lib/Manifest.ps1` 替换为 sentinel throw，验证 default delegation 与 disabled no-fallback 都不会触发 legacy import；release invariant 同步锁定 no-fallback guard 早于 legacy dot-source。本批不删除 PowerShell 文件，不改变命令集合、Go output schema、fallback retirement inventory counts、sync/promote review-first、case-local write semantics、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 232 / Stage 8 退休 workstream whole-command PowerShell fallback：`start`、`handoff` 与 `continue` 的 JSON preview、explicit apply、文本 preview 与 bare/default 工作线 flow 均继续默认委托 Go；PowerShell façade 对非 `-Apply` 且空 `-Format` 的 workstream 调用显式传 `-Format text`，保持用户可读输出，同时 direct Go CLI 保持默认 JSON contract。当 `REKIT_GO_DISABLE=1`、Go delegation 不可用或 fake backend 不应被触发时，这三个 command 现在整体输出 “PowerShell fallback has been retired” 并失败，不再进入 legacy workstream fallback。`powerShellDeprecation.fallbackRetirement` inventory 同步变为 `fallbackRetirement=true noFallback=19 candidates=0 removalModules=14`。本批不删除 PowerShell 文件，不改变 start/handoff/continue JSON schema、case-local facts/routing/run digest/lane resume/checkpoint/board 写入语义、Mission Control brief、sync/promote review-first、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 231 / Stage 8 退休 structured workstream PowerShell fallback：`start`、`handoff` 与 `continue` 的 Go-owned JSON preview / explicit apply invocation 继续默认委托 Go；当 `REKIT_GO_DISABLE=1`、Go delegation 不可用或 fake backend 不应被触发时，PowerShell façade 输出 “PowerShell fallback has been retired” 并失败，不再进入 legacy workstream fallback。无 `-Apply` 的文本 preview / bare 文本工作线 flow 仍保留为迁移期 compatibility，因此 `powerShellDeprecation.fallbackRetirement` inventory 仍保持 `fallbackRetirement=true noFallback=16 candidates=2 removalModules=14`。本批不删除 PowerShell 文件，不改变 start/handoff/continue Go output schema、case-local facts/routing/run digest/lane resume/checkpoint/board 写入语义、Mission Control brief、sync/promote review-first、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 230 / Stage 8 退休 case lifecycle PowerShell fallback：`attach`、`repair`、`init` 与 `bootstrap` 的非写入预览和显式 `-Apply` 继续默认委托 Go；当裸 lifecycle 命令、`REKIT_GO_DISABLE=1` 或 Go delegation 不可用时，PowerShell façade 输出 “PowerShell fallback has been retired” 并失败，不再进入 legacy case lifecycle fallback。`powerShellDeprecation.fallbackRetirement` 基线同步变为 `fallbackRetirement=true noFallback=16 candidates=2 removalModules=14`；remaining candidate fallback 继续保留给 start/handoff/continue 与无 `-Apply` 文本工作线等迁移期 compatibility 路径。本批不删除 PowerShell 文件，不改变 case lifecycle Go output schema、metadata/thin shim/managed docs/template/state 写入语义、case-local shim thin boundary、sync/promote review-first、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 229 / Stage 8 退休 `promote` PowerShell fallback：promote review、review artifacts、`promote -CreateCandidates`、`promote -CreateCandidates -WhatIf -Format json`、`promote -Apply` 与 `promote -Apply -WhatIf -Format json` 继续默认委托 Go；当文本 promote what-if、`REKIT_GO_DISABLE=1` 或 Go delegation 不可用时，PowerShell façade 输出 “PowerShell fallback has been retired” 并失败，不再进入 legacy promote fallback。`powerShellDeprecation.fallbackRetirement` 基线同步变为 `fallbackRetirement=true noFallback=12 candidates=3 removalModules=14`；remaining candidate fallback 继续保留给 case lifecycle、start/handoff/continue 与文本工作线等迁移期 compatibility 路径。本批不删除 PowerShell 文件，不改变 promote Go output schema、candidate/index/tooling candidate 写入语义、pack source backup/deny/restore/validation、review-first、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 228 / Stage 8 退休 `sync` / `update` PowerShell fallback：sync/update review、review artifacts、`sync -Apply`、`sync -Apply -WhatIf -Format json` 与 update alias 继续默认委托 Go；当 `REKIT_GO_DISABLE=1` 或 Go delegation 不可用时，PowerShell façade 输出 “PowerShell fallback has been retired” 并失败，不再进入 legacy sync/update fallback。`powerShellDeprecation.fallbackRetirement` 基线同步变为 `fallbackRetirement=true noFallback=11 candidates=4 removalModules=14`；remaining candidate fallback 继续保留给 case lifecycle、promote、start/handoff/continue 与文本工作线等迁移期 compatibility 路径。本批不删除 PowerShell 文件，不改变 sync Go output schema、managed docs 写入语义、backup/deny/restore、review-first、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 227 / Stage 8 退休 `overview` 与 `note` PowerShell fallback：overview 文本/JSON 与缺 board 初始化、`note -List` 文本/table/tsv/JSON、`note` append 与 `note -WhatIf` 继续默认委托 Go；当 `REKIT_GO_DISABLE=1` 或 Go delegation 不可用时，PowerShell façade 输出 “PowerShell fallback has been retired” 并失败，不再进入 legacy overview/note fallback。`powerShellDeprecation.fallbackRetirement` 基线同步变为 `fallbackRetirement=true noFallback=9 candidates=5 removalModules=14`；remaining candidate fallback 继续保留给 case lifecycle、sync/promote、start/handoff/continue 与文本工作线等迁移期 compatibility 路径。本批不删除 PowerShell 文件，不改变 overview/note JSON/text envelope、facts JSONL schema、case state、sync/promote review-first、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 226 / Stage 8 退休 `gate -WhatIf` / `gate -Apply` pending-gate PowerShell fallback：gate 路径继续默认委托 Go，只做 pending-gate request preview 或 append `.rekit/facts/requests.jsonl`，不执行 actual heavy-tool、不写 authority/confirmed；当 `REKIT_GO_DISABLE=1` 或 Go delegation 不可用时，PowerShell façade 输出 “PowerShell fallback has been retired” 并失败，不再进入 legacy gate fallback。`powerShellDeprecation.fallbackRetirement` 基线同步变为 `fallbackRetirement=true noFallback=7 candidates=7 removalModules=14`，remaining candidate fallback 继续保留给 case lifecycle、sync/promote、overview/note/workstream 等迁移期 compatibility 路径。本批不删除 PowerShell 文件，不改变 pending-gate schema、case state、sync/promote review-first、actual heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 225 / Stage 8 退休 `plan-subagents` PowerShell fallback：`plan-subagents` review artifact 写入继续默认委托 Go，只生成 packet / summary / combined diff 路径、不自动 spawn agent、不写 board/facts/lanes/handoff/authority；当 `REKIT_GO_DISABLE=1` 或 Go delegation 不可用时，PowerShell façade 输出 “PowerShell fallback has been retired” 并失败，不再执行 PowerShell review artifact fallback。`powerShellDeprecation.fallbackRetirement` 基线同步变为 `fallbackRetirement=true noFallback=6 candidates=8 removalModules=14`，remaining candidate fallback 继续保留给 case lifecycle、sync/promote、overview/note/gate/workstream 等迁移期 compatibility 路径。本批不删除 PowerShell 文件，不改变 case state、sync/promote review-first、heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 224 / Stage 8 退休低风险 read-only PowerShell fallback：`status`、`packs`、`doctor` 与 `validate` 现在和 `release-check` 一样属于 Go-owned no-fallback 命令；当 `REKIT_GO_DISABLE=1` 或 Go delegation 不可用时，PowerShell façade 对这些命令输出 “PowerShell fallback has been retired” 并失败，不再执行 PowerShell 业务实现。`powerShellDeprecation.fallbackRetirement` 基线同步变为 `fallbackRetirement=true noFallback=5 candidates=9 removalModules=14`，remaining candidate fallback 继续保留给 case lifecycle、sync/promote、overview/note/gate/workstream 与 `plan-subagents` 等迁移期 compatibility 路径。本批不删除 PowerShell 文件，不改变 case state、sync/promote review-first、heavy-tool/authority/confirmed 或外部副作用边界。
- Batch 223 / Stage 8 新增 PowerShell fallback retirement release inventory：`release-check -Format json` 的 `powerShellDeprecation` 现在输出 `fallbackRetirement` 子库存，机器可读地列出 Go-default commands、no-fallback commands、fallback retirement candidate commands、blocked commands 与 removal-candidate PowerShell modules；text output 与 release handoff signal 同步展示 `fallbackRetirement=true noFallback=1 candidates=10 removalModules=14`，为后续按独立 batch 删除或降级 PowerShell fallback 提供确定性前置检查。文档同步说明 no-fallback / candidate / removal module 分类语义。本批只做 Go-owned release inventory、测试和文档，不改变 PowerShell façade 委托集合、不删除 PowerShell 文件、不改变 `REKIT_GO_DISABLE=1` fallback、case state、sync/promote review-first、heavy-tool/authority/confirmed 边界。
- Batch 222 / Stage 8 扩展 public default docs readiness 到支撑文档、runtime migration 与 tests guide：`internal/rekit/defaultdocs` 现在把 `docs/go-runtime-migration.md`、`docs/vision.md`、`docs/reference-absorption.md`、`docs/agent-team-rollout-plan.md` 与 `rekit/tests/README.md` 纳入 `publicDefaultDocs` inventory，并继续拒绝这些支撑文档中的 `rekit.ps1` façade 命令片段和 PowerShell / pwsh shell fence 作为默认路径；vision、reference absorption、Agent Team rollout、Go runtime migration 与 tests guide 的验证说明同步收敛为 Go-native release readiness / doctor / test 路径，legacy façade smoke 只保留为按需 compatibility 验证。本批只做支撑文档收尾、readiness/inventory 扩展和测试补强，不改变 runtime 写入面、PowerShell façade 委托集合、case state、sync/promote review-first、heavy-tool/authority/confirmed 边界，也不删除 PowerShell 文件。
- Batch 221 / Stage 8 扩展 public default docs readiness 到路线与策略文档：`internal/rekit/defaultdocs` 现在把 `docs/mission-control-product-direction.md`、`docs/go-first-convergence-plan.md` 与 `docs/powershell-deprecation.md` 纳入 `publicDefaultDocs` inventory，并继续拒绝这些默认接手/路线文档中的 `rekit.ps1` façade 命令片段和 PowerShell / pwsh shell fence；Go-first convergence plan 的 release readiness 目标门禁同步收敛为 Go-native `release-check`、`status`、`packs`、`doctor`、`go test`、`go vet` 与 `git diff --check`，façade/fallback smoke 只作为按需 compatibility 追加。release readiness、deprecation roadmap、release handoff details、CLI tests 与 release fixture 同步更新。本批只做文档收尾、readiness/inventory 扩展和测试补强，不改变 runtime 写入面、PowerShell façade 委托集合、case state、sync/promote review-first、heavy-tool/authority/confirmed 边界，也不删除 PowerShell 文件。
- Batch 220 / Stage 8 扩展 public default docs readiness 到公开默认 shell fence：`internal/rekit/defaultdocs` 现在把 `docs/release-readiness.md` 纳入 `publicDefaultDocs` inventory，新增 `forbiddenShellFences[]` 诊断并拒绝 README、CLAUDE、canonical `/rekit` skill、autonomous goal 与 release readiness 中的 PowerShell / pwsh code fence 作为默认公开命令展示；README、CLAUDE、autonomous goal 与 release readiness 的默认命令块改为 neutral `text` fence，release readiness 避免展示 `rekit.ps1` façade 命令片段为默认路径。text output 与 release handoff details 同步展示 forbidden shell fence 计数。本批只做公开默认文档 readiness、文档 fence 收敛和测试补强，不改变 runtime 写入面、PowerShell façade 委托集合、case state、sync/promote review-first、heavy-tool/authority/confirmed 边界，也不删除 PowerShell 文件。
- Batch 219 / Stage 8 新增 Go-owned public default docs readiness inventory：新增 `internal/rekit/defaultdocs`，由 Go tests 与 `release-check -Format json` 的 `publicDefaultDocs` 字段共同验证 README、canonical `/rekit` skill、项目 CLAUDE 与 autonomous goal 保持 Mission Control / `/rekit` / Go-native backend 默认公开路径，并拒绝把 `rekit.ps1` 命令片段重新展示为默认用户入口；`releaseHandoff.signals[]` 新增 `public default docs`，text output 展示 `public default docs: ... ready=true`。本批只做 Go readiness/inventory、release-check/handoff 输出、测试 fixture 与路线文档，不改变 runtime 写入面、PowerShell façade 委托集合、case state、sync/promote review-first、heavy-tool/authority/confirmed 边界，也不删除 PowerShell 文件。
- Batch 218 / Stage 8 新增 Go-owned case shim readiness inventory：新增 `internal/rekit/caseshim`，由 Go tests、kit/case `doctor` 与 `release-check -Format json` 的 `caseShim` 字段共同验证 case-local thin shim 保持 metadata 跳转，不包含 PowerShell façade、raw Go CLI 命令或 fallback 开关；`releaseHandoff.signals[]` 新增 `case shim readiness`，text output 展示 `case shim: ... ready=true`。case shim 文档补充 canonical runtime / Go-native backend 语义，并明确 shim 不展示底层脚本或 CLI 命令。本批只做 Go readiness/invariant、doctor/release-check inventory、shim 文档和路线文档，不改变 runtime 写入面、PowerShell façade 委托集合、case state、sync/promote review-first、heavy-tool/authority/confirmed 边界，也不删除 PowerShell 文件。
- Batch 217 / Stage 8 将默认 release gate 收敛到 Go-native / 跨平台路径：`rekit/tests/catalog.json` 的 `recommendedMinimum` 改为 `release-check`、`status`、`packs`、`doctor`、`go test`、`go vet` 与 `git diff --check`；`.github/workflows/release-gate.yml` 改为 Linux、Windows、macOS 三个平台运行同一组 Go-native release checks；`release-check` 的 `ciReleaseGate` inventory 与 release invariants 同步锁定三平台 job、required commands，并禁止默认 workflow 漂移回 `rekit.ps1`、`facade-smoke.ps1`、大型 matrix、真实 case smoke 或 heavy-tool 步骤。PowerShell façade smoke 保留为按需 compatibility 验证，不再属于默认 release gate；本批不改变 runtime 写入面、PowerShell façade 委托集合、pack manifest、case state、heavy-tool/authority/confirmed 边界，也不删除 PowerShell 文件。
- Batch 216 / Stage 8 文档收尾将长期接手 anchor 升级为 PowerShell-free / Go-native / 跨平台 convergence：更新 `docs/autonomous-goal.md` 的新会话接手语句和精简 `/goal`，明确每轮先读 repo docs 校准方向、详细路线/关键决策/验证/下一步必须写回 `docs/batch-plan.md` 或相关设计文档，且不要把单个 batch、提交或工作树干净视为长期 goal 完成；同步升级 `docs/powershell-deprecation.md` 为 PowerShell-free Go-native convergence roadmap，并更新 README、CLAUDE、`/rekit` skill、vision、reference absorption、Go-first convergence 与 release readiness 的入口文案。该批只做接手/路线文档和方向锚点，不改变 runtime 写入面、PowerShell façade 委托集合、release-check schema、CI workflow、pack manifest 或 heavy-tool/authority/confirmed 边界。
- Batch 215 / Stage 5-6 继续收口 shared facts read：`internal/rekit/mission` 新增 `ReadFact(caseRoot, kind)` / `ReadStrictFact(caseRoot, kind)` 与 per-kind `ReadFactEventIDs` / `ReadStrictFactEventIDs`，让 ledger facts snapshot、Go `note -List` strict facts 读取和 `gate -Apply` duplicate eventId scan 复用 shared `FactPath` 与 fact kind reader。补 mission package tests 覆盖 non-strict/strict fact read、unsafe kind 拒绝与 per-kind eventId 聚合。该批只做 Go runtime 内部复用、测试和文档，不改变 note/gate/overview/continue JSON/text envelope、strict/non-strict reader 语义、duplicate eventId skip、facts 文件名、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 214 / Stage 5-6 继续收口 shared facts append：`internal/rekit/mission` 新增 `FactPath(caseRoot, kind)` 与 `AppendFact(caseRoot, kind, value)`，统一 fact kind → `.rekit/facts/*.jsonl` safe path、parent mkdir 与 shared JSONL append；Go `note` append、`gate -Apply` pending-gate request 写入与 workstream continue facts promotion 改为复用该 helper，删除 note/gate/workstream 中 fact path + mkdir + append 的并行实现。补 mission package tests 覆盖 fact path mapping/safety 与 append fact 可读性，并更新 README internal state model。该批只做 Go runtime 内部复用、测试和文档，不改变 note/gate/continue JSON/text envelope、duplicate eventId skip、facts 文件名、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 213 / Stage 5-6 继续收口 shared JSONL append：`internal/rekit/mission` 新增 `AppendJSONLine(path, value)`，统一 JSON marshal、append open flags 与 CRLF JSONL 行写入；Go `note` append、`gate -Apply` pending-gate request ledger 写入、workstream lane event/request routing 与 continue facts promotion 改为复用该 helper，删除 workstream-local append helper 与 note/gate-local append boilerplate。补 mission package test 覆盖创建文件、连续 append、CRLF JSONL 与 strict reader 可读性。该批只做 Go runtime 内部复用、测试和文档，不改变 note/gate/start/continue JSON/text envelope、duplicate eventId skip、facts/lane JSONL 路径、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 212 / Stage 5-6 继续收口 note duplicate eventId ledger scan：`internal/rekit/mission` 在 non-strict `ReadLedgerEventIDs(caseRoot)` 基础上新增 `ReadStrictLedgerEventIDs(caseRoot)`，统一复用 shared `FactFileNames()`、strict facts JSONL reader 与 `Value()` 聚合 ledger eventId；Go `note` append 的 duplicate eventId 检查改为复用 strict helper，删除 note-local full ledger scan。补 mission package test 覆盖 strict malformed JSONL error，补 note package test 覆盖跨 ledger kind duplicate eventId skip。该批只做 Go runtime 内部复用、测试和文档，不改变 note append/list JSON/text envelope、strict malformed JSONL 报错、duplicate eventId skip 语义、facts 文件名、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 211 / Stage 5-6 继续收口 continue duplicate eventId ledger scan：`internal/rekit/mission` 新增 `ReadLedgerEventIDs(caseRoot)`，统一按 shared `FactFileNames()` / `ReadFactFile()` 读取 `.rekit/facts/*.jsonl` 并汇总 eventId；Go `continue -WhatIf/-Apply` 改为复用该 helper，删除 continue-local known event id scanner。补 mission package test 覆盖 shared facts mapping、non-strict malformed JSONL 跳过与 eventId 聚合。该批只做 Go runtime 内部复用、测试和文档，不改变 continue JSON/text envelope、duplicate eventId skip 语义、facts 文件名、run artifacts、routing、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 210 / Stage 5-6 继续收口 workstream continue shared facts promotion：新增 `internal/rekit/workstream/continue_facts.go`，把 continue preview/apply 的 facts would-write 生成与 shared facts JSONL append helper 收到同一 `mission.FactRelPath(kind)` source；`continue` 的 observation/request/candidate/publication/decision 写入和 authority-deferred preview 不再各自拼装 fact path。补 workstream package tests 覆盖 would-write path 与 append helper 实际写入 `.rekit/facts/<kind>.jsonl`。该批只做 Go runtime 内部复用、测试和文档，不改变 continue JSON/text envelope、shared facts 文件名、duplicate eventId、run artifacts、routing、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 209 / Stage 5-6 继续收口 workstream lane/workspace JSONL path helper：`internal/rekit/workstream` 在 Batch 208 的 local JSONL 文件集合基础上新增 `LaneJSONLPaths()`、`WorkspaceJSONLPaths()` 与 lane-local single-path helpers，`start` 的 lane/workspace JSONL 初始化、lane event append、lane resume inbox/tasks 读取，`continue` 的 request routing/tasks duplicate scan，以及 `doctor` 的 lane/workspace JSONL validation 改为复用同一 workstream-local path source。补 workstream package tests 覆盖 lane/workspace path 顺序与 single-path helper。该批只做 Go runtime 内部复用、测试和文档，不改变 start/continue/doctor JSON/text envelope、写入路径结果、run artifacts、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 208 / Stage 5-6 继续收口 workstream local JSONL 文件集合：`internal/rekit/workstream` 新增 `LaneJSONLFileNames()`、`WorkspaceJSONLFileNames()` 与 `LaneOutputJSONLPaths()`，让 `start` 的 lane/workspace JSONL 初始化、`continue` 的 lane output 读取与 input refs、以及 `doctor` 的 lane/workspace JSONL validation 复用同一 workstream-local helper，同时保持这些文件与 shared `.rekit/facts/*.jsonl` ledger helpers 的边界清楚。补 workstream package tests 覆盖 lane/workspace local JSONL 顺序与 lane output path 顺序。该批只做 Go runtime 内部复用、测试和文档，不改变 start/continue/doctor JSON/text envelope、写入路径结果、run artifacts、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 207 / Stage 5-6 继续收口 ledger kind 与 facts path：Go `note` 的 kind 顺序、kind validation、missing-kind error、duplicate eventId scan 改为复用 `mission.LedgerKinds()`，append 路径改为复用 `mission.FactRelPath()`，`doctor` 的 facts JSONL validation 改为遍历 `mission.FactRelPaths()`，移除 note-local ledger kind list 与 doctor-local facts file list。补 note package test 覆盖 no-kind list 使用 shared ledger kinds 顺序。该批只做 Go runtime 内部复用、测试和文档，不改变 note/doctor JSON/text envelope、facts 文件名、写入路径结果、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 206 / Stage 5-6 继续收口 case doctor JSONL validation：`internal/rekit/mission` 新增 shared `ValidateJSONLines` 与内部 `scanJSONLines`，`doctor` 的 facts、lane inbox/tasks/outbox 与 workspace JSONL 校验改为复用 shared validator，移除 doctor-local scanner/unmarshal 逻辑，同时保持 missing file no-op、BOM trim 与 malformed line 诊断语义。补 mission tests 覆盖 shared JSONL validation 的 missing file、BOM 和行号诊断。该批只做 Go runtime 内部复用、测试和文档，不改变 doctor/mission JSON envelope、case 写入语义、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 205 / Stage 5-6 继续收口 Go runtime JSONL reader：`gate` duplicate eventId 检查改为复用 `mission.ReadJSONLineObjects` / `mission.Value`，移除 gate-local scanner/unmarshal 逻辑；`workstream` start/continue 直接调用 shared JSONL reader，删除 package-local passthrough wrapper，减少 facts、lane inbox/tasks 与 workspace outbox 读取路径的漂移点。补 gate package test 覆盖 malformed JSONL 行仍按 shared non-strict reader 跳过且不重复 append。该批只做 Go runtime 内部复用、测试和文档，不改变 gate/start/continue JSON envelope、写入路径、run artifacts、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 204 / Stage 5-6 继续收口 workstream handoff facts snapshot：`internal/rekit/workstream` 的 handoff/continue Mission Control brief 与 handoff Markdown 渲染改为复用 `mission.ReadLedgerFacts` / `mission.LedgerFacts`，删除 handoff-local facts struct 与 adapter，确保 handoff、continue 与 overview 共享同一 ledger snapshot reader。补 workstream package test 覆盖 handoff facts 读取 full ledger snapshot、pending decision 与 batch aggregation。该批只做 Go runtime 内部复用、测试和文档，不改变 handoff/continue JSON envelope、Markdown 输出语义、run artifacts、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 203 / Stage 5-6 继续收口 workstream facts 文件映射：`internal/rekit/mission` 新增 shared `LedgerKinds`、`FactFileNames`、`FactRelPath` 与 `FactRelPaths`，`start` 初始化 facts JSONL、`handoff` facts 读取、`continue` known event id 扫描以及 continue preview/apply 的 shared facts 写入路径复用同一 ledger kind → facts path helper，降低 facts 文件名列表漂移风险。补 mission tests 覆盖 shared file list 与 rel path。该批只做 Go runtime 内部复用、测试和文档，不改变 start/continue/handoff JSON envelope、写入路径结果、run artifacts、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 202 / Stage 5-6 继续收口 workstream board snapshot 读取：`workstream` 的 board 类型改为 `mission.Board` alias，`readBoard` 改为直接复用 `mission.ReadBoard`，`continue` 的 open lane 选择改为复用 `mission.OpenBoardLanes`，移除 workstream-local board struct、board JSON parser 与 open-lane filter 复制逻辑。补 mission tests 覆盖 shared open-board-lane status 规则。该批只做 Go runtime 内部复用、测试和文档，不改变 start/continue/handoff JSON envelope、run artifacts、board refresh 写入语义、PowerShell façade 委托集合或 heavy-tool/authority/confirmed 边界。
- Batch 201 / Stage 5-6 将 note/gate 的 board lane guard 收口到 `internal/rekit/mission`：新增 shared `AssertBoardLane`、`BoardLaneIDs` 与 `BoardHasLane` helper，保留 note 的大小写敏感 lane guard 与 gate 的大小写不敏感 lane guard，同时移除 note/gate 各自维护的 known-lane 列表和 missing/empty board 错误拼装逻辑。补 mission tests 覆盖 missing board、empty board、case-sensitive/case-insensitive lane lookup 与 known lane 诊断。该批只做 Go runtime 内部复用、测试和文档，不改变 note/gate JSON envelope 或写入语义、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不自动 spawn agent、不写 authority/confirmed、不改变 sync/promote review-first 语义。
- Batch 200 / Stage 5-6 继续收口 Go `note` 的 ledger 与 board 读取：`internal/rekit/mission` 新增 shared `FactFileName` ledger kind → facts JSONL 映射，`note -List`、duplicate `eventId` 检查与 note lane guard 复用 `mission.ReadStrictFactFile` / `ReadBoard`，移除 note-local JSONL reader、board struct 与 fact-file switch 复制逻辑。补 note/mission tests 覆盖 shared fact-file mapping、strict JSONL error 与 note list 映射读取。该批只做 Go runtime 内部复用、测试和文档，不改变 note append/list JSON envelope 或写入语义、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不自动 spawn agent、不写 authority/confirmed、不改变 sync/promote review-first 语义。
- Batch 199 / Stage 5-6 继续收口 overview 的 Mission Control snapshot 读取：`internal/rekit/mission` 新增 strict ledger facts reader、`LedgerFacts`、pending decision 计数与 batch event 聚合 helper，Go `overview` 复用 `mission.ReadBoard` / `ReadStrictLedgerFacts` / `BoardLaneLabel` 构建文本与 JSON inventory，移除 overview-local board JSON、facts JSONL、pending decision、batch event 与 lane label 复制逻辑。补 mission tests 覆盖 strict JSONL、pending decision 与 batch aggregation。该批只做 Go runtime 内部复用、测试和文档，不改变 overview JSON/text 字段、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不自动 spawn agent、不写 authority/confirmed、不改变 sync/promote review-first 语义。
- Batch 198 / Stage 5-6 将 case-local Mission Control snapshot 读取逻辑抽入 `internal/rekit/mission`：新增 shared board/facts JSONL reader、`CaseBrief`、board lane adapter 与 JSONL reader tests，并让 `start`、`handoff` facts loading 和 `gate` preview/apply 复用同一 snapshot helper，移除 gate-local board/facts/JSONL/label 复制逻辑，降低后续 `missionBrief` envelope 在 pending-gate、open intervention、open candidate/decision blocker 语义上的漂移风险。该批只做 Go runtime 内部复用、测试和文档，不改变 PowerShell façade 委托集合、不执行 heavy-tool、不自动 spawn agent、不写 authority/confirmed、不改变 sync/promote review-first 语义。
- Batch 197 / Stage 5-6 将 Go `gate` preview/apply JSON envelope 扩展为结构化 Mission Control brief：`gate -WhatIf -Format json` 输出当前 pre-apply `missionBrief`，`gate -Apply -Format json` 在 pending-gate request ledger 写入或重复 eventId 识别后输出 post-apply `missionBrief`，自动化可直接看到 gate request 是否让对应 lane blocked 以及 next action/escalation。补 gate package/CLI tests 覆盖 what-if no-write brief、apply 后 pending-gate blocker brief 和 no-heavy-tool/no-authority 边界。该批只增强 case-local gate JSON 可消费性，不改变 PowerShell façade 委托集合、不执行 heavy-tool、不自动 spawn agent、不写 authority/confirmed、不改变 sync/promote review-first 语义。
- Batch 196 / Stage 5-6 将 Go `start` JSON envelope 扩展为结构化 Mission Control brief：`start -WhatIf/-Apply -Format json` 输出现在包含 `missionBrief`，preview 暴露当前 pre-apply Mission Control 状态，显式 apply 在 lane/board refresh 后暴露 post-apply ready/blocked lanes、pending gates、open decisions、interventions、next agent actions 与 escalations 摘要，让维护自动化创建或进入 lane 后可直接判断 Mission Control 状态。补 start CLI tests 覆盖 preview no-write brief 与 apply 后新 lane ready brief。该批只增强 case-local start JSON 可消费性，不改变 PowerShell façade 委托集合、不执行 heavy-tool、不自动 spawn agent、不写 authority/confirmed、不改变 sync/promote review-first 语义。
- Batch 195 / Stage 5-6 将 Go `continue` JSON envelope 与 run artifacts 扩展为结构化 Mission Control brief：`continue -WhatIf/-Apply -Format json` 输出现在包含 `missionBrief`，显式 apply 后 `.rekit/runs/<run-id>/status.json` 与 `digest.md` 也记录同一 ready/blocked lanes、pending gates、open decisions、interventions、next agent actions 与 escalations 摘要，lane executor 继续后可直接判断 Mission Control 状态而不必另跑 overview/handoff 或解析 Markdown。补 continue CLI tests 覆盖 preview no-write、apply 后 open candidate blocker、run status 与 digest brief。该批只增强 case-local continue 可消费性，不改变 PowerShell façade 委托集合、不执行 heavy-tool、不自动 spawn agent、不写 authority/confirmed、不改变 sync/promote review-first 语义。
- Batch 194 / Stage 5-6 将 Go `handoff` JSON envelope 扩展为结构化 Mission Control brief：项目级与指定 lane 的 `handoff -WhatIf/-Apply -Format json` 输出现在包含 `missionBrief`，与 Markdown handoff 和 overview 共用同一 ready/blocked lanes、pending gates、open decisions、interventions、next agent actions 与 escalations 语义，自动化可不解析 Markdown 就判断接手状态。补 handoff CLI tests 覆盖 preview/apply、project/lane 和 decision-only blocker 的结构化 brief。该批只增强 case-local handoff JSON 可消费性，不改变 PowerShell façade 委托集合、不执行 heavy-tool、不自动 spawn agent、不写 authority/confirmed、不改变 sync/promote review-first 语义。
- Batch 193 / Stage 5-6 将 Mission Control brief 聚合逻辑抽入新的 `internal/rekit/mission` package：overview、project handoff 与 lane handoff 现在复用同一 blocker、line item、next action 与 escalation helper，lane/project handoff 只保留展示层包装与 lane-local 文案选项。补 mission package tests 锁定 pending-gate/open intervention/open candidate/open decision blocker 语义、deferred decision terminal 处理和 lane-local line 输出。该批只降低 Go Mission Control brief 漂移风险，不改变 PowerShell façade 委托集合、不执行 heavy-tool、不自动 spawn agent、不写 authority/confirmed、不改变 sync/promote review-first 语义。
- Batch 192 / Stage 5-6 将 Mission Control brief 扩展到 Go 项目级 handoff：`/rekit handoff -Apply` 写出的 `.rekit/handovers/latest.md` 现在在工作线列表前汇总 ready/blocked lanes、pending gates、open decisions、interventions、next agent actions 与 escalations，使用与 overview / lane handoff 一致的 blocker 语义。补 project handoff CLI tests、README、agent-team usage、Go-first convergence 与 `/rekit` skill 说明。该批只增强 case-local handoff 接手索引，不执行 heavy-tool、不自动 spawn agent、不写 authority/confirmed、不改变 PowerShell façade 委托集合。
- Batch 191 / Stage 5-6 对齐 Mission Control brief blocker 语义：Go lane handoff 现在读取本 lane open candidates 与 open decisions，并与 overview 一样把 pending gate、open intervention、open candidate/decision 都视为 blocked lane 条件；lane handoff 的 `## Mission Control brief` 会在 decision-only 或 candidate-only 场景显示 `blocked: true` 与对应 next agent action。补 CLI decision-only/candidate-only parity tests、README、agent-team usage、Go-first convergence 与 `/rekit` skill 说明。该批只修正 case-local handoff/overview 可见性一致性，不执行 heavy-tool、不自动 spawn agent、不写 authority/confirmed、不改变 PowerShell façade 委托集合。
- Batch 190 / Stage 8 将 `plan-subagents` review artifacts 纳入默认 Go façade 委托：`rekit/rekit.ps1` 现在对 safe `plan-subagents` 调用默认转发到 Go backend，并透传 route/task/items/items-file/shard/review artifact 参数；`REKIT_GO_DISABLE=1` 继续回退 PowerShell fallback。补 façade smoke、plan-subagents smoke、release invariant、PowerShell deprecation inventory 与相关文档。该批只收缩 PowerShell 语义面并生成 review packet/summary/diff artifact，不自动 spawn agent、不写 board/facts/lanes/handoff/authority/confirmed、不执行 heavy-tool。
- Batch 189 / Stage 5-6 新增 Go-owned Mission Control brief：`/rekit overview` 文本与 JSON envelope 现在输出 `missionBrief`，汇总 ready/blocked lanes、pending gates、open decisions、interventions、next agent actions 与 escalations；lane handoff 也新增 `## Mission Control brief`，让新会话先看到当前 lane 是否被 gate/intervention/decision 阻塞以及下一步动作。补 CLI tests、README、agent-team usage 与 `/rekit` skill 说明。该批只做 case-local overview/handoff summarization、测试和文档，不执行 heavy-tool、不写 authority/confirmed、不改变 PowerShell façade 委托集合。
- Batch 188 固化 Lane-centric Agent Team Mission Control 最终产品方向：新增 `docs/mission-control-product-direction.md`，明确主 Agent / Mission Commander 统筹 durable member lanes、可替换 Claude Code session executors、短命 tactical subagents、Human-in-the-Lane reconcile、预授权 lane autonomy 与 pack-based team memory；同步 README、CLAUDE、vision/design、agent-team usage/rollout、Go-first、release readiness、reference absorption、common policies、vmp-re toolchain-router 和 `/rekit` skill；重写 `docs/autonomous-goal.md`，提供新会话接手语句与中大型 vertical slice goal，并将 Mission Control 文档纳入 `release-check` required documents、release handoff read-first inventory 和 release invariants。该批只固化已确认产品方向、接手 goal、文档与测试 invariant，不实现真实多会话调度、不执行 heavy-tool、不写 authority/confirmed、不改变 PowerShell façade 委托集合。
- Batch 187 / Stage 7 收紧 Go gate risk override scalar contract：`gate -WhatIf/-Apply` 现在会校验用户传入的 `-Risk` override，要求为受支持的小写 risk scalar（`medium`、`high`、`critical`），拒绝 `High`/`low` 等大小写漂移或不支持值；gate fallback 路径也会复核 manifest `defaultRisk`，确保 pending-gate preview/request ledger 不写入不可消费 risk。补 gate/CLI tests、façade smoke invalid risk probe、rekit skill、common tool-adapter policy、evidence ledger、template toolchain-router、验证记录与 Go-first convergence 记录。该批只做 gate preview/request contract、测试和文档，不执行 heavy-tool、不写 authority/confirmed、不改变 PowerShell façade 委托集合。
- Batch 186 / Stage 7 收紧 Go gate stop-condition override token contract：`gate -WhatIf/-Apply` 现在会校验用户传入的 `-StopConditions` override，要求每项为小写 slug/snake token，拒绝空项、重复项和带空格的人类短语，确保 pending-gate preview/request ledger 中的止损条件与 manifest 默认值保持同一机器可消费 contract。补 gate package tests、façade smoke invalid override probe、rekit skill 说明、验证记录与 Go-first convergence 记录。该批只做 gate preview/request contract、测试和文档，不执行 heavy-tool、不写 authority/confirmed、不改变 PowerShell façade 委托集合。
- Batch 185 / Stage 7 显式化 manifest heavy-tool gate stop-condition token contract：Go manifest `ValidateSchema` 现在要求 `heavyToolGates.stopConditions` 的默认止损条件使用机器可消费的小写 slug/snake token，拒绝带空格、人类短语式或其它非法 stop condition；现有 pack manifest 已从短语迁移为 token。补 manifest tests、pack authoring 指南、说明性 schema、Go-first convergence 记录与项目级 verify skill 运行面说明。该批只做 pack-neutral manifest contract、manifest token 迁移、验证说明、测试和文档，不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 184 / Stage 7 显式化 manifest heavy-tool gate scalar contract：Go manifest loader 不再把 `heavyToolGates.id` 与 `defaultRisk` 静默转小写，`ValidateSchema` 现在要求 gate id 和 risk 使用明确的小写可消费值，避免大小写漂移被 loader 归一化后伪装成 schema-valid gate。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 183 / Stage 7 显式化 manifest subagent route id slug contract：Go manifest `ValidateSchema` 现在要求 `subagentRoutes.id` 的 namespace 精确匹配当前 pack id，且 route 名为小写 slug，避免大小写漂移或不可消费 route id 进入 inventory / review packet。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 182 / Stage 7 显式化 manifest subagent route list duplicate contract：Go manifest `ValidateSchema` 现在要求 `subagentRoutes.taskTypes`、`mainAgentOwns` 与 `outputContract` 的逗号/分号列表项在非空且合法之外还必须去重，避免 route contract 携带重复 token 进入 plan-subagents / review packet。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 181 / Stage 7 显式化 manifest scalar/map field contract：Go manifest `ValidateSchema` 现在要求 `name` 使用稳定 machine id、`version` 使用 semver-like 值、`managedBlock` 仅包含 file/blockId/source 且 blockId 为 namespaced id；`syncPolicy` 与 `workstreamDefaults` 拒绝未支持 key，`budgets` 的非 defaultMarkdown key 必须是相对安全路径。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 180 / Stage 7 显式化 manifest managed/template/local/promote file list contract：Go manifest `ValidateSchema` 现在要求 `managedFiles`、`templateFiles`、`localNeverOverwrite` 与 `promoteFiles` 的路径项非空、相对安全且不重复；`templateFiles` 必须以 `.template.md` 结尾，`localNeverOverwrite` 继续不能与 managed/template 源重叠，`promoteFiles` 继续必须是 managedFiles 子集。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 179 / Stage 7 显式化 manifest source/authority/deny list contract：Go manifest `ValidateSchema` 现在要求 `toolingCandidateSources` 与 `authorityFiles` 的路径项非空、可定位且不重复，并要求 `promoteDenyPatterns` 非空、可编译且不重复，避免 candidate source、authority allowlist 或 promote deny baseline 虽显式声明但携带重复/不可消费项。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 178 / Stage 7 显式化 manifest lane type list contract：Go manifest `ValidateSchema` 现在要求 `laneTypes.id` 使用小写 slug，并对 `canWrite`、`readOnly` 与 `outputs` 列表项进行空项 / 重复项校验，同时要求 `outputs` 使用机器可消费的小写 slug/snake token，避免工作线边界显式声明但携带不可消费或重复列表项。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 177 / Stage 7 显式化 manifest heavy-tool gate list contract：Go manifest `ValidateSchema` 现在要求 `heavyToolGates.sideEffects` 与 `stopConditions` 的列表项保留并拒绝空项，且按大小写无关方式拒绝重复项；`sideEffects` 继续要求小写 action/effect slug 并必须包含 gate action id，避免重型工具门禁虽显式声明但携带不可消费或重复的 gate metadata。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 176 / Stage 7 显式化 manifest policy/tooling/prompt list contract：Go manifest `ValidateSchema` 现在校验 `commonPolicies` 的小写 slug / 去重约束，并要求 `policyOverlays`、`toolingFiles` 与 `promptFiles` 的非空项分别落在受支持的 pack/repo-relative 路径范围内，避免可选列表虽然显式存在却携带不可消费 token/path。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 175 / Stage 7 显式化 manifest subagent route numeric bounds contract：Go manifest `ValidateSchema` 现在要求 `subagentRoutes.targetItemsPerAgent` 保持在 1-64、`maxParallel` 保持在 1-16，避免极端分片大小或并发上限进入 schema-valid route contract 后放大 review packet / plan-subagents 调度风险。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 174 / Stage 7 显式化 manifest subagent route list token contract：Go manifest `ValidateSchema` 现在要求 `subagentRoutes.taskTypes`、`mainAgentOwns` 与 `outputContract` 的逗号/分号分隔 token 使用机器可消费的小写 slug/snake token，拒绝空项之外的空格、大写或非法字符，避免 route contract 以不可匹配或不可序列化的列表 token 进入 schema-valid inventory。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 173 / Stage 7 显式化 manifest subagent route shard contract：Go manifest `ValidateSchema` 现在要求每个 `subagentRoutes.shardBasis` 使用机器可消费的小写 slug 或 `-or-` 分隔组合，拒绝空分片项、尾随 `-or-` 或大写/非法字符，避免 plan-subagents shard policy 以不可解析维度进入 schema-valid route contract。补 manifest test、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 172 / Stage 7 显式化 manifest subagent route policy overlay contract：Go manifest `ValidateSchema` 现在要求非空 `subagentRoutes.policyOverlay` 必须来自当前 manifest 显式声明的 `policyOverlays` 列表，避免 route 引用未登记 pack overlay 或复制残留文件后仍进入 schema-valid route contract。补 manifest test、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 171 / Stage 7 显式化 manifest subagent route permissions contract：Go manifest `ValidateSchema` 现在要求每个 `subagentRoutes.subagentPermissions` 使用当前 runtime 支持的显式权限值（`read-only` 或 `read-only-or-workspace-only`），避免未知权限字符串进入 schema-valid route contract 后被 plan-subagents 或 reviewer packet 误解。补 manifest test、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 170 / Stage 7 显式化 manifest subagent route namespace ownership：Go manifest `ValidateSchema` 现在要求每个 `subagentRoutes.id` 的 namespace 与当前 pack id 匹配，避免复制 `_template` 或其它 pack route id 后仍以 schema-valid route 进入 inventory、review packet 或 plan-subagents 输出。补 manifest test、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 169 / Stage 7 显式化 manifest subagent route list-field contract：Go manifest `ValidateSchema` 现在检查 `subagentRoutes.taskTypes`、`mainAgentOwns` 与 `outputContract` 逗号/分号分隔字段，拒绝空 token、尾随分隔符或仅分隔符内容，避免 route contract 以不可消费的列表字段进入 schema-valid inventory。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 168 / Stage 7 显式化 manifest subagent route id contract：Go manifest `ValidateSchema` 现在要求每个 `subagentRoutes.id` 使用 namespaced id（例如 `<pack>:<route>`），避免新增 pack 以无命名空间 route id 进入 schema-valid inventory，导致跨 pack route、review packet 或 plan-subagents 输出难以追踪来源。补 manifest test、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 167 / Stage 7 显式化 manifest subagent route trigger contract：Go manifest `ValidateSchema` 现在要求每个 `subagentRoutes` entry 显式声明非空 `trigger`，避免 route 只有 taskTypes/shardBasis 而缺少触发条件说明仍被视为 schema-valid。补 manifest test、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 166 / Stage 7 显式化 manifest optional route/tooling list presence：Go manifest parser 将 `commonPolicies`、`policyOverlays`、`subagentRoutes`、`toolingFiles` 与 `promptFiles` 纳入统一 list presence contract，要求 schema-valid pack 即使对可选 route/policy/tooling/prompt 列表使用空列表也必须显式声明，避免新增 pack 的 doctor、route inventory 或 prompt/tooling 校验边界被 loader 空 slice 隐式吞掉。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 165 / Stage 7 显式化 manifest required list presence inventory：Go manifest parser 继续扩展 required list presence 记录，将 `promoteFiles`、`toolingCandidateSources`、`authorityFiles`、`promoteDenyPatterns`、`heavyToolGates` 与 `laneTypes` 纳入显式 key 检查，先区分缺 key 与显式空列表，再对必须非空的列表给出非空诊断，避免新增 pack 把缺失 list 与有意空 list 混淆。补 manifest tests、pack authoring 指南、说明性 schema 与 Go-first convergence 记录。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 164 / Stage 7 显式化 manifest runtime maps：Go manifest parser 将 `workstreamDefaults` 与 `budgets` 纳入显式 map presence 记录，并由 `ValidateSchema` 要求 schema-valid pack 显式声明这两个 runtime map；缺失 `workstreamDefaults` 或 `budgets` 不再只表现为某个子 key 空值，避免新增 pack 的工作线路由、backup root 或预算边界被隐式空 map/运行时安全 fallback 掩盖。补 pack authoring、说明性 schema 与 load/no-fallback drift test。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 163 / Stage 7 显式化 manifest syncPolicy：Go manifest parser 记录 `syncPolicy` map 是否显式声明，并由 `ValidateSchema` 要求 schema-valid pack 写出 managedFiles/templateFiles/localFiles 三项当前 runtime 支持的策略值；缺失 map、缺失 key 或未支持值分别给出明确诊断，避免新增 pack 的 sync 写入语义被隐式空 map 或泛化 unsupported-value 错误掩盖。补 schema、pack authoring 指南和 load/no-fallback drift test。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 162 / Stage 7 暴露 manifest schemaVersion inventory：`manifest.PackSummary`、`/rekit packs` table/JSON、`/rekit status -Format json` manifest summary、`release-check` `packs[]` 与 `releaseHandoff.packMaturity` 现在输出 manifest `schemaVersion`，并将 `schemaVersionReady` 纳入 pack maturity release handoff signal，避免 release gate 只能看到 schema valid 而无法机器审计 manifest schema contract 版本 readiness。该批只做 pack-neutral inventory、release handoff gate、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 161 / Stage 7 显式化 manifest schemaVersion：Go manifest parser 保留 `schemaVersion` 原始字段并由 `ValidateSchema` 要求 schema-valid pack 显式声明 `schemaVersion: 1`，不再让缺失或未支持版本通过 schema validation；补 schema、pack authoring 指南和 load/no-fallback drift test，避免新增 pack 的 manifest contract 版本边界被隐式接受。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 160 / Stage 7 显式化 manifest required list presence：Go manifest parser 记录 `managedFiles`、`templateFiles` 与 `localNeverOverwrite` 是否在 manifest 中显式声明，并由 `ValidateSchema` 要求 schema-valid pack 写出这三个 required list key；允许空列表但不再让缺失 key 通过 loader 阶段默认空 slice 或 managedFiles 非空检查混淆 schema 诊断。补 schema、pack authoring 指南、manifest/promote package fixtures 与 load/no-fallback drift test，避免新增 pack 的 sync/promote/local 边界被隐式推断。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 159 / Stage 7 显式化 manifest heavyToolGates requiresConfirmation 字段：Go manifest parser 保留原始 `heavyToolGates.requiresConfirmation` 文本并由 `ValidateSchema` 要求每个 heavy-tool gate 显式声明 `true`，不再把缺失或非法 confirmation gate 静默解析为 false；补 schema、pack authoring 指南和 drift test，避免新增 pack 的 heavy-tool 确认门禁被隐式降级。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 158 / Stage 7 显式化 manifest lane authority 字段：Go manifest parser 保留原始 `laneTypes.authority` 文本并由 `ValidateSchema` 要求每个 lane 显式声明 `true` 或 `false`，不再把缺失或非法 authority 静默解析为 false；补 schema、pack authoring 指南和 drift test，避免新增 pack 的工作线 authority 边界被隐式降级。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 157 / Stage 7 显式化 manifest description 字段：`ValidateSchema` 要求 pack manifest 显式声明一行用途摘要，并将 `description` 加入说明性 schema required 列表；补 pack authoring 指南和 drift test，避免新增 pack 以空描述进入 schema-valid inventory。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 156 / Stage 7 显式化 manifest identity 字段：移除 Go manifest loader 对缺失 `name` 与 `version` 的隐式默认值注入，改为 `ValidateSchema` 要求 pack manifest 显式声明 pack 名称与版本；更新 schema、pack authoring 文档和 drift test，避免新增 pack 静默继承 pack id 或 `0.0.0` 版本。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 155 / Stage 7 显式化 manifest managedBlock 三要素：移除 Go manifest loader 对缺失 `managedBlock.file`、`blockId` 与 `source` 的隐式默认值注入，要求 pack manifest 显式声明 managed block host、block id 与 source；补 sync/init/apply schema validation、schema 文档、pack authoring 指南和 drift test，避免新增 pack 静默继承 runtime 的 `CLAUDE.local.md` / `rekit:router` / `CLAUDE.local.snippet.md` 默认值。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 154 / Stage 7 显式化 manifest laneTypes 工作线边界：移除 Go manifest parser 对缺失 `laneTypes.title` 与 `workspaceRoot` 的隐式默认值，改为 `ValidateSchema` 要求 pack 显式声明 lane title、workspaceRoot、canWrite、readOnly 与 outputs；更新 schema、pack authoring 文档和 drift test，避免新增 pack 静默继承 runtime 的 lane 展示名或 workspace 默认路径。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 153 / Stage 7 显式化 manifest budgets 默认预算：移除 Go manifest loader 对缺失 `budgets.defaultMarkdown` 的 `16384` 隐式注入，改为 `ValidateSchema` 要求 pack 显式声明 Markdown/text 默认预算正整数；保留 `BudgetLimit` 的运行时安全 fallback 只用于调用方容错，并补 schema、pack authoring 文档和 drift test，避免新增 pack 通过 runtime 隐式继承默认预算。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 152 / Stage 7 显式化 manifest promoteFiles 回流范围：移除 Go manifest loader 对空 `promoteFiles` 的 `managedFiles` 隐式 fallback，改为 `ValidateSchema` 要求 pack 显式声明允许 case → kit 回流的 managed 文件子集；更新 schema、pack authoring 文档和 drift test，避免新增 pack 静默把所有 managed files 纳入 promote 回流面。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 151 / Stage 7 显式化 manifest promote deny baseline：移除 Go manifest loader 对空 `promoteDenyPatterns` 的隐式 fallback，改为 `ValidateSchema` 要求 pack 显式声明 promote deny baseline；更新 schema、pack authoring 示例和测试，避免新增 pack 通过 runtime 隐式继承 RE/VMP 倾向的 artifact/trace/dump deny baseline。该批只做 pack-neutral manifest contract、测试和文档，不改变既有 pack manifest 内容、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 150 / Stage 7 收口 Go runtime default pack 决策点：新增 `internal/rekit/defaults.DefaultPack`，将 Go CLI parse、runtime context、manifest load/normalize、legacy instance metadata fallback 的默认 pack 从散落的 `vmp-re` literal 迁到单一 package；测试改用该默认常量，并新增 release invariant 扫描 `internal/rekit/**`，防止 production Go runtime 重新散落 default pack literal。该批只做 pack-neutral hardening、测试和文档，不改变默认 pack 值、不改变 PowerShell façade 委托集合、不执行 heavy-tool、不写 case/pack runtime state、不写 authority/confirmed。
- Batch 149 / Stage 8 新增 release handoff pack maturity summary：`releaseHandoff` 增加 `packMaturity`，汇总 pack 总数、maturity counts、按 maturity 分组的 pack id、schema validity、manifest heavy-tool gate readiness 与每 pack gate action；新增 `pack maturity summary` readiness signal，text 输出展示 `packMaturity=<n>`，release invariant 锁定 `packMaturity` 文档锚点，fixture 覆盖 pack 缺 heavy-tool gate 时 inventory 标记 warning。该批只做确定性 release inventory、测试和文档，不执行 CI、不新增外部副作用、不扩大 PowerShell façade、不执行 heavy-tool、不写 authority/confirmed。
- Batch 148 / Stage 8 新增 release handoff known gaps inventory：`releaseHandoff` 增加 `knownGaps[]`，把 `docs/release-readiness.md` `Known gaps` 转为机器可读 index/category/summary，并新增 `known gaps summary` readiness signal；text 输出展示 `knownGaps=<n>`，release invariant 锁定 `knownGaps[]` 文档锚点，fixture 覆盖缺失 known gaps 时 `release-check.ready=false`。该批只做确定性 release inventory、测试和文档，不执行 CI、不新增外部副作用、不扩大 PowerShell façade、不执行 heavy-tool、不写 authority/confirmed。
- Batch 147 / Stage 8 新增 release notes freshness gate：`releaseHandoff` 增加 `releaseNotes` freshness inventory，对照 `docs/batch-plan.md` 最新 Batch id 与 `CHANGELOG.md` `Unreleased` 段，发现最新批次缺 release note 时让 `release-check.ready=false` 并输出诊断；text 输出展示 `releaseNotes=true/false`，并补 stale notes 单元测试。该批只做确定性 release inventory、测试和文档，不执行 CI、不新增外部副作用、不扩大 PowerShell façade、不执行 heavy-tool、不写 authority/confirmed。
- Batch 146 / Stage 8 新增 Go-owned release handoff summary：`release-check -Format json` 新增 `releaseHandoff`，汇总新会话 read-first 文档、关键 readiness signals、latest batch 摘要、最小验证命令和 next actions，text 输出展示 handoff summary，并用 CLI tests 与 release readiness 文档锁定接手质量。该批只做确定性 release inventory、测试和文档，不执行 CI、不新增外部副作用、不扩大 PowerShell façade、不执行 heavy-tool、不写 authority/confirmed。
- Batch 145 / Stage 8 收紧 `release-check` failure semantics：当 release inventory `ready=false` 时，Go CLI 会先输出完整 JSON/text 诊断再返回非零错误，使本机/CI 不会在发现 release gate、pack schema、PowerShell deprecation 或 CI workflow 漂移时误判通过；同时补 CLI 单元测试和 release readiness 文档。该批只改变 release-check 退出语义，不执行测试编排、不新增外部副作用、不扩大 PowerShell façade、不执行 heavy-tool、不写 authority/confirmed。
- Batch 144 / Stage 8 新增 CI release gate inventory：`release-check -Format json` 新增 `ciReleaseGate`，解析 `.github/workflows/release-gate.yml` 的 workflow checks、required jobs、required commands 与 forbidden broad/heavy steps，text 输出展示 CI release gate summary，并在 Go tests / release invariant / release readiness 文档中锁定轻量 CI 不漂移到大型 PowerShell matrix、真实临时 case smoke 或 heavy-tool。该批只做确定性 CI inventory 与文档/测试，不执行 CI、不新增外部副作用、不扩大 PowerShell façade、不执行 heavy-tool、不写 authority/confirmed。
- Batch 143 / Stage 7-8 新增 manifest-driven heavy-tool gate readiness：所有 pack manifest 显式声明 `heavyToolGates`（debug、dump、full-trace、inject、network、patch、symex），Go manifest schema/summary、`/rekit packs` 与 `release-check -Format json` 输出 gate action inventory；Go `gate -WhatIf/-Apply` 改为只接受 pack manifest 声明的 action，并用 manifest `defaultRisk` / `stopConditions` 生成 preview 或 pending-gate request。该批只做可审计 gate contract、preview/ledger 写入和 release inventory，不执行 heavy-tool、不扩大 PowerShell façade、不写 authority/confirmed。
- Batch 142 / Stage 8 增强 Go-owned `release-check` 的 PowerShell deprecation inventory：`release-check -Format json` 新增 `powerShellDeprecation`，从 `docs/powershell-deprecation.md` 解析命令归属矩阵、`rekit/lib/*.ps1` 模块状态、freeze gates 与 blocked migrations，并对照 `rekit/rekit.ps1` 默认 Go façade 委托集合和实际 PowerShell 模块清单发现漂移；text 输出展示 deprecation summary，该批只做确定性 inventory，不删除 PowerShell、不改变 façade 写入面、不执行 smoke 编排或 heavy-tool。
- Batch 141 / Stage 8 新增长期自主 goal 与新会话接手指南：新增 `docs/autonomous-goal.md`，并按维护者反馈精简为大方向、自主推进循环和可复制 goal 语句；在 README、CLAUDE、release readiness、vision、reference absorption 与 Go-first convergence 入口中接入，并纳入 `release-check` required documents，防止上下文压缩后偏回微批次或 PowerShell smoke/catalog 扩张，同时避免用过长约束束缚模型发挥。
- Batch 140 / Stage 8 新增轻量 CI release gate：新增 `.github/workflows/release-gate.yml`，在 Ubuntu 上运行 `release-check -Format json`、`go test ./...` 与 `go vet ./...`，在 Windows 上运行 `release-check -Format json`、`/rekit doctor` 与 `facade-smoke.ps1`，并用 release invariant 锁定 workflow 不默认运行大型 pack matrix、真实 case smoke 或 heavy-tool 相关步骤。
- Batch 139 / Stage 8 增强 Go-owned `release-check` gate profile：`release-check -Format json` 新增 `gateProfile`，把 catalog `recommendedMinimum` 解析为本机/CI 可消费的 step kind、repo-local path、present/resolved 状态，并在 text 输出中展示 gate profile 与命令类型；该批只做确定性 inventory，不执行测试、不新增外部 CI workflow、不写 case/pack/runtime state、不扩大 PowerShell 编排、不执行 heavy-tool、不写 authority/confirmed。
- Batch 138 / Stage 6-7 新增 android-native Agent Team real dry-run smoke：新增 `android-native-agent-team-dryrun-smoke.ps1`，使用公共 `/rekit` façade 与临时 case 跑通 `android-native` 的 `init -Apply`、`start -Apply`、`plan-subagents` review packet、candidate/verification/decision `note` append、`gate -WhatIf/-Apply -Action inject`、`note -List`、`overview` text/JSON、lane `handoff -Apply` 与 `doctor`，验证 native-analysis lane、inject/hook pending-gate 和 handoff 展示不泄漏 web/generic-binary/malware/vuln/ctf/unpack/ollvm/vmp/template 语义，同时保持不自动 spawn agent、不连接设备、不 attach Frida、不执行 hook/inject/dump/patch/heavy-tool、不写 authority/confirmed。
- Batch 137 / Stage 6-7 新增 ollvm Agent Team real dry-run smoke：新增 `ollvm-agent-team-dryrun-smoke.ps1`，使用公共 `/rekit` façade 与临时 case 跑通 `ollvm` 的 `init -Apply`、`start -Apply`、`plan-subagents` review packet、candidate/verification/decision `note` append、`gate -WhatIf/-Apply -Action full-trace`、`note -List`、`overview` text/JSON、lane `handoff -Apply` 与 `doctor`，验证 obfuscation-analysis lane、full-trace/CFG pending-gate 和 handoff 展示不泄漏 web/generic-binary/malware/vuln/ctf/unpack/vmp/template 语义，同时保持不自动 spawn agent、不执行样本/full-trace/dump/patch/heavy-tool、不写 deobfuscated binary、不写 authority/confirmed。
- Batch 136 / Stage 6-7 新增 unpack-pe Agent Team real dry-run smoke：新增 `unpack-pe-agent-team-dryrun-smoke.ps1`，使用公共 `/rekit` façade 与临时 case 跑通 `unpack-pe` 的 `init -Apply`、`start -Apply`、`plan-subagents` review packet、candidate/verification/decision `note` append、`gate -WhatIf/-Apply -Action dump`、`note -List`、`overview` text/JSON、lane `handoff -Apply` 与 `doctor`，验证 unpack-analysis lane、dump/unpack pending-gate 和 handoff 展示不泄漏 web/generic-binary/malware/vuln/ctf/vmp/template 语义，同时保持不自动 spawn agent、不执行样本/debug/dump/patch/heavy-tool、不写 unpacked binary、不写 authority/confirmed。
- Batch 135 / Stage 6-7 新增 ctf Agent Team real dry-run smoke：新增 `ctf-agent-team-dryrun-smoke.ps1`，使用公共 `/rekit` façade 与临时 case 跑通 `ctf` 的 `init -Apply`、`start -Apply`、`plan-subagents` review packet、candidate/verification/decision `note` append、`gate -WhatIf/-Apply -Action network`、`note -List`、`overview` text/JSON、lane `handoff -Apply` 与 `doctor`，验证 challenge-analysis lane、remote/network pending-gate 和 handoff 展示不泄漏 web/generic-binary/malware/vuln/vmp/template 语义，同时保持不自动 spawn agent、不远程连接、不 brute force、不 fuzz、不 replay exploit、不执行 network/debug/dump/patch/heavy-tool、不写 flag/authority/confirmed。
- Batch 134 / Stage 6-7 新增 vuln-research Agent Team real dry-run smoke：新增 `vuln-research-agent-team-dryrun-smoke.ps1`，使用公共 `/rekit` façade 与临时 case 跑通 `vuln-research` 的 `init -Apply`、`start -Apply`、`plan-subagents` review packet、candidate/verification/decision `note` append、`gate -WhatIf/-Apply -Action debug`、`note -List`、`overview` text/JSON、lane `handoff -Apply` 与 `doctor`，验证 vuln-analysis lane、debug/repro pending-gate 和 handoff 展示不泄漏 web/generic-binary/malware/vmp/template 语义，同时保持不自动 spawn agent、不连接 live target、不主动扫描、不 fuzz、不 replay exploit、不执行 debug/dump/patch/heavy-tool、不写 authority/confirmed。
- Batch 133 / Stage 6-7 新增 malware-analysis Agent Team real dry-run smoke：新增 `malware-analysis-agent-team-dryrun-smoke.ps1`，使用公共 `/rekit` façade 与临时 case 跑通 `malware-analysis` 的 `init -Apply`、`start -Apply`、`plan-subagents` review packet、candidate/verification/decision `note` append、`gate -WhatIf/-Apply -Action network`、`note -List`、`overview` text/JSON、lane `handoff -Apply` 与 `doctor`，验证 sample-analysis lane、network/sandbox pending-gate 和 handoff 展示不泄漏 web/generic-binary/vmp/template 语义，同时保持不自动 spawn agent、不执行样本/sandbox/network detonation/debug/dump/patch/heavy-tool、不写 authority/confirmed。
- Batch 132 / Stage 6-7 新增 generic-binary-re Agent Team real dry-run smoke：新增 `generic-binary-agent-team-dryrun-smoke.ps1`，使用公共 `/rekit` façade 与临时 case 跑通 `generic-binary-re` 的 `init -Apply`、`start -Apply`、`plan-subagents` review packet、candidate/verification/decision `note` append、`gate -WhatIf/-Apply -Action debug`、`note -List`、`overview` text/JSON、lane `handoff -Apply` 与 `doctor`，把 Batch 125 的 package E2E 扩展成可重复真实临时 case dry-run，同时验证不自动 spawn agent、不写 authority/confirmed、不执行样本/debug/trace/dump/patch/heavy-tool，且 handoff 不泄漏 web/vmp/template 语义。
- Batch 131 / Stage 8 新增 PowerShell façade freeze invariant：扩展 `internal/rekit/manifest` release invariants，静态锁定 `rekit/rekit.ps1` 默认 Go façade 委托集合、`ValidateSet` 覆盖、`release-check` Go-only/no-target guard、`plan-subagents` legacy/internal 边界、blocked heavy-tool/authority/confirmed 不得进入默认委托，以及 `facade-smoke.ps1` 中的 release-check fake delegation / `REKIT_GO_DISABLE` 回归信号；该批不删除 PowerShell 代码、不改变 runtime 写入面，仅把 Batch 129 deprecation strategy 的 freeze guard 固化为 Go test。
- Batch 130 / Stage 8 新增 Go-owned release-check inventory：新增 `internal/rekit/releasecheck` 与 `/rekit release-check`，以只读 JSON/text envelope 汇总 recommended minimum、必备文档入口、pack schema 状态、安全边界与 known gaps，并把该命令接入 release readiness、test catalog、smoke guide、PowerShell deprecation matrix 和 façade 默认 Go 委托；该命令只做确定性 inventory，不执行测试、不写 case/pack 状态、不新增大型 PowerShell 编排或外部 CI 副作用。
- Batch 129 / Stage 8 新增 PowerShell runtime deprecation strategy：新增 `docs/powershell-deprecation.md`，明确 Go-owned、façade-only、legacy-only、blocked 命令归属，列出 `rekit/lib/*.ps1` 模块冻结/保留状态、fallback retirement gates 与禁止迁移清单，并用 release invariant 锁定 deprecation 文档、README/CLAUDE/release readiness/go-first 入口，避免后续继续扩张 PowerShell runtime 语义。
- Batch 128 / Stage 8 新增 release readiness checklist 与 Go invariant：新增 `docs/release-readiness.md`，集中记录本机 release gate、recommended minimum、发布前人工检查、pack maturity matrix、Go-owned/PowerShell legacy 状态与 known gaps，并新增 manifest release invariant 测试防止 checklist 漏掉核心命令、边界和接手章节；同时在 README、CLAUDE 与 Go-first convergence 入口中接入该 checklist。
- Batch 127 / Stage 6-7 完成 web-security Agent Team real dry-run smoke：新增 `web-security-agent-team-dryrun-smoke.ps1`，使用公共 `/rekit` façade 与临时 case 跑通 `web-security` 的 `init -Apply`、`start -Apply`、`plan-subagents` review packet、candidate/verification/decision `note` append、`gate -WhatIf/-Apply -Action network`、`note -List`、`overview` text/JSON、lane `handoff -Apply` 与 `doctor`，把 Batch 126 的 package E2E 扩展成可重复真实临时 case dry-run，同时验证不自动 spawn agent、不写 authority/confirmed、不执行真实网络请求/扫描/fuzz/exploit replay/heavy-tool，且 handoff 不泄漏 generic-binary/vmp/template 语义。
- Batch 126 / Stage 6-7 完成 Go web-security pack-neutral E2E package test：新增非 RE-only 的 `web-security` Go CLI package 闭环测试，覆盖 `start -Apply` 创建 `feature-authz` lane、`plan-subagents` 选择 `web-security:feature-analysis` route、candidate/verification/decision `note` append、`gate -Apply -Action network` pending-gate request、`overview` text/JSON 与 lane `handoff -Apply` 展示，验证 Web/API pack 的 endpoint/feature route、network gate、`workspace/features/**` 与 handoff 不泄漏 `generic-binary-re`/binary/vmp/template 语义，同时保持不自动 spawn agent、不写 authority/confirmed、不执行请求回放/扫描/fuzz/exploit replay/network 操作、不新增 PowerShell 编排。
- Batch 125 / Stage 6-7 完成 Go generic-binary-re pack-neutral E2E package test：新增非 `_template` / 非 `vmp-re` 的 `generic-binary-re` Go CLI package 闭环测试，覆盖 `start -Apply` 创建 `binary-analysis` lane、`plan-subagents` 选择 `generic-binary-re:binary-analysis` route、candidate/verification/decision `note` append、`gate -Apply` pending-gate request、`overview` text/JSON 与 lane `handoff -Apply` 展示，验证非 feature lane、`workspace/binary/**`、pack-specific route 和 handoff 不泄漏 `_template`/feature/vmp 语义，同时保持不自动 spawn agent、不写 authority/confirmed、不执行样本/debug/trace/dump/patch、不新增 PowerShell 编排。
- Batch 124 / Stage 6 完成 Go reviewer/decision E2E package test：新增 `_template` pack 的 Go CLI package 闭环测试，覆盖 `start -Apply` → candidate `note` append → reviewer `verification` append → main `decision` append → `note -List` text/JSON → `overview` text/JSON → lane `handoff -Apply` 的 reviewer verdict / merge decision 可见性链路，验证 candidate、verification、decision、batch 聚合与 handoff 展示可被 Go runtime 串起来，同时保持不自动 spawn agent、不写 authority/confirmed、不执行 heavy-tool、不新增 PowerShell 编排。
- Batch 123 / Stage 6 完成 Go gate/dispatch E2E package test：新增 `_template` pack 的 Go CLI package 闭环测试，覆盖 `start -Apply` → `plan-subagents` review artifact → `gate -Apply` pending-gate request → `overview` text/JSON → lane `handoff -Apply` 的 bounded dispatch / heavy-tool gate 可见性链路，验证 packet/summary observability、pending-gate ledger、overview sections 与 handoff 展示可被 Go runtime 串起来，同时保持不自动 spawn agent、不写 authority/confirmed、不执行 heavy-tool、不新增 PowerShell 编排。
- Batch 122 / Stage 5-6 完成 Go workstream E2E package test：新增 `_template` pack 的 Go CLI package 闭环测试，覆盖 attached case 中 `start -Apply` → `note` append → lane outbox → `continue -Apply` → lane/project `handoff -Apply` 的 case-local 状态流，验证 facts/routing/run digest/resume/board/handoff 可被 Go runtime 串起来，同时保持不写 authority/confirmed、不执行 heavy-tool；该测试把 Stage 5 完成信号与 Stage 6 dry-run harness 的第一条非 `vmp-re` pack 验证纳入 `go test ./internal/rekit/cli`。
- Batch 121 / Stage 5 完成 continue apply default Go façade：attached case 的 `/rekit continue -Apply [selector]` 默认委托 Go，显式写入 case-local facts/routing/run digest/lane resume/checkpoint 与 board refresh；authority/confirmed 写入继续被 defer，需要显式用户确认，不执行 heavy-tool；`continue -WhatIf -Format json` 仍保持非写入 preview，文本/无 `-Apply` 工作线 flow 与 `REKIT_GO_DISABLE=1` 继续回退 PowerShell，并由 Go CLI tests、continue what-if/apply smoke 与 façade smoke 覆盖默认委托、幂等 skipped、authority guard 和 fallback 边界。
- Batch 120 / Stage 5 完成 note list text default façade：attached case 的 `/rekit note -List` 文本/table/tsv 查询与 `note -List -Format json` 均默认委托 Go，保持只读 ledger 查询、不写 facts/board/lane/handoff/authority/confirmed、不执行 heavy-tool；append 与 `-WhatIf` 既有 Go 默认路径不变，`REKIT_GO_DISABLE=1` 继续回退 PowerShell，并由 façade smoke、Agent Team review-loop smoke 与 Go CLI/note tests 覆盖默认委托、文本 fallback、JSON envelope 和 no-write 边界。
- Batch 119 / Stage 5 完成 overview board initialization default façade：attached case 缺 `.rekit/board.json` 时，Go overview 会初始化 case-local board/facts/policy/default authority lane；`/rekit overview` 默认文本输出与 `overview -Format json` 均经 façade 委托 Go，JSON envelope 在本次初始化时标记 `isMutation=true`；只写 case-local workstream state，不写 facts event/authority/confirmed、不执行 heavy-tool，并由 Go CLI tests、overview readonly smoke 与 façade smoke 覆盖初始化、后续只读、默认委托和 fallback 边界。
- Batch 118 / Stage 5 完成 continue JSON preview default façade：attached case 的 `/rekit continue -WhatIf -Format json` 默认委托 Go，输出非写入 JSON preview；文本 preview、无 `-WhatIf` 的 JSON 调用、`continue` apply/text flow 与 `REKIT_GO_DISABLE=1` 继续回退 PowerShell；不写 facts/run/board/lane/authority/confirmed、不执行 heavy-tool，并由 façade smoke、continue what-if smoke 与 Go CLI tests 覆盖默认委托、fallback、no-write preview 和 apply guard。
- Batch 117 / Stage 5 完成 start/handoff apply default façade：attached case 的 `/rekit start -WhatIf -Format json`、`start -Apply`、`handoff -WhatIf -Format json` 与 `handoff -Apply` 默认委托 Go，输出 JSON envelope；文本 preview/无 `-Apply` 文本工作线 flow、`continue` apply/text flow 与 `REKIT_GO_DISABLE=1` 继续回退 PowerShell；不写 authority/confirmed、不执行 heavy-tool，并由 façade smoke、start/handoff smoke 与 Go CLI tests 覆盖默认委托、fallback、no-write preview 和工作线/handoff 写入边界。
- Batch 116 / Stage 5 完成 note append default façade：attached case 的 `/rekit note` append 与 `note -WhatIf` 默认委托 Go，输出 JSON envelope，仍只 append `.rekit/facts/*.jsonl` 或预览、不写 authority/confirmed、不执行 heavy-tool，并由 `internal/rekit/note` package tests、façade smoke 与 Agent Team review-loop smoke 覆盖 no-write、schema/lane guard、eventId 幂等、默认委托和展示闭环。
- Batch 115 / Stage 5 完成 gate apply default façade：attached case 的 `/rekit gate -Apply` pending-gate request ledger 写入默认委托 Go，仍只 append `.rekit/facts/requests.jsonl`、要求 `-Actor`、不执行 heavy-tool、不写 confirmed/authority，并由 `internal/rekit/gate` package tests、façade smoke 与 gate parity smoke 覆盖 no-write、actor guard、幂等、默认委托和展示闭环。
- Batch 114 / Stage 5 完成 gate what-if default façade：attached case 的 `/rekit gate -WhatIf` 非写入 heavy-tool gate preview 默认委托 Go，输出 pending-gate request plan 但不写 ledger、不执行 heavy-tool，保留 `gate -Apply`、实际工具执行和 start/handoff/continue preview 的既有边界，并由 façade与 gate parity smoke 覆盖默认委托、no-write 和 fallback。
- Batch 113 / Stage 5 完成 overview/note JSON readonly default façade：已初始化 case 的 `/rekit overview -Format json` 与 `/rekit note -List -Format json` 只读查询默认委托 Go，保留 overview 文本/缺 board 初始化、note append/text list、gate/workstream preview 的既有 PowerShell 或显式 enable 边界，并由 façade、overview readonly 与 review-loop smoke 覆盖默认委托、fallback 和 no-write 约束。
- Batch 112 / Stage 4 完成 promote apply actual write Go default façade：`/rekit promote -Apply` 实际 pack source 写入默认委托 Go，继续要求显式 `-Apply` 与 review-first 用户确认，保留文本 what-if 与 `REKIT_GO_DISABLE=1` PowerShell fallback，并由 package tests 与 promote apply smoke 覆盖 backup、deny、validation rows、restore 与 cleanup 边界。
- Batch 111 / Stage 4 完成 promote apply restore test hardening：扩展 `internal/rekit/promote` package tests，使用自包含临时 repo/case/pack 覆盖 `Apply` what-if no-write、actual apply backup/validation rows、blocked deny 不写与 validation failure restore，减少 `promote -Apply` pack source 写入边界只靠 PowerShell smoke 捕获回归的风险。
- Batch 110 / Stage 4 完成 sync package test hardening：新增 `internal/rekit/sync` package tests，使用自包含临时 repo/case/pack 覆盖 `ApplyPreview` no-write、`Apply` managed/template/managed-block/state 写入、force template overwrite、backup path 与 backup escape guard，减少 sync 写入边界只靠 PowerShell smoke 捕获回归的风险。
- Batch 109 / Stage 4 完成 promote package test hardening：新增 `internal/rekit/promote` package tests，使用自包含临时 repo/case/pack 覆盖 `CreateCandidates` what-if no-write、candidate/index/tooling candidate 写入、sanitization redaction、unique candidate path 与 restore helper，减少 promote 写入边界只靠 PowerShell smoke 捕获回归的风险。
- Batch 108 / Stage 4 完成 promote candidate 写入 Go default façade：`/rekit promote -CreateCandidates` 实际候选写入默认委托 Go，继续覆盖 candidate/index/tooling candidate、deny/sanitize、pack-root containment 与 cleanup smoke；文本 promote what-if、`REKIT_GO_DISABLE=1` fallback 和 `promote -Apply` 实际 pack source 写入仍保留 PowerShell fallback 或手动 Go CLI，保持 review-first、no authority/confirmed/heavy-tool 边界。
- Batch 107 / Stage 4 完成 promote 非写入 Go default façade：`/rekit promote` review、review artifact 写入、`promote -CreateCandidates -WhatIf -Format json` 与 `promote -Apply -WhatIf -Format json` 非写入 preview 默认委托 Go，`REKIT_GO_DISABLE=1` 保留 PowerShell fallback；文本 promote what-if 与 promote candidate/apply 实际写入继续走 PowerShell fallback 或手动 Go CLI，保持 review-first、deny/sanitize、no pack actual write/authority/heavy-tool 边界。
- Batch 106 / Stage 4 完成第一组 sync Go default façade：`/rekit sync` review、`sync -Apply` 实际写入与 `sync -Apply -WhatIf -Format json` 非写入 preview 默认委托 Go，`REKIT_GO_DISABLE=1` 保留 PowerShell fallback；文本 `sync -Apply -WhatIf` 继续走 PowerShell dry-run，保持 review-first、backup、state、no pack/promote/authority/heavy-tool 边界。
- Batch 105 / Stage 3 完成第一组 case lifecycle Go default façade：`attach`、`repair`、`init/bootstrap` 的非写入预览与显式 `-Apply` 默认委托 Go，`REKIT_GO_DISABLE=1` 保留 PowerShell fallback；Go `attach` 补齐 legacy metadata 与初始 state 写入，保持 case-local shim thin，不写 managed docs、board/facts/lanes、authority/confirmed 或 heavy-tool 输出。
- 新增仓库根目录 `CLAUDE.md`，提供 Claude Code 维护本 kit/template/runtime 仓库的入口说明、分层改动边界和验证命令。
- 新增 `docs/vision.md`，定义网络安全研究 / 安全工程 Agent Team 框架定位、模块边界和后续阶段实施方案；`vmp-re` 是首个成熟 pack。
- 新增 `references/vmp-re/agent-driven-re.md`，定义 VMP RE Agent Team 的角色、packet、candidate→confirmed 流程和人工门禁。
- 在 `docs/vision.md` 中新增批次执行协议，明确后续可分批实施、自审调整、需停下询问的边界和计划写回文档要求。
- 新增 `common/policies/agent-team.md` 与 `common/policies/tool-adapters.md`，沉淀跨 pack Agent Team 和外部工具 adapter 通用契约。
- 新增 `docs/agent-team-usage.md`，说明新架构使用方式、旧 case 兼容、主线/功能支线工作流和后续优化空间。
- 新增 `docs/reference-absorption.md`，映射参考文章、`ida-agent-bridge`、`clark-utov` 的吸收点、当前落地能力和后续优化项。
- 新增 `docs/pack-authoring.md`、`docs/evidence-ledger.md`、`docs/orchestration-plan.md` 和 `docs/batch-plan.md`，分别记录 pack 编写、证据账本、半自动编排和后续批次计划。
- 新增 `packs/_template/` pack 作者骨架，用于后续创建 `web-security`、`malware-analysis`、`vuln-research`、`ctf`、`unpack-pe`、`android-native`、`ollvm` 等新 pack。
- 增强 `doctor` 的 manifest、policy overlay、case thin shim、board/lane 和 JSONL 校验，作为后续 runtime 架构调整的安全网。
- 为 `packs/_template` 补齐最小 policy overlay registry，使模板 pack 可通过 pack validation。
- 将 B3 工作线默认主线、默认 start 类型、长期 handoff 路径、sync backup root、authority files 和 request 默认路由改为 manifest 驱动，减少 `vmp-re` 硬编码。
- 新增 Go runtime 迁移方案文档，明确 PowerShell façade + Go deterministic backend 的渐进迁移路线。
- 新增 `docs/go-first-convergence-plan.md`，作为 Batch 101 后的新阶段导航，明确 Go deterministic runtime owner、release readiness、Agent Team dry-run 闭环、PowerShell 收缩和 pack-neutral 收口方向，防止后续长轮次自主实施因上下文压缩偏回 PowerShell smoke/catalog 扩张。
- 新增 Go read-only runtime skeleton，支持手动运行 `status`、pack `doctor/validate` 与 manifest/policy validation，并补充 manifest/CLI/runtime guard tests；未实现 case doctor 时显式拒绝 case target。
- 新增 Go `sync/promote` review-only plan skeleton，先输出非写入 JSON plan，并拒绝 `sync -Apply`、`promote -CreateCandidates/-Apply` 等写入路径。
- 新增 `docs/agent-team-rollout-plan.md`，记录 Agent Team 当前"契约已固化、编排未落地"的真实状态，并按选项 C（契约 dry-run 优先）给出 R0-R7 分批实施计划与 R3 决策门。
- 在 README、CLAUDE.md、`docs/vision.md` 执行清单与候选清单中接入 Agent Team rollout 计划入口。
- 新增 `docs/agent-team-dryrun-script.md`，定义 R0 端到端契约压测脚本（mock 对象 + S1-S5 五步 + 字段缺口记录区），并在 kit 仓库外创建临时 case `agent-team-dryrun` 供 R1 执行。
- Agent Team rollout R1-R7 完成：R1 契约压测暴露 6 类缺口；R2 回写 `common/policies/agent-team.md`（evidence_id、decision event 字段、packet-vs-event 关系、lane id 规范化）、`evidence.md`、`handoff.md`（workspace packet 引用）、`subagents.md`（L1 tool_scope）；R3 决策 ledger runtime 优先；R4 新增内部 `note` 命令与 `Add-RekitFactEvent`，让 agent 手动 append observation/candidate/decision/request/publication 到 `.rekit/facts/*.jsonl`，overview 聚合手写事件，handoff 增加 workspace packet 引用区段，overview pending 计数对齐 decision event；R5 判定不扩 runtime（spawn 是主会话职责）；R6 heavy-tool gate 写入 vmp-re verification overlay。
- 修复 `B3.Core.ps1` `Join-RekitRelativePath` 在 Windows PowerShell 5.1 下因 `[System.IO.Path]::GetRelativePath` 不存在而崩溃的问题，改用 PS 5.1 兼容的路径前缀比较实现。
- 在 `docs/agent-team-rollout-plan.md` 追加 §4 Kit runtime hardening 计划：B1 `note` schema 校验、B2 overview 未决/pending/冲突明细、B3 handoff 引用 decision event，作为 R0-R7 后续优化路线。
- Kit runtime hardening B1-B3 完成：B1 `Add-RekitFactEvent` 加 confidence/decision/status 枚举校验、evidenceRefs 非空校验、lane 存在性校验（`Invoke-RekitNote` 传 Board）；B2 `overview` 增加"未决 candidate"（含同 subject 冲突标记）和"pending-gate"明细区段，限 N=10，超出提示"另有 N 条"；B3 `Write-RekitLaneHandoff` 增加 `## decision` 区段，列 latest 5 条 decision 摘要。临时 case 验证非法值被拒、合法值写入、overview 明细正确、handoff 含 decision 区段。
- Kit runtime hardening B5-B7 完成：B5 `Write-RekitLaneHandoff` 增加 `## pending-gate` 区段，decision 区段兼容 `action` 字段（auto `continue` 写的 decision 用 `action`，`note` 用 `decision`，两套并存）；B6 `overview` 增加"最近 decision"明细区段，兼容 `action` 字段；B7 `Invoke-RekitNote` 加 `-List` 查询模式，按 kind/lane 过滤列出事件，限 N=20。临时 case 验证 handoff 含 decision+pending-gate、overview 含最近 decision、`note -List` 全部/按 kind/按 kind+lane 过滤正确。
- 在 `docs/agent-team-rollout-plan.md` 追加 §5 Ledger schema 对齐与架构治理（C 系列）：基于 `docs/evidence-ledger.md` 草案审查发现 runtime ledger 字段偏离草案（自定 decision 枚举、缺 actor/risk/related/candidateId/claim/verifier/confirmedBy/writes、缺 4 种 kind、auto 用 action 字段），按草案 Runtime 落地顺序分 C1-C8 八批对齐，兼容历史 JSONL 不迁移。
- Agent Team rollout C1 完成：`orchestration-plan.md` O2 标注 bounded dispatch 已被 rollout R5 否决（保留设计参考）；`vision.md` §8 候选清单第 7 条同步 `packs/_template` 与 `B3.Lane` 修复已完成、后续按 batch-plan/rollout §4-§5 推进；`rollout-plan.md` §"当前状态"段同步 R0-R7 已完成状态；`.claude/skills/rekit/SKILL.md` 命令清单补 `note`（含 `-List` 查询，标注为账本写入/查询入口与 schema 校验）。纯文档批次，未动 runtime。
- Agent Team rollout C2 完成：`Add-RekitFactEvent` 基础字段对齐 `docs/evidence-ledger.md` 草案——补 `-Actor`/`-Risk`/`-Related` 参数与 `schemaVersion: 1`/`actor`/`risk`/`related` 字段；`validDecision` 改 `accept|reject|defer|supersede`（`confirm` 写入被拒）；`validStatus` 取草案 6 值与 runtime 已落地 3 值的并集（`open|accepted|rejected|superseded|resolved|deferred|pending-gate|confirmed|needs_more_evidence`），保留 `pending-gate`/`confirmed`/`needs_more_evidence` 不破坏 R6 heavy-tool gate 契约（`packs/vmp-re/policies/verification.overlay.md` 用 `note -Kind request -Status pending-gate` 登记 gate、`overview`/`handoff` 读层过滤 `pending-gate`）；`Invoke-RekitNote` 补 param + 透传新参数；`overview` `terminalStatus` 补 `accepted`/`resolved`。临时 case 验证 accept 写入、confirm 被拒、accepted status、pending-gate 保留、actor/risk/schemaVersion 字段落地、历史 JSONL 仍可读、overview 不崩。
- Agent Team rollout C3 完成：补齐 `docs/evidence-ledger.md` 草案定义的全部 9 种事件类型——`Add-RekitFactEvent`/`Get-RekitFactFilePath` ValidateSet 扩 `hypothesis`/`verification`/`intervention`/`rollback`；`Get-RekitBoardPaths` 补 4 路径属性（`Hypotheses`/`Verifications`/`Interventions`/`Rollbacks`）；`Ensure-RekitBoard` 初始化 4 个新空 JSONL；`Validate.ps1` `Test-RekitWorkstreamState` 与 `B3.Auto.ps1` `Get-RekitKnownEventIds` 校验/去重循环同步补 4 文件；verification kind 加 `-TargetRef`/`-Verifier`（manual-review|schema-check|focused-trace|parity|cross-run|tool-review）/`-Verdict`（accepted|rejected|inconclusive|needs-more-evidence）扩展字段与校验，intervention kind 加 `-Action`（override|rollback|heavy-tool-approval|schema-migration|external-side-effect）/`-ApprovedBy`/`-Scope`/`-Expires` 扩展字段与校验；`Invoke-RekitNote` param 补扩展字段并透传，`note -List` 展示 verification/intervention/candidate-risk/decision-confirmedBy 字段。临时 case 验证 4 新文件初始化、5 种新 kind 写入、非法 verdict/action 被拒、扩展字段落地、`note -List` 展示新字段。
- Agent Team rollout C4 完成：`New-RekitDecision`（`B3.Auto.ps1`）字段对齐 `docs/evidence-ledger.md` 草案——`action` 字段改 `decision`（值映射 `auto-publish`/`auto-route`/`auto-apply-authority`/`auto-accept-shared`→`accept`，`pending-user`/`defer`→`defer`），补 `schemaVersion:1`/`confirmedBy:runtime`/`subject`/`summary`，authority 写入时附 `writes`（file/backup/diff），`extra` 保留给非 authority 场景；9 处 `Add-RekitJsonLine -Path $paths.Decisions` 调用保留直接写（原方案"改走 `Add-RekitFactEvent` 统一写入路径"因 eventId 去重会破坏 auto 流程对同一 eventId 写两条记录的设计而调整）；读层（overview/handoff/note-List）已兼容 `action`/`decision` 双字段（B5/B6 实现）。临时 case `continue` smoke 验证新 decision event 含 `decision:accept`/`confirmedBy:runtime`，历史 `action` 字段 decision 仍可读，overview "最近 decision" 兼容展示。
- Agent Team rollout C5 完成：展示层对齐草案新字段——`overview` "最近 decision" 显示 `by=<actor|confirmedBy>`（actor 优先、fallback confirmedBy）；`Write-RekitLaneHandoff` `## decision` 区段显示 `by=<confirmedBy|actor>`；`note -List` decision 展示从 `confirmedBy=` 改为 `by=` 并 fallback `actor`（`note` 写的 decision 用 `-Actor`，auto 写的用 `confirmedBy`，统一展示为 `by=`）；verification/intervention/candidate-risk 展示已在 C3 落地。临时 case 验证 overview/handoff/note-List 三处 `by=main` 显示正确，历史无 actor/confirmedBy 的事件 `by=` 为空（append-only 不回填）。
- Agent Team rollout C6 完成：policy 文档去重，建立 single source of truth——`evidence.md` 改为 `agent-team.md` 的证据原则补充（顶部声明不重复定义 packet schema，packet/event 字段指向 `agent-team.md` 与 `docs/evidence-ledger.md`，更新 `note`/`continue` 入口与 9 种 kind 引用）；`handoff.md` 补 `## decision event 引用` 小节（指向 `evidence-ledger.md` decision schema，`by` 字段 confirmedBy 优先 fallback actor、`## pending-gate` 区段）；`tool-output.md` 内容并入 `tool-adapters.md` "输出契约"小节（含报告格式模板），`tool-output.md` 改为 deprecated 指向，`policies/manifest.yml` title 标 deprecated（保留注册避免外部引用断链）；`subagents.md` 补 overlay 可 extend `decision` 枚举说明 + ledger decision 与 reviewer output contract `needs_l2|needs_l3` 是不同层的澄清；`review-first.md`/`write-boundaries.md` 顶部互引（review-first 偏流程、write-boundaries 偏边界）。doctor 验证 policy registry 与 budget 不回归。
- Agent Team rollout C7 完成：handoff 子系统独立成 `rekit/lib/B3.Handoff.ps1`，含 `Get-RekitLatestRunDigestPath`/`Write-RekitProjectHandoff`/`Write-RekitLaneHandoff`/`Write-RekitHandoff` 4 函数（从 `B3.Commands.ps1` 机械移动，行为不变）；`rekit.ps1` dot-source `B3.Handoff.ps1`（在 `B3.Commands` 前）；`B3.Commands.ps1` 从 534 行降到 339 行，聚焦用户级命令入口。`B3.Handoff.ps1` 用 UTF-8 with BOM 写入（含中文，PS 5.1 解析需 BOM，与 `B3.Commands.ps1`/`B3.Auto.ps1` 一致）。临时 case 验证 `handoff main` 与项目级 handoff 生成不回归、handoff 文件区段（新会话开场/推荐读取/边界）正确、doctor 双通过。
- Agent Team rollout C8 完成（C 系列收尾）：`docs/reference-absorption.md` 候选清单勾选 evidence ledger runtime 项（行 37 改 `[x]`，heavy-tool gate runtime/bounded dispatch 加注当前形态），更新"当前已落地/尚未落地"描述、clark-utov 映射表（runtime ledger 9 种 kind + decision 字段对齐草案，batch 模型与 intervention 强制门禁待实现）、能力表（ledger 草案 → runtime 已落地）、"还没落地"清单（ledger runtime 已落地，batch/intervention 待实现）、ledger 最小实现注（已完成 9 种 kind 文件 + note/continue 写入 + overview/handoff/note-List 读层）。C 系列自审：C1-C8 八批全部完成，runtime ledger 字段集与枚举对齐 `docs/evidence-ledger.md` 草案，读层兼容历史 `action`/`confirm`/`pending-gate` 值，append-only 不迁移旧事件，policy single source of truth，handoff 子系统独立，B3.Commands.ps1 降到 339 行。最终验证 `doctor` 双通过、`go test ./...` 通过、`git diff --check` 通过。
- Agent Team rollout D 系列启动：`docs/agent-team-rollout-plan.md` 新增 §6 batch / intervention / gate 闭环路线，并完成 D1 post-merge sanity（main 状态、B3.Handoff.ps1 UTF-8 BOM、dot-source 顺序、case shim thin boundary 与验证安全网均通过）。
- Agent Team rollout D2 完成：ledger event 增加可选 `batchId` 字段；`note` 支持 `-BatchId` 写入与 `note -List` batch 展示；`continue` 自动派生 `batch-<runId>` 并写入 event/decision/publication/digest；overview 新增“最近 batch”跨 9 种 fact JSONL 聚合；rollback/intervention 可通过 `TargetRef batch-...` 指向整批事件。历史 JSONL 不迁移、无 `batchId` 事件正常读取。
- Agent Team rollout D3 完成：overview 增加“未解决 intervention”“最近 intervention”“最近 rollback”读层；lane handoff 增加 `## intervention` 与 `## rollback` 区段；`note -List` 对 intervention/rollback 展示 target/action/approvedBy/scope/status/reason/batch。该批只增强展示闭环，不自动执行 heavy-tool、不自动回滚文件、不迁移历史 JSONL。
- Agent Team rollout D4 完成：按 Go backend 优先方向新增 `internal/rekit/gate` 与 Go CLI `-Command gate -WhatIf`，输出非写入 heavy-tool gate JSON plan（`eventPreview.kind=request`、`status=pending-gate`、`requiresConfirmation=true`、含 action/scope/budget/triedLightSteps/stopConditions/target/batchId），校验 attached case + lane id；不默认接入 PowerShell façade、不写 ledger、不执行 heavy-tool。
- Go runtime G2.3 完成：`-Command gate -Apply` 显式 append `.rekit/facts/requests.jsonl` pending-gate request，要求 `-Actor`，与 `-WhatIf` 互斥；eventId 由 gate 语义字段派生，重复写入返回 `applied=false`/`duplicate eventId`。该路径只写 gate request，不执行 heavy-tool、不写 confirmed/authority，也暂不列入 PowerShell 默认委托安全集合。
- Go runtime G2.1 完成：`sync/promote` review-only backend 增加 `-ReviewOutputDir` / `-PacketPath` / `-DiffPath` artifact 写入，输出 `packet.json`、`summary.md`、bounded diff 与 promote sanitized preview；该路径只写 review artifact，`isMutation=false`、`writesArtifacts=true`，不写 managed docs、pack 或 candidates。
- Go runtime G2.4 完成：`rekit.ps1` 增加显式 Go façade 委托开关（`REKIT_GO_ENABLE` / `REKIT_GO_DISABLE` / `REKIT_GO_EXE`），仅覆盖安全集合：`status`、kit doctor/validate、`sync/promote` review-only artifact、`gate -WhatIf` dry-run；默认仍走 PowerShell，不委托写入命令、case doctor、`gate -Apply`、`note` 或工作线命令。
- Go runtime G2.5 完成：Go `doctor/validate` 增加 attached case 只读校验，覆盖 instance/legacy metadata、case-local shim parity、managed/template files、managed block、facts JSONL、board/lane/workspace JSONL；PowerShell façade 显式启用 Go 时也可委托 case doctor。
- Go runtime G2.6 完成：新增 `rekit/tests/facade-smoke.ps1`，覆盖默认不委托、显式 Go 安全集合、`REKIT_GO_DISABLE` 优先级、`sync` 写入 flags 回退 PowerShell、`gate -WhatIf` 非写入委托与未启用时拒绝。
- Go runtime G3.1 完成：新增 `internal/rekit/attach` 与 Go CLI `-Command attach`，支持 `-WhatIf` 非写入预览和 `-Apply` 只写 `.rekit/instance.yml` + case-local thin shim；不写 managed docs、legacy metadata、state、board/facts/lanes，也暂不纳入 PowerShell façade 委托。
- Go runtime G3.2 完成：新增 `internal/rekit/casebind` 与 `internal/rekit/repair`，支持 Go CLI `-Command repair` 默认/`-WhatIf` 非写入预览和 `-Apply` 刷新 `.rekit/instance.yml`、`.re-template.yml`、case-local thin shim；不写 managed docs、board/facts/lanes 或 authority，也暂不纳入 PowerShell façade 委托。
- Gate request schema parity 完成：PowerShell `overview`、lane `handoff`、`note -List -Kind request` 现在展示 Go `gate -Apply` 写入 pending-gate request 的 `actor/risk/target/batchId/gate{action,scope,budget,triedLightSteps,stopConditions}` 字段；新增 `rekit/tests/gate-parity-smoke.ps1` 覆盖 Go 写入 + PowerShell 三处读层展示。
- G3.3 `sync -Apply` 迁移预研完成：新增 `docs/sync-apply-migration.md` 固化 PowerShell 写入语义、Go 迁移契约与 S1-S18 测试矩阵；新增 `rekit/tests/sync-review-parity-smoke.ps1` 验证 PowerShell/Go sync review action 与 bounded diff parity，仍未实现 Go 写入。
- G3.4 Go `sync -Apply` 手动路径完成：Go CLI 显式 `-Command sync -Apply` 可刷新 metadata/shim/legacy metadata、同步 managed files、处理 template create/skip/`-Force`、更新 managed block、写 `.rekit/state.json` 并报告 backup/writes；新增 `rekit/tests/sync-apply-smoke.ps1` 覆盖临时 case apply、backup、state、Go/PowerShell doctor。该写入路径仍不经 PowerShell façade 委托。
- G3.5 Go `init/bootstrap` 手动路径完成：Go CLI 显式 `-Command init|bootstrap -WhatIf` 输出非写入预览，`-Apply` 可创建临时 case 的 metadata/shim/legacy metadata、managed files、template files、managed block 与 `.rekit/state.json`；新增 `docs/init-bootstrap-migration.md` 和 `rekit/tests/init-bootstrap-smoke.ps1` 覆盖 preview 无副作用、apply、`-Force`、Go/PowerShell doctor 与 façade fallback。该写入路径仍不经 PowerShell façade 委托。
- G3.6 `promote -CreateCandidates` 迁移预研完成：新增 `docs/promote-candidates-migration.md` 固化 PowerShell candidate 写入、tooling sanitization、deny pattern 与 Go 迁移契约；新增 `rekit/tests/promote-candidates-preflight-smoke.ps1` 验证 PowerShell `-WhatIf -CreateCandidates` baseline、Go promote review artifact/sanitized preview parity、Go 写入 guard 与 façade fallback。本批不写 pack candidates，不实现 Go `promote -CreateCandidates` 写入。
- G3.7 Go `promote -CreateCandidates` 手动路径完成：新增 `promote.CreateCandidates` helper 与 CLI JSON result，支持 `-WhatIf` 非写入预览和显式 candidate/index/tooling candidate 写入；新增 `rekit/tests/promote-candidates-apply-smoke.ps1` 覆盖临时 case candidate 写入、blocked deny、tooling sanitization、pack-root containment、cleanup 与 façade fallback。该路径仍不经 PowerShell façade 委托，不实现 `promote -Apply`，不覆盖 pack managed docs。
- G3.8/G3.9 `promote -Apply` 迁移完成：`docs/promote-apply-migration.md` 固化 PowerShell apply baseline、backup/deny/validation/cleanup 语义与 Go 迁移契约；Go backend 新增 `promote.Apply` helper 与 CLI `-Apply/-Apply -WhatIf` JSON result，支持非写入 preview、safe managed docs backup 后写回 pack source、blocked deny 不写、写入后 pack validation；新增 `rekit/tests/promote-apply-preflight-smoke.ps1` 与 `rekit/tests/promote-apply-smoke.ps1` 覆盖 PowerShell baseline、Go apply、backup、pack-root containment、cleanup 与 façade fallback。该路径仍不经 PowerShell façade 委托，不写 authority/confirmed，不执行 heavy-tool。
- G4.1 Go `overview` 只读路径完成：新增 `internal/rekit/overview` renderer 与 CLI `-Command overview`，读取既有 `.rekit/board.json` 与 9 类 facts JSONL，输出工作线、共享事实、未决 candidate、pending-gate、decision、batch、intervention、rollback 和下一步建议；缺 board 时拒绝并提示先用 PowerShell overview 初始化。新增 `rekit/tests/overview-readonly-smoke.ps1` 验证只读、缺 board guard、Go gate request 展示字段和 façade fallback；公共 PowerShell façade 仍不委托工作线命令。
- G4.2 Go `start` 手动路径完成：新增 `internal/rekit/workstream` start helper 与 CLI `-Command start -WhatIf/-Apply`，支持非写入预览、显式初始化 board/facts/policy/default authority lane、创建或进入 feature lane、刷新 lane resume/checkpoint 与 board；新增 `rekit/tests/start-apply-smoke.ps1` 覆盖 preview 只读、apply scaffold、existing/force、Go/PowerShell doctor 与 façade fallback。公共 PowerShell façade 仍不委托工作线命令，不写 authority/confirmed，不执行 heavy-tool。
- G4.3 Go `handoff` 手动路径完成：新增 `workstream` handoff helper 与 CLI `-Command handoff -WhatIf/-Apply`，支持非写入预览、显式写项目级/工作线级 handoff、刷新 lane resume/checkpoint，并展示 workspace packet、decision、pending-gate、intervention、rollback 区段；新增 `rekit/tests/handoff-apply-smoke.ps1` 覆盖 preview 只读、apply 输出、Go/PowerShell doctor 与 façade fallback。公共 PowerShell façade 仍不委托工作线命令，不写 authority/confirmed，不执行 heavy-tool。
- G4.4 Go `plan-subagents` 手动路径完成：新增 `internal/rekit/subagents` plan helper 与 CLI `-Command plan-subagents`，支持 route/taskType 选择、`Items`/`ItemsFile` 分片、`ItemsPerAgent`/`MaxParallel` override，以及 review packet/summary artifact 写入；新增 `rekit/tests/plan-subagents-smoke.ps1` 覆盖 attached case、out-of-case guard、missing routes、Go/PowerShell doctor 与 façade fallback。公共 PowerShell façade 仍不委托该内部命令，不启动 subagent、不写 board/facts/lanes/authority/confirmed，不执行 heavy-tool。
- Agent Team review loop Batch A 启动：收敛通用 contract，明确 reviewer output decision 与 ledger decision 分层，canonical decision enum 使用 `accept|reject|defer|supersede`，历史 `confirm`/`confirmed`/`action` 仅作为读层兼容；`agent-team.md` 更新 `/rekit note` 手动 append 与 `/rekit continue` 自动抽取两条 facts event 路径，`evidence-ledger.md` 补 request/heavy-tool gate 扩展字段与 `needs_more_evidence` → `needs-more-evidence` 归一化说明。
- Agent Team review loop Batch B 启动：同步 VMP managed docs 与 manifest route；`agent-driven-re.md` 补 canonical contract 指向、`evidence_id`、runtime normalized lane id、`output_contract`、accepted decision 与 confirmed/authority 写入分层；`vmp-re` 两条 `subagentRoutes.outputContract` 补 `tier_used,tool_scope`，对齐通用 subagent contract。
- Agent Team review loop Batch C 完成：新增 `rekit/tests/agent-team-review-loop-smoke.ps1`，覆盖 `plan-subagents` review packet、`note -Kind verification`、`note -Kind decision`、`note -List`、`overview` 与 lane `handoff` 的最小闭环；该 smoke 只使用临时 case，不启动 subagent、不写 confirmed/authority、不执行 heavy-tool。
- Agent Team review loop Batch D 完成：PowerShell 与 Go `overview` 现在展示最近 verification，lane `handoff` 增加 `## verification` 区段；相关 smoke 与 Go CLI fixtures 覆盖 reviewer verdict 到 main decision 的可见性。该批只增强读层展示，不写 confirmed/authority、不执行 heavy-tool。
- Agent Team review loop Batch E 完成：`/rekit continue` digest 升级为结构化摘要，记录 inputs、route、packet refs、outputs、decisions 与 open risks，并在 `status.json` 写入对应索引字段；新增 `rekit/tests/continue-digest-smoke.ps1` 覆盖临时 case。该批不启动 subagent、不写 confirmed/authority、不执行 heavy-tool。
- Agent Team review loop Batch F 完成：重新评估 Go `note` / `continue` 迁移，结论是下一步优先 Go `note` 手动路径（先 `-List` 只读，再 append），`continue` 因 authority append、routing、digest/status 与 lane/board 刷新副作用继续暂缓到 G5 gate/parity tests 完整后。
- G4.5a Go `note -List` 只读路径完成：新增 `internal/rekit/note` 与 CLI `-Command note -List`，读取 9 类 facts JSONL，支持 `-Kind`/`-Lane` 过滤并展示 candidate/request/decision/verification/intervention/rollback 关键字段；该路径只读、不写 board/facts/lanes/handoff/authority/confirmed，不纳入 PowerShell façade 委托。
- G4.5b Go `note` append 手动路径完成：CLI `-Command note` 可显式 append 9 类 ledger event 到 `.rekit/facts/*.jsonl`，支持 PowerShell 对齐 enum/schema 校验、lane guard、eventId dedupe 与 `-WhatIf` 非写入预览；该路径只写 facts JSONL，不写 board/lane/handoff/authority/confirmed，也不纳入 PowerShell façade 委托。
- G5 preflight baseline 完成：新增 `rekit/tests/continue-preflight-smoke.ps1`，覆盖 PowerShell `continue` 的 authority append gate matrix（evidence、accepted verifier、confidence、CSV schema、conflict、max rows、allowlist）、backup/bounded diff、CSV 失败恢复、request routing 幂等、digest/status parity 与 `-WhatIf` no-write，作为后续 Go `continue -WhatIf` 迁移前测试网。
- G5 Go `continue -WhatIf` 预览路径完成：新增 `internal/rekit/workstream/continue.go` 与 CLI `-Command continue -WhatIf`，读取既有 board/lane/outbox/workspace 输出非写入 JSON preview，包含 inputs、packet refs、收集事件、routing/authority 决策预览、wouldWrites 与 blocked actions；新增 `rekit/tests/continue-whatif-smoke.ps1` 覆盖临时 case 预览、no-write、unsupported apply guard 与 PowerShell façade fallback。该路径不写 facts/run/board/lane/authority/confirmed，不纳入 façade 委托，不执行 heavy-tool。
- Batch 58 bounded dispatch 可观测性增强：Go 与 PowerShell `plan-subagents` review packet/summary 增加 `observability` 与 `reviewLoop`，记录 route 选择原因、review artifact 路径、shard 初始 `planned` 状态、blocked runtime actions、spawn/merge owner、verdict writeback 和 completion criteria；`plan-subagents` smoke 覆盖 Go 与 PowerShell fallback artifacts。该路径仍不启动 subagent、不写 board/facts/lanes/handoff/authority/confirmed。
- Batch 59 项目定位纠偏：将顶层定位从 RE-only 修正为面向网络安全研究与安全工程任务的 Claude Code Agent Team 框架，明确 `vmp-re` 是首个成熟 pack / 验证场而非最终边界，并同步 README、CLAUDE.md、vision、reference absorption、usage、pack authoring、skill、case shim、design 与 pack template 描述。
- Batch 60 完成：`packs/_template` 新增 managed `agent-team.md` 与两条 pack-neutral `subagentRoutes`，让新安全领域 pack 复制模板后即可通过 `plan-subagents` 生成 bounded review packet；Go manifest schema 与 PowerShell doctor 增加 subagent route 必填字段、正整数分片、reference 边界和 route id 唯一性校验，smoke 覆盖 Go 与 PowerShell fallback 的 `_template` route packet。
- Batch 61 完成：新增首个非 RE pack 骨架 `packs/web-security`，覆盖 Web/API 安全评估的 scope、Agent Team routes、toolchain router、tooling catalog、passive triage 与 bounded request replay recipe，并补 smoke 验证 Go/PowerShell doctor、init 与 `plan-subagents` route packet。
- Batch 62 完成：新增只读 `/rekit packs` pack inventory 矩阵，Go 与 PowerShell 均可列出 `_template`、`vmp-re`、`web-security` 的成熟度、schema、route、managed/tooling、authority lane 与版本信息；显式 `REKIT_GO_ENABLE=1` 时 façade 可委托该只读命令，并补 schema parity 与委托 sentinel smoke。
- Batch 63 完成：pack `manifest.yml` 新增显式 `maturity` 字段（`mature` / `template` / `skeleton` / `experimental`），`/rekit packs` 的成熟度优先读取 manifest，Go 与 PowerShell schema 校验均拒绝缺失或不支持的 maturity，避免后续多安全领域 pack 依赖描述启发式推断。
- Batch 64 完成：`/rekit packs` 新增 `-Format json` 机器可读输出，Go backend、PowerShell fallback 与显式 Go façade 委托均支持同一 envelope（`command/schemaVersion/isMutation/packCount/packs[]`），便于后续 pack-neutral 编排、CI 和新会话接手直接消费 pack inventory。
- Batch 65 完成：`/rekit status` 新增 `-Format json` 机器可读输出，Go backend、PowerShell fallback 与显式 Go façade 委托均支持同一只读 envelope（`command/schemaVersion/isMutation/runtimeRoot/templateRoot/pack/target/mode/case/manifest`），让 kit/case 绑定状态可被 CI、smoke 和后续编排直接消费。
- Batch 66 完成：`/rekit doctor` / `validate` 新增 `-Format json` 机器可读验证输出，Go backend、PowerShell fallback 与显式 Go façade 委托均支持 `command/schemaVersion/isMutation/pack/target/mode/valid/summary/rows[]` envelope，方便自动化消费 pack/case 验证 rows。
- Batch 67 完成：`/rekit note -List` 新增 `-Format json` 机器可读 ledger event 查询输出，Go backend 与 PowerShell fallback 均支持按 kind/lane 过滤后返回 `schemaVersion/command/caseRoot/repoRoot/pack/isMutation/kind/lane/eventCount/groups[]`，便于 review loop、handoff 和后续编排直接消费 facts JSONL。
- Batch 68 完成：`/rekit overview` 新增 `-Format json` 机器可读项目总览输出，Go backend 与 PowerShell fallback 均返回 `schemaVersion/command/caseRoot/repoRoot/pack/isMutation/automationMode/lanes/counts/sections/nextSteps` envelope，方便新会话接手、CI 和后续 orchestration 直接消费工作线与 review-loop 摘要。
- Batch 69 完成：`/rekit handoff -WhatIf` 新增 `-Format json` 机器可读写入预览，PowerShell fallback 在不委托 Go 的前提下返回 `schemaVersion/command/caseRoot/repoRoot/pack/isMutation/applied/requiresConfirmation/selector/project/lane/writes/blockedActions/nextSteps` envelope，便于接手自动化和审查流程安全消费 handoff 计划。
- Batch 70 完成：`/rekit start -WhatIf` 新增 `-Format json` 机器可读非写入预览，PowerShell fallback 在不委托 Go 的前提下返回 `schemaVersion/command/caseRoot/repoRoot/pack/isMutation/applied/requiresConfirmation/lane/writes/blockedActions/nextSteps` envelope，并拒绝 apply + JSON，便于自动化安全消费 start 工作线计划。
- Batch 71 完成：`/rekit continue -WhatIf` 新增 `-Format json` 机器可读非写入预览，PowerShell fallback 在不委托 Go 的前提下返回 `schemaVersion/command/caseRoot/repoRoot/pack/isMutation/applied/requiresConfirmation/selector/lane/runId/batchId/summary/inputs/packetRefs/events/openRisks/wouldWrites/blockedActions/nextSteps` envelope，并拒绝 apply + JSON，便于自动化预审 continue 收集、路由和 authority append 计划。
- Batch 72 完成：显式 `REKIT_GO_ENABLE=1` 时，PowerShell façade 可将 `start`、`handoff`、`continue` 的 `-WhatIf -Format json` 非写入机器可读预览委托给 Go backend；文本预览、apply/write 路径和非 JSON 工作线命令继续走 PowerShell fallback，保持 `/rekit` 公共入口稳定且不扩大写入委托面。
- Batch 73 完成：显式 `REKIT_GO_ENABLE=1` 且 attached case 已有 `.rekit/board.json` 时，PowerShell façade 可将 `overview -Format json` 只读机器可读项目总览委托给 Go backend；文本 overview 与缺 board 初始化继续走 PowerShell fallback，保留首次 overview 初始化语义。
- Batch 74 完成：显式 `REKIT_GO_ENABLE=1` 且目标为 attached case 时，PowerShell façade 可将 `note -List -Format json` 只读 ledger 查询委托给 Go backend；note append、`-WhatIf`、文本 `note -List` 继续走 PowerShell fallback，避免扩大 facts 写入委托面。
- Batch 75 完成：显式 `REKIT_GO_ENABLE=1` 时，PowerShell façade 可将 `attach -WhatIf -Format json` 与 `repair -Format json` 非写入 metadata 预览委托给 Go backend；attach/repair 文本预览和 `-Apply` 写入路径继续走 PowerShell fallback，保留确认后写入边界。
- Batch 76 完成：显式 `REKIT_GO_ENABLE=1` 时，PowerShell façade 可将 `init/bootstrap -WhatIf -Format json` 非写入初始化预览委托给 Go backend；init/bootstrap 文本预览与 `-Apply` 写入路径继续走 PowerShell fallback，避免扩大 case scaffold 写入委托面。
- Batch 77 完成：显式 `REKIT_GO_ENABLE=1` 时，PowerShell façade 可将 `promote -CreateCandidates -WhatIf -Format json` 非写入候选生成预览委托给 Go backend；promote 文本 what-if、实际 candidate 写入和 apply 写入路径继续走 PowerShell fallback，避免扩大 pack 写入委托面。
- Batch 78 完成：显式 `REKIT_GO_ENABLE=1` 时，PowerShell façade 可将 `promote -Apply -WhatIf -Format json` 非写入 pack apply 预览委托给 Go backend；promote 文本 apply what-if 与实际 apply 写入路径继续走 PowerShell fallback，避免扩大 pack source 写入委托面。
- Batch 79 完成：Go backend 增加 `sync -Apply -WhatIf` 非写入 JSON preview，显式 `REKIT_GO_ENABLE=1` 时 PowerShell façade 仅将 `sync -Apply -WhatIf -Format json` 委托给 Go；文本 sync apply what-if 与实际 `sync -Apply` 写入继续走 PowerShell fallback，避免扩大 case managed docs 写入委托面。
- Batch 80 完成：扩展 `facade-smoke.ps1` 为 Go façade 安全集合矩阵，使用 fake `REKIT_GO_EXE` sentinel 覆盖 attach/repair/init/bootstrap/sync/promote/overview/note/workstream JSON preview/read-only 委托与文本/write fallback，锁定近期开放的公共入口边界。
- Batch 81 完成：`facade-smoke.ps1` 默认改为自包含临时 case fixture，并将 review artifact、fake backend 与测试 case 统一放入 per-run matrix root 清理；仍支持显式 `-CaseRoot` 验证长期 dryrun case，降低 façade 安全集合矩阵对外部状态、硬编码 lane 和固定 temp 路径的依赖。
- Batch 82 完成：新增 `sync-apply-parity-smoke.ps1`，用双临时 case 对比 PowerShell 与 Go `sync -Apply` / `-Force` 后的 managed docs、template、managed block、metadata/shim 与 sync state；同时修复 PowerShell façade `sync/update -Force` 未转发到 `Sync-RekitPack` 的问题，使文本入口与 Go apply force 语义对齐。收尾补充 README、仓库 CLAUDE.md 与 `/rekit` skill 中的 `sync -Force` / 自包含 `facade-smoke.ps1` 维护说明。
- Batch 83 完成：新增 `agent-team-d5-dryrun-smoke.ps1`，用 `_template` 自包含 mock/非敏感临时 case 跑通 candidate → verification → decision → batch → intervention/rollback → handoff；验证 `note -List` 文本/JSON、overview 最近 batch 与 intervention/rollback 展示、lane handoff 区段和 doctor，同时保持 authority/confirmed、heavy-tool、pack source 与外部动作零写入。
- Batch 84 完成：新增 `ida-agent-bridge-readonly.md`，把 `ida-agent-bridge` 从 candidate tooling 推进到只读 index packet contract / capability card；manifest、tooling catalog、tooling README、IDA/x64dbg recipe、reference absorption 与 Agent Team rollout 文档已链接该 contract，仍不安装、不连接 IDA、不实现 runtime-level adapter、不允许 rename/comment/patch/debug/dump/network。
- Batch 85 完成：新增 `packs/malware-analysis` 防御性恶意样本分析 pack skeleton，覆盖 manifest、managed docs、task handoff template、policy overlay 空 registry、tooling catalog、static triage 与 sandbox sidecar review recipe；新增 `malware-analysis-pack-smoke.ps1` 与 pack inventory fixtures，验证 Go/PowerShell doctor、Go init/case doctor、`plan-subagents` route packet、promote review 和 no-write 边界，同时保持样本执行、联网、dump/debug/patch、authority/confirmed 与真实 artifact 零写入。
- Batch 86 完成：新增 `packs/vuln-research` 授权漏洞研究 pack skeleton，覆盖 manifest、managed docs、task handoff template、policy overlay 空 registry、tooling catalog、crash triage 与 repro sidecar review recipe；新增 `vuln-research-pack-smoke.ps1` 与 pack inventory fixtures，验证 Go/PowerShell doctor、Go init/case doctor、`plan-subagents` route packet、promote review 和 no-write 边界，同时保持主动扫描、fuzz、exploit replay、真实目标复现、authority/confirmed 与真实 artifact 零写入。
- Batch 87 完成：新增 `packs/ctf` 授权 CTF/靶场 challenge pack skeleton，覆盖 manifest、managed docs、task handoff template、policy overlay 空 registry、tooling catalog、challenge triage 与 local repro review recipe；新增 `ctf-pack-smoke.ps1` 与 pack inventory fixtures，验证 Go/PowerShell doctor、Go init/case doctor、`plan-subagents` route packet、promote review 和 no-write 边界，同时保持远程连接、bruteforce、fuzz、exploit replay、flag/authority/confirmed 与真实 artifact 零写入。
- Batch 88 完成：新增 `packs/unpack-pe` 授权 PE unpacking / loader triage pack skeleton，覆盖 manifest、managed docs、task handoff template、policy overlay 空 registry、tooling catalog、PE static triage 与 loader unpack review recipe；新增 `unpack-pe-pack-smoke.ps1` 与 pack inventory fixtures，验证 Go/PowerShell doctor、Go init/case doctor、`plan-subagents` route packet、promote review 和 no-write 边界，同时保持样本执行、debug/dump/patch、unpacked binary、authority/confirmed 与真实 artifact 零写入。
- Batch 89 完成：新增 `packs/ollvm` 授权 OLLVM / native obfuscation analysis pack skeleton，覆盖 manifest、managed docs、task handoff template、policy overlay 空 registry、tooling catalog、control-flow triage 与 MBA simplification review recipe；新增 `ollvm-pack-smoke.ps1` 与 pack inventory fixtures，验证 Go/PowerShell doctor、Go init/case doctor、`plan-subagents` route packet、promote review 和 no-write 边界，同时保持样本执行、trace/dump/patch、deobfuscated binary、authority/confirmed 与真实 artifact 零写入。
- Batch 90 完成：新增 `packs/android-native` 授权 Android native / JNI / NDK 分析 pack skeleton，覆盖 manifest、managed docs、task handoff template、policy overlay 空 registry、tooling catalog、APK native triage 与 emulator hook review recipe；新增 `android-native-pack-smoke.ps1` 与 pack inventory fixtures，验证 Go/PowerShell doctor、Go init/case doctor、`plan-subagents` route packet、promote review 和 no-write 边界，同时保持设备连接、Frida attach、hook 执行、抓包、authority/confirmed 与真实 artifact 零写入。
- Batch 91 完成：新增 `packs/generic-binary-re` 授权通用二进制逆向 pack skeleton，覆盖 manifest、managed docs、task handoff template、policy overlay 空 registry、tooling catalog、static binary triage 与 function behavior review recipe；新增 `generic-binary-re-pack-smoke.ps1` 与 pack inventory fixtures，验证 Go/PowerShell doctor、Go init/case doctor、`plan-subagents` route packet、promote review 和 no-write 边界，同时保持样本执行、debug/trace/dump/patch、authority/confirmed 与真实 artifact 零写入。
- Batch 92 完成：新增 `rekit/tests/pack-smoke-lib.ps1`，将 `web-security`、`malware-analysis`、`vuln-research`、`ctf`、`unpack-pe`、`ollvm`、`android-native` 与 `generic-binary-re` pack smoke 迁移为 thin configuration wrapper，复用 Go/PowerShell doctor、Go init、case doctor、`plan-subagents` route packet、promote review managed-doc candidate 与 no-write 边界检查，降低后续安全领域 pack smoke 的重复维护成本。
- Batch 93 完成：新增 `rekit/tests/pack-smoke-matrix.ps1`，可默认运行 8 个安全领域 skeleton pack smoke，也支持 `-Packs` 子集、逗号分隔、`all`、去重、未知 pack guard 与 `-FailFast`，在不改变各 pack smoke 语义的前提下降低手动串联验证的遗漏和失败定位成本。
- Batch 94 完成：`pack-smoke-matrix.ps1` 新增 `-Format text|json`，默认保持文本输出，JSON envelope 提供 `schemaVersion/command/isMutation/workRoot/failFast/packCount/failedCount/ok/packs/results[]`，每个 result 含 pack、script、success、exitCode、elapsedMs 与原始 output，便于 CI 或后续编排稳定消费 pack smoke 结果。
- Batch 95 完成：`pack-smoke-matrix.ps1` 新增 `-DiscoveryOnly` 只读一致性检查，对比 `/rekit packs -Format json` 中 schema-valid skeleton packs、matrix 显式清单和 `*-pack-smoke.ps1` wrappers，报告 missing/extra/orphan/missing-script 与显式 excluded packs，避免新增 skeleton pack 后遗漏 smoke 或 matrix 清单。
- Batch 96 完成：新增 `rekit/tests/pack-smoke-matrix-selftest.ps1`，覆盖 matrix discovery 文本/JSON、matrix JSON envelope、重复 pack 去重、文本输出和 unknown pack guard，锁定 pack smoke matrix 自身的输出契约与错误边界。
- Batch 97 完成：新增 `rekit/tests/README.md` 测试维护指南，按 façade/inventory/pack matrix、pack skeleton、sync/promote/init/attach、workstream/ledger/gate/Agent Team 分类梳理 smoke 入口、适用场景、默认 WorkRoot、安全边界和推荐验证组合。
- Batch 98 完成：补齐 `rekit/tests/README.md` 在 `CLAUDE.md`、`docs/pack-authoring.md`、`docs/go-runtime-migration.md` 与 `docs/agent-team-usage.md` 中的维护入口和验证选择说明，让测试指南成为 smoke 选择的单一导航入口。
- Batch 99 完成：新增 `rekit/tests/catalog.json` 机器可读测试导航目录，记录主要 smoke 的 category、purpose、recommendedFor、WorkRoot/CaseRoot 支持、安全边界和相关文档；该 catalog 只供选择测试或后续 CI/自动化消费，不自动执行命令。
- Batch 100 完成：新增 `rekit/tests/catalog-smoke.ps1`，校验 `catalog.json` schema、唯一 id、字段完整性、脚本/文档路径、pack smoke entry 与 discovery guard 一致性，并将 catalog 自测纳入 tests README 和 README 维护入口。
- Batch 101 完成：扩展 `rekit/tests/catalog.json` 覆盖全部 `rekit/tests/*.ps1` smoke/helper，新增 `case-scaffold` 分类，并增强 `catalog-smoke.ps1` 枚举脚本目录、在新增脚本未登记时失败，保持机器可读测试导航与实际维护入口同步。
- Batch 103 完成：新增 Go-first release invariant tests，将 `catalog.json` schema、全部 `rekit/tests/*.ps1` 覆盖、recommended minimum、related docs 存在性，以及 schema-valid skeleton pack 与 catalog、wrapper、pack smoke matrix 的一致性迁入 `go test ./...`；同时把 `go vet ./...` 纳入 tests guide 和 catalog recommended minimum，并补 Go 默认 `vmp-re` status JSON contract。PowerShell catalog/matrix smoke 保留为 compatibility/parity 层，不继续作为新的编排 owner 扩张。
- Batch 104 完成：低风险 read-only `/rekit` façade 默认委托 Go，`status`、`packs`、kit/case `doctor/validate` 不再需要 `REKIT_GO_ENABLE=1` 即走 Go backend；`REKIT_GO_DISABLE=1` 仍可强制回退 PowerShell，扩展 preview/review 安全集合继续要求显式 `REKIT_GO_ENABLE=1`。`facade-smoke.ps1` 与 `pack-inventory-smoke.ps1` 增加默认委派 fake backend sentinel 和 disable fallback 覆盖，文档同步 Go read-only runtime 已成为默认路径。

### Fixed

- 修复 Windows PowerShell 5.1 解析含非 ASCII 的 runtime `.ps1` 文件时可能因 UTF-8 无 BOM 产生 mojibake 并破坏语法的问题。
- 修复裸 `attach` 后执行 `sync` review 时，空 `CLAUDE.local.md` host text 被 PowerShell 参数绑定拒绝的问题，并统一空白 host 下 sync review 与 `sync -Apply` 的 managed block 写入结果。
- 修复 G3.7 promote candidates Go 单测与 apply smoke 的清理策略：对已有 `promote-candidates/index.json`、tooling candidates 和原始目录结构做 snapshot/restore，避免验证覆盖人工候选索引或残留空 backup/candidate 目录污染 pack。

### Verified

- 使用临时 case 完成 `init/status/doctor/sync/promote` 与 `attach/status/sync/sync -Apply/doctor/promote` smoke test，验证 case-local 边界与 review-first 流程。

### Changed

- 更新 README 顶部定位，将项目说明从单纯 context kit 扩展为面向网络安全研究与安全工程任务的 Claude Code Agent Team 框架，并区分维护本仓库与接入安全 case（当前以 `vmp-re` 为例）的入口。
- 扩展 `vmp-re` 工作流与工具路由，加入轻到重分析路线、重型工具升级门禁和 `ida-agent-bridge` 候选工具说明。
- 将日常 `/rekit` 工作流收敛为 `overview / continue / start / handoff`，移除公开的 `board / auto / lane / policy` 旧入口。
- 将原集中式 B3 PowerShell runtime 拆分为 `B3.Core/State/Policy/Lane/Auto/Commands` 模块，并新增项目级 handoff 生成。
- 更新 README、skill、case shim、policy/reference/prompt 文档，用户层统一使用“工作线 / 主线 / 功能支线”术语。
- 明确 `/rekit overview` 只是项目总览，`/rekit continue main|<name>` 才选择工作线；`/rekit handoff` 改为项目级接手索引，并新增指定工作线 handoff。

## 0.2.0 - 2026-06-12

### Added

- 新增目录级 canonical `/rekit` skill，clone 后在 kit 仓库内直接可用。
- 新增 `rekit/rekit.ps1` runtime 与 Manifest / Instance / Sync / Promote / Validate 模块。
- 新增 case-local `/rekit` shim 模板与 `.rekit/instance.yml` / `state.json` 实例模型。
- 新增 `promote` 工作流，用于将 case 中可复用 managed docs 生成候选或显式写回 pack。
- 新增 `packs/vmp-re/tooling/`，保存工具 catalog、recipes、脚本模板化清单、补丁/止损经验和 promote 候选。
- 新增 `docs/promote-sync.md`。
- 新增 manifest 路径 root containment 检查，避免 managed/promote/tooling 路径越出 case 或 pack 根目录。

### Changed

- `packs/vmp-re/manifest.yml` 升级为 sync/promote/managed block/tooling/budget 的单一事实源。
- `sync/promote/doctor` 对显式 case target 增加 attached-case guard，避免拼错路径时隐式创建假 case 或从普通目录回流。
- `promoteDenyPatterns` 收紧为覆盖 artifact/capture/trace/dump 路径与更通用 ctx/round 状态。
- `bootstrap.ps1`、`update.ps1`、`validate.ps1` 改为兼容 wrapper，转调 `rekit/rekit.ps1`。
- README 与 design 文档改为以 `/rekit`、runtime、pack、instance 四层模型说明，并去除具体 case 路径示例。

## 0.1.0 - 2026-06-12

### Added

- 初版 `vmp-re` pack。
- 按需路由、渐进式披露、工具链路由、singleton handler 复核模板。
- `bootstrap.ps1` / `update.ps1` / `validate.ps1`。
- 四层目录模型：`kits/`、`cases/`、`tools/`、`shared-artifacts/`。
