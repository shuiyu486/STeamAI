# Template pack policy overlays

This directory is intentionally minimal. New packs can add overlay files here when they need to specialize common policies.

Keep overlays pack-generic and free of case-specific paths, sample names, addresses, traces, dumps, or artifacts.
