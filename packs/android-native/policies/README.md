# Android native policy overlays

This pack currently relies on common policies and keeps overlays empty until repeated case experience justifies an Android-native-specific rule.

Do not add case-specific APK names, package names, hashes, DEX/SO binaries, device or emulator ids, hook scripts, traffic captures, dumps, traces, patch bytes, keystores, tokens, credentials, raw artifacts, customer context, endpoints, or absolute paths here.
