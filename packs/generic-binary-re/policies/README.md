# Generic binary RE policy overlays

This pack currently relies on common policies and keeps overlays empty until repeated case experience justifies a generic-binary-re-specific rule.

Do not add case-specific sample names, hashes, binaries, dumps, traces, memory snapshots, patch bytes, full function bodies, symbol tables, IOCs, customer context, credentials, raw artifacts, or absolute paths here.
