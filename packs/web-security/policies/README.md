# Web-security policy overlays

This pack currently relies on common policies and keeps overlays empty until repeated case experience justifies a web-specific rule.

Do not add case-specific targets, credentials, request/response bodies, vulnerability details, customer names, HAR/pcap paths, or scan artifacts here.
