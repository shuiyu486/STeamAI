# `{{FINDING_ID}}` — `{{TITLE}}`

- Owner：`{{OWNER}}`
- Verifier：`{{VERIFIER_OR_NONE}}`
- 状态：`draft`
- Confidence：`{{CONFIDENCE}}`

## 结论

`{{CLAIM}}`

## Evidence

- `{{EVIDENCE_REF}}`

## 尚未证明

`{{UNPROVEN_OR_LIMITATIONS}}`

每个 finding 默认一名 owner、最多一名 verifier。没有 evidence 引用的内容只能作为 hypothesis，不能作为交付结论。

仅当问题跨越实质性处理/状态边界时，在现有结论正文写一条有界连接链，说明条件、传递/转换依据、观察前的预测与检查结果；保留原预测，反证出现后明确修正哪一连接。综合 F 直接引用全部支撑连接的 E，局部 F 只能作为导航，不假定 F→F 递归校验。缺连接时保留局部结论并在“尚未证明”中限定整体范围；两个局部 accepted 不等于整体 accepted，须由 Reviewer 对综合 claim 与相关 E 做整体复核。单点任务不强制填写这些内容。
